# LAGDA Backend — Architecture Invariants

Rules every `BACKEND-XX` command checks before modifying backend code.

**Enforcement column is honest.** "Documentation" means a human or a reviewing
command must catch a violation — nothing fails automatically. This repository has
already shipped one contract that existed but was never consumed
(`RouteMeta.status`, declared on 225 routes, read by no code, drifted until three
routes misreported themselves). Invariants become executable as the packages they
govern are created; until then the column says so rather than implying safety
that does not exist.

**Current enforcement is tracked in [`ENFORCEMENT_MATRIX.md`](./ENFORCEMENT_MATRIX.md)**,
which BACKEND-01 introduced. That file is authoritative for *what executes*; this
one is authoritative for *what the rules are*.

| ID | Invariant | Enforcement today |
|---|---|---|
| **INV-001** | No PDF library import outside `packages/sealing`. | **ENFORCED** — ESLint, backend + frontend |
| **INV-002** | Only the signing-completion use case may invoke `DocumentSealer`. | Documented only → BACKEND-09 |
| **INV-003** | Workspace-owned data access must be workspace scoped at the repository boundary. | Documented only → BACKEND-06/07 |
| **INV-004** | API routes may not contain primary domain logic. | Documented only → BACKEND-11 |
| **INV-005** | `core` may not depend on infrastructure packages. | **ENFORCED** — ESLint (BACKEND-01) |
| **INV-006** | Frontend source files are not backend runtime dependencies. | **ENFORCED** — architecture test (BACKEND-01) |
| **INV-007** | Shared API/domain contracts originate from `@lagda/contracts`. | **Partially enforced** — ESLint bars infrastructure from `contracts`; origination needs BACKEND-02 |
| **INV-008** | Public API types may not expose infrastructure-library types. | Documented only → BACKEND-09/11 |
| **INV-009** | eNotary implementation is outside current scope. | Documentation + frontend disclosure tests |
| **INV-010** | Original accepted uploaded documents are immutable. | Documented only → BACKEND-05 |
| **INV-011** | Operational logs and evidence/audit records are separate concerns. | Documented only → BACKEND-11/12 |
| **INV-012** | Retry-sensitive operations must support durable idempotency. | Documented only → BACKEND-03 |
| **INV-013** | Business state transitions must be explicit and validated. | Documented only → BACKEND-07 |
| **INV-014** | Tenant isolation is tested, not assumed. | Documented only → BACKEND-07 |
| **INV-015** | Later architectural changes require an ADR or an explicit backend command. | Process |

## Additional invariants from repository inspection

These were derived from findings in
[`architecture.md` §7](./architecture.md#7-repository-findings), not from the
generic rule set.

| ID | Invariant | Why | Enforcement today |
|---|---|---|---|
| **INV-016** | `WorkspaceId` is a branded type and is used for every workspace-owned reference in `@lagda/contracts`. | The tenant key is currently a plain `string` in ~78% of its declarations (F-1). Branding it is what makes INV-003 checkable by the compiler rather than by review. | Documentation → **BACKEND-02 blocker** |
| **INV-017** | The backend redactor matches sensitive keys by substring/pattern and recurses through arrays. | The frontend redactor does neither: `resetToken` and `sessionToken` pass through, and arrays are returned unredacted (F-2). Server logs are persistent, so porting it verbatim would be worse than having none. | Documentation → unit test at BACKEND-01 |
| **INV-018** | Status transition rules are owned by `core`. Frontend `*Status` unions are vocabulary, not lifecycle authority. | 50 status unions exist with zero transition tables (F-3). There is no state machine to port. | Documentation → `core` |
| **INV-019** | No invariant may be recorded here without an enforcement plan naming the command that makes it executable. | Prevents this file becoming the next `RouteMeta.status` (F-4). | This table's enforcement column |
| **INV-020** | No package dependency cycles, and `package.json` workspace dependencies must agree with TypeScript project references. | The graph is declared in two places. If they drift, `tsc --build` orders packages by one graph while the code depends on the other, and a cycle makes the build order non-deterministic. Added by BACKEND-01 when the packages became real. | **ENFORCED** — architecture test |
| **INV-021** | Shared contract types are DERIVED from runtime schemas (`Static<typeof XSchema>`), never declared alongside them. | A hand-written type and a hand-written validator for the same structure drift, and the drift is invisible until a request is rejected in production. ADR-002. | Documented → review |
| **INV-022** | `@lagda/contracts` stays browser-compatible: no Node built-ins, no environment reads, no side effects on import. | The frontend is a first-class consumer. A `node:crypto` import would break its build, and a side effect on import would make a contract package do something. | **PARTIALLY ENFORCED** — ESLint bars infrastructure; a browser bundle check needs the frontend to consume it (OD-005) |
| **INV-023** | Public response contracts set no field that the operation does not require, and separate public from authenticated shapes as distinct types. | Verification is unauthenticated. One shape guarded by a boolean has to be checked correctly at every call site forever; two types cannot leak into each other. | **ENFORCED for verification** — `additionalProperties: false` plus tests asserting the public schema rejects owner-only fields |
| **INV-024** | Contracts contain no non-JSON values — no `Date`, `BigInt`, `Map`, `Set`, `Buffer`, or class instances. | A `Date`-typed field describes something the wire never carries; the receiver always gets a string. Making the string the contract removes the asymmetry. | **ENFORCED for verification** — round-trip test |
| **INV-025** | A serialized status value is an API contract. Changing one is a compatibility change, not a refactor. | `pending` → `awaiting` is invisible to TypeScript and breaks every stored record and client. | Documented → review |
| **INV-026** | Every API error response uses the canonical `ApiError` envelope from `@lagda/contracts/api`. | One shape, or every client writes a parser per endpoint. | **ENFORCED for the schema** — runtime schema + tests; route adoption needs BACKEND-11 |
| **INV-027** | Clients branch on `error.code`, never on `error.message`. HTTP mapping never inspects a message either. | Copy and localization change; `message.includes("not found")` is how status mapping silently breaks. | Documented → review |
| **INV-028** | Unknown properties in mutation request bodies are rejected. | Silently stripping hides stale clients and typos, and rejecting is the cheapest defence against mass assignment. | **ENFORCED** — `additionalProperties: false` + tests |
| **INV-029** | List sort keys are closed typed unions per endpoint, never free strings. | A free sort key reaching a repository becomes `ORDER BY ${input}`. | **ENFORCED** — `sortSchema()` + test rejecting `id; DROP TABLE documents` |
| **INV-030** | API timestamps are RFC 3339 UTC strings, never `Date` objects. | `JSON.stringify` produces a string, so a `Date`-typed field describes something the wire never carries. | **ENFORCED for extracted contracts** — pattern + tests |
| **INV-031** | Request and response contracts stay distinct types where server-owned or write-only fields exist. | Structural reuse is how `createdAt` becomes settable and a password reaches a response. | Documented → review |
| **INV-032** | Route handlers map domain results into declared response contracts. A persistence row is never serialized directly. | It is how persistence-only and secret fields leak. | Documented → BACKEND-11 |
| **INV-033** | Core never reads the current time. Time-dependent rules take `now` as a parameter. | A hidden clock read makes expiry logic untestable without stubbing globals, and non-reproducible across dates. | **ENFORCED** — purity test |
| **INV-034** | Core never generates identifiers, tokens, or randomness. | Identity comes from the application layer; a domain that invents IDs cannot be replayed. | **ENFORCED** — purity test |
| **INV-035** | Lifecycle state changes go through named actions. No generic `setStatus`/`setState`/`setWorkspaceId`. | A generic setter makes the transition table decorative — every invariant becomes bypassable in one line. | **ENFORCED** — purity test |
| **INV-036** | Terminal signing states never transition back to an active state. | A completed transaction is legally significant history. Corrections create a new transaction. | **ENFORCED** — every terminal state × every action |
| **INV-037** | The frontend does not depend on `@lagda/core`. | Core is backend-only. Shared needs belong in `@lagda/contracts`. | Documented → review |
| **INV-038** | Domain events and errors carry no infrastructure concerns — no HTTP status, log level, severity, queue name, or delivery detail. | Core must not decide how a failure is presented or recorded. | **ENFORCED for errors** — `DomainError` has no such fields |
| **INV-039** | Application use cases may not import concrete infrastructure, including LAGDA's own `@lagda/db`, `@lagda/storage` and `@lagda/sealing`. | Those packages implement application's ports; importing them inverts the dependency and creates a cycle. Only composition roots import both sides. | **ENFORCED** — ESLint |
| **INV-040** | Workspace-owned repository ports require workspace scope in their signature. | Tenant isolation must not depend on a caller remembering to filter. A method resolving a member from any workspace is a cross-tenant read waiting to happen. | **ENFORCED by port shape** — no unscoped lookup exists |
| **INV-041** | Application business time comes from `Clock`; generated entity IDs come from explicit generators. | Hidden `Date.now()` or `randomUUID()` makes a use case untestable and non-reproducible. | **ENFORCED** — tests assert both |
| **INV-042** | Application errors contain no HTTP status codes, headers, or log levels. | One use case must serve an HTTP route, a worker, and a future partner API. | **ENFORCED** — test asserts no status property |
| **INV-043** | Use cases receive resolved actor context, never raw HTTP, session, cookie, or token objects. | A worker must be able to invoke the same use case without fabricating a request. A token in an actor object reaches logs. | Documented → review |
| **INV-044** | A tenant-scoped lookup for another workspace's resource is indistinguishable from one that does not exist. | Distinguishing them confirms the existence of another tenant's data to anyone who can guess an ID. | **ENFORCED** — anti-enumeration test |
| **INV-045** | External side effects are not treated as durable simply because a transaction committed. | Publishing before commit describes state that may not exist; after commit without an outbox, the event is lost if the process dies. | Documented → BACKEND-06/16 |
| **INV-046** | Database access is confined to `@lagda/db`. Kysely, `pg` and row types never appear elsewhere. | The query layer is an implementation choice, not part of the domain. Application must survive replacing it. | **ENFORCED** — ESLint |
| **INV-047** | Workspace-owned relationships preserve tenant identity at the DATABASE level via compound keys where practical. RLS, if adopted, is defence in depth and never the only boundary. | A child FK referencing only a parent ID lets a row in workspace A reference a parent in workspace B, with nothing but application code preventing it. | **PARTIALLY ENFORCED** — the compound-key target exists; referencing tables arrive in BACKEND-07 |
| **INV-048** | Source-controlled migrations are the sole schema-change mechanism, and applied migrations are immutable. | Manual production changes are drift with no audit trail. Editing an applied migration makes history unreproducible. | **ENFORCED** — CI migrates from zero |
| **INV-049** | Business-significant timestamps are persisted explicitly from the application `Clock`, distinct from technical row timestamps. | Two clocks for one business event is a contradiction discovered later, in evidence. | Documented → review |
| **INV-050** | Uploaded document bytes are not stored in PostgreSQL. The database holds metadata, references and hashes. | Binaries in a transactional database wreck backup, replication and restore time. Changing it needs an ADR. | Documented → BACKEND-17 |
| **INV-051** | SQL values are always parameterized; dynamic identifiers come from code-level whitelists. | The shortest path from a query parameter to SQL injection. | Documented → review |
| **INV-052** | Database integration tests use real PostgreSQL, and must not mutate schema. | SQLite would not exercise timestamptz, compound constraints or SQLSTATE. TRUNCATE clears rows but not DDL, so a schema-mutating test leaks a broken schema into later runs. | **ENFORCED** — real PostgreSQL in CI |
| **INV-053** | Database error classification uses SQLSTATE, never message text. | `message.includes("duplicate key")` breaks on an upgrade or a non-English locale, silently. | **ENFORCED** — SQLSTATE helpers, tested |
| **INV-054** | Every workspace-owned repository method requires workspace scope AND the transaction context. No optional tenant parameter, no `skipTenantCheck`, no bypass flag. | Optional tenancy defeats the invariant, and a bypass flag becomes the security trap everyone reaches for. Reads take the transaction because RLS context is transaction-local. | **ENFORCED** — port shape + RLS |
| **INV-055** | Tenant context is transaction-local (`SET LOCAL`), established in exactly one place, and never session-level. | A session-level setting rides a pooled connection into the next request — a silent, intermittent, load-dependent cross-tenant read. | **ENFORCED** — leak tests incl. after rollback |
| **INV-056** | Missing tenant context fails CLOSED: no rows, not all rows. | If absent context meant unrestricted, every bug that loses context becomes a full cross-tenant read. | **ENFORCED** — tested |
| **INV-057** | The runtime database role owns no tenant tables and lacks `BYPASSRLS`; tenant tables use `FORCE ROW LEVEL SECURITY`. | An owner bypasses RLS unless forced. Testing as a superuser gives false confidence while production leaks. | **ENFORCED** — asserted before every RLS test |
| **INV-058** | RLS is defence in depth and never replaces explicit repository predicates. | A query relying on an invisible policy is one nobody can review by reading it. | **ENFORCED** — both layers tested separately |
| **INV-059** | `workspace_id` is immutable for tenant-owned resources; there is no generic reassignment. | Moving a row between tenants is a privileged audited operation, not an ordinary update. | **ENFORCED** — RLS `WITH CHECK` rejects it |
| **INV-060** | A cross-tenant lookup is indistinguishable from an absent resource. | Any difference confirms the resource exists elsewhere. | **ENFORCED** — application + DB |
| **INV-061** | Adding a workspace-owned table requires updating TENANCY_MODEL.md and TENANCY_TEST_MATRIX.md, with RLS, compound FK, tenant index and cross-tenant tests. | Tenancy applied to some tables and forgotten on others is the realistic failure mode. | Process → review |
| **INV-062** | Repository ports are declared by `@lagda/application`; `@lagda/db` implements them. No row or query-library type escapes `@lagda/db`. | An interface defined in `db` and imported by `application` inverts the dependency the architecture rests on. | **ENFORCED** — ESLint + package boundaries |
| **INV-063** | Tenant-owned repositories are obtained from a unit of work and bound to one workspace. Scope is never a method parameter. | A workspace parameter makes a cross-tenant call *typeable*; binding makes it unsayable. A control depending on a runtime policy to catch a compile-time-expressible error is weaker than one where the error cannot be written. | **ENFORCED** — port shape |
| **INV-064** | A repository refuses to persist a record whose workspace differs from its scope, and never rewrites it to match. | Silently rewriting turns a programmer error into a data-corruption event, moving a record between tenants. | **ENFORCED** — contract suite, both adapters |
| **INV-065** | Every repository in one unit of work shares the same database transaction. | Independently built repositories can hold the pool while another holds the transaction — atomicity that looks correct and is not. | **ENFORCED** — rollback test against PostgreSQL |
| **INV-066** | Persistence errors are classified by SQLSTATE and constraint name, never message text; unknown errors pass through untranslated. | Message parsing breaks on an upgrade or locale. Downgrading an unknown error to "conflict" or "not found" tells a caller data is absent when the database is unreachable. | **ENFORCED** — tested |
| **INV-067** | State-sensitive writes use conditional updates rather than read-then-write. | Two concurrent readers of the same value both write, and the second silently overwrites the first. | **ENFORCED** — stale-writer test |
| **INV-068** | Repositories persist and retrieve only. No email, queue, webhook, sealing or audit side effects. | Hidden side effects make every caller's behaviour unpredictable. | Documented → review |
| **INV-069** | The repository behavioural contract runs against both the in-memory fake and PostgreSQL. | Divergence means either the fake is lying to application tests or the adapter is wrong. Only the PostgreSQL run is security proof. | **ENFORCED** — both suites |
| **INV-070** | `DocumentSealer` exposes exactly ONE operation, `seal()`. Merging, hashing and certificate rendering stay private to `@lagda/sealing`. | Every extra method is a call site a future remote signing service must reproduce. A seam becomes a rewrite when twenty callers each learned a different piece of it. | **ENFORCED** — method count asserted; probed |
| **INV-071** | `DocumentSealer` is declared in exactly one file, and no second copy exists in any package. | Two definitions typecheck, drift, and give the two layers different contracts. | **ENFORCED** — exact-path assertion |
| **INV-072** | Document bytes cross the seam as `Uint8Array`, never Node's `Buffer`. | `Buffer` is Node-only; a signing service in another runtime could not satisfy a contract demanding one. | **ENFORCED** — asserted on comment-stripped source; probed |
| **INV-073** | The final artifact's digest is computed AFTER every byte-changing step, from the exact bytes returned. | A digest taken from an intermediate buffer fails against the document people actually hold, and fails silently — both values look like valid digests. | **ENFORCED** — compared against an independently computed digest |
| **INV-074** | Hashes are named for the artifact they identify (`preparedDocumentHash`, `signedDocumentHash`). Never one field or column called `hash`. | An ambiguous name survives exactly as long as one person remembers which artifact it meant. | Documented → review (see OD-022: `Sha256Digest` is unbranded, so the two are mutually assignable) |
| **INV-075** | The sealer never mutates the caller's document buffer. | pdf-lib may write through the array it is given; the caller still needs those bytes to match the recorded digest. | **ENFORCED** — byte-equality test |
| **INV-076** | Every sealed artifact records `sealScheme`, `sealVersion` and `digestAlgorithm` from the first record written. | Retrofitting is impossible — the information is gone. A second scheme makes every earlier artifact ambiguous. | **ENFORCED** — exact metadata assertion |
| **INV-077** | The completion certificate is a SEPARATE artifact, never appended to the sealed document. | Appending changes the signed document's page count, so the file delivered is no longer the file participants signed. Handoff §15 stores them separately. | **ENFORCED** — page-count and inequality tests |
| **INV-078** | The sealer reads no clock, no randomness and no environment. `sealedAt` and `verificationId` are inputs. | A hidden clock makes output non-reproducible, and a sealer that mints verification IDs hands LAGDA's identifier namespace to a future third-party service. | **ENFORCED** — source scan on comment-stripped source |
| **INV-079** | No pdf-lib error, message or type reaches a caller. Failures are LAGDA-owned types with a stable `code` and an explicit `retryable`. | A caller that must understand pdf-lib's exceptions could not be satisfied by a Java or .NET signer. `retryable` is what stops the pipeline retrying a permanently malformed document forever. | **ENFORCED** — tested, including the raw `TypeError` path |
| **INV-080** | SHA-256 hashing happens in exactly one file. | Several implementations across layers is how one ends up base64 and another hex, and a verification comparison silently never matches. | **ENFORCED** — exact-path assertion on `node:crypto` imports |
| **INV-081** | The PDF coordinate flip happens in exactly one function. | A miscomputed rectangle does not crash — it places a signature in the wrong half of a structurally valid page. | **ENFORCED** — three boundary tests on `toPdfRect` |
| **INV-082** | Sealing is never invoked inside a database transaction. | It is slow, external, and cannot be rolled back when the commit later fails. | Documented → review (no consumer exists yet) |
| **INV-083** | Every architectural boundary test is verified by deliberately violating it, including a negative control proving the permitted case still passes. | A boundary test that cannot fail proves nothing. This command's probes exposed two dead detectors and four dead lint rules. | **ENFORCED** — by process; probe results recorded in SEALING_TEST_MATRIX.md |
| **INV-084** | Signing evidence is append-only. The runtime database role holds INSERT and SELECT on evidence tables and is explicitly revoked UPDATE and DELETE. | Handoff §32 requires an append-only store. Repository discipline alone is one careless method from being untrue. | **ENFORCED** — `has_table_privilege` asserted, with a negative control; probed by granting UPDATE |
| **INV-085** | Evidence immutability is an OPERATIONAL control and must never be described as cryptographic non-repudiation. | It does not prove a database administrator could not alter a row. Overstating a security property is worse than not having it. | Documented → review |
| **INV-086** | Every evidence, artifact, seal and verification row carries `workspace_id` as a first-class column. Tenancy is never inferred through a join. | Evidence is the most sensitive data LAGDA holds, and a join is one refactor from being an outer join that leaks. | **ENFORCED** — schema + RLS + tests |
| **INV-087** | An evidence event type is a historical FACT, never a status. | `document-viewed` is permanent; the recipient's status may still be awaiting-signature. Conflating them makes the timeline and the state machine fight. | **ENFORCED** — no unique constraint on (request, type); repeat events tested |
| **INV-088** | A recipient actor is not a `UserId`. Actor identity is a discriminated union, and a `system` actor carries no ID. | External signers have no LAGDA account. A synthetic user for the expiry worker makes "who did this" unanswerable for every automated action. | **ENFORCED** — DB CHECK + union type |
| **INV-089** | `occurred_at` comes from the application `Clock`; `recorded_at` is stamped by the database and is not application-settable. | "When this row landed" is not something a caller can honestly claim to know, and a client-supplied occurrence time is not evidence. | **ENFORCED** — tested with a past `occurred_at` |
| **INV-090** | Evidence timelines are ordered by `(occurred_at, evidence_event_id)`. A timestamp alone is not a total order. | Two recipients can act in the same millisecond, leaving the planner free to choose. Insertion order is never a timeline. | **ENFORCED** — tie test; probed by dropping the tiebreak |
| **INV-091** | One artifact row per byte-distinct artifact. A digest, size, storage reference or type is never overwritten. | An overwritten digest destroys the only evidence that the earlier bytes existed. | **ENFORCED** — no UPDATE privilege |
| **INV-092** | Artifact digests are constrained by an ALGORITHM-AWARE check, never a bare length rule. | A fixed 64-character check must be *dropped* to add SHA-512, and dropping a CHECK is how historical rows stop being validated. | **ENFORCED** — malformed and uppercase digests rejected |
| **INV-093** | No `UNIQUE` on an artifact digest. | Two identical PDFs legitimately share a SHA-256; the digest is content identity, not row identity. | **ENFORCED** — duplicate-digest test |
| **INV-094** | Artifact provenance is a compound-FK relation, tenant-safe, and an artifact is never derived from itself. | Ancestry inferred from naming conventions is not ancestry. | **ENFORCED** — cross-tenant and self-source rejected |
| **INV-095** | One finalization per signing request, enforced on BOTH `document_seals` and `verification_records`. | Resealing is not a product feature; a completion retry must converge rather than mint a competing verification identity. | **ENFORCED** — each constraint isolated by raw SQL and probed independently |
| **INV-096** | `seal_version` is constrained `> 0`, never to the current version. | Pinning it to 1 makes introducing version 2 a schema change under load. | **ENFORCED** — version 7 accepted, 0 rejected |
| **INV-097** | An unknown seal scheme is rejected at write time, never silently read back as the current scheme. | A misinterpreted historical artifact is worse than a rejected write. | **ENFORCED** — CHECK constraint tested |
| **INV-098** | A verification hash has exactly ONE writable home. Denormalized copies are forbidden. | Two independently writable copies drift on the first partial write, and the verification page and seal record would disagree about what was signed. | **ENFORCED** — schema; verification reaches hashes through `seal_id` |
| **INV-099** | Public verification exposes an explicit ALLOWLIST of named columns, never a filtered row type. | `Omit<Row, "secret">` silently starts exposing whatever a later command adds. | **ENFORCED** — exact key set asserted; probed by leaking `workspace_id` |
| **INV-100** | The public verification lookup returns `null` for absent AND restricted, indistinguishably. | A caller that could tell them apart would have an oracle for which verification IDs exist. | **ENFORCED** — tested |
| **INV-101** | A verification ID is not authentication. It never grants document or signing access. | It is published by design; treating a public value as a credential is how a verification page becomes a document leak. | Documented → review (BACKEND-42) |
| **INV-102** | Evidence relations are `ON DELETE RESTRICT`. Signing evidence never cascades away with a parent. | Evidence must survive the deletion of the document it describes. Retention and erasure are policy (BACKEND-55), not a cascade. | **ENFORCED** — schema |
| **INV-103** | Evidence rows carry no binary content and no secrets. `details` is bounded, versioned, and validated per event type. | Evidence is not a document store, and a payload with no cap quietly becomes one. | **ENFORCED** — 8 KiB CHECK; typed payload |
| **INV-104** | Evidence is created by the SERVER from the authenticated actor, the server clock, observed request context and authoritative state — never from a client-supplied event payload. | A client that can name its own event type, timestamp and IP is describing itself, not being observed. | Documented → BACKEND-11/36 must enforce at the route boundary |
| **INV-105** | Client IP is recorded only from server-observed connection context after trusted proxy configuration exists. | An `X-Forwarded-For` recorded before then is attacker-controlled text wearing the costume of evidence. | **ENFORCED** — column exists, nothing writes it; BACKEND-11/56 owns the writer |
| **INV-106** | HTTP frameworks are confined to `@lagda/api`. Application, core, contracts and db never import Fastify. | A use case that imports Fastify cannot be called by the worker, and a domain rule that knows about a Reply cannot be tested without one. | **ENFORCED** — architecture test with a negative control; probed |
| **INV-107** | Importing `@lagda/api` starts no listener. Server start is an explicit `startServer()` call. | An import that binds a port does so in every test run and in any tool that merely typechecks the package. | **ENFORCED** — no call at statement position in the entry point; probed |
| **INV-108** | Route modules may not import `@lagda/db`, `kysely`, `pg`, `@lagda/sealing` or `@lagda/storage`. The composition root may. | A route able to reach the query builder can issue an unscoped query, bypassing the unit of work and the tenant context it carries. | **ENFORCED** — directory-scoped ESLint; 9 probes including 3 negative controls |
| **INV-109** | Every route declares a response schema, and Fastify serializes through it. | Response schemas are a security boundary, not documentation: an undeclared field cannot escape. TypeScript cannot provide this. | **ENFORCED** — leaky-handler test; schema-count architecture test |
| **INV-110** | Unknown properties in request bodies are REJECTED, never stripped. | Stripping hides stale clients and typos; rejecting is also the cheapest defence against mass assignment. | **ENFORCED** — `removeAdditional: false`; probed by flipping it |
| **INV-111** | Framework and validator errors never reach a client. Every failure passes through one mapper. | A public contract that depends on Ajv's internals makes a validator upgrade a breaking API change. | **ENFORCED** — no `instancePath`/`schemaPath`/`ajv` in responses |
| **INV-112** | Unexpected errors return a generic body with no stack, SQL, path or library message. Detail goes to the log with the same request ID. | The envelope has no field to put a secret in; the request ID is what lets support find the line that does. | **ENFORCED** — credential-leak test; probed by returning `error.message` |
| **INV-113** | Every request carries one server-generated request ID, on every response including 404s, in the error body, and in every log line. | Support cannot correlate a reported failure if the failing response is the one without an ID. | **ENFORCED** — tested on success, 404 and 500 |
| **INV-114** | A client-supplied request ID is ignored. | It flows into logs, and a client-chosen value can carry CRLF into a response header. | **ENFORCED** — `requestIdHeader: false`; CRLF injection tested; probed |
| **INV-115** | Forwarded client IP is trusted only under explicit proxy configuration. `TRUST_PROXY=true` is rejected outright. | `true` trusts the whole chain, so any client could choose the IP recorded as signing evidence. | **ENFORCED** — config rejects it; spoofing tested both ways; probed |
| **INV-116** | Observed request metadata carries explicit provenance and is never read from a request body. | A client that can name its own IP is describing itself, not being observed. | **ENFORCED** — `IpProvenance` on the type; INV-104 governs the writer |
| **INV-117** | CORS origins are matched by exact equality. No wildcard, no substring, and never a wildcard with credentials. | `origin.includes("lagda.io")` admits `lagda.io.attacker.example`. | **ENFORCED** — lookalike origins tested; probed by substituting a substring check |
| **INV-118** | API startup never applies database migrations. | Every replica would race to alter the schema during a rolling deploy. | **ENFORCED** — architecture test on migration symbols |
| **INV-119** | Health and readiness are separate. Liveness never touches the database; readiness signals through the status code. | Restarting the API does not fix the database. A 200 carrying `not-ready` means every probe passes forever. | **ENFORCED** — both tested, including the database-down case |
| **INV-120** | Unauthenticated infrastructure routes disclose no environment, version, hostname or dependency detail. | Each helps fingerprint a deployment and none helps an orchestrator decide anything. | **ENFORCED** — health body key set asserted; readiness failure leak-tested |
| **INV-121** | Request and response bodies are never logged. Sensitive headers are removed, not masked. | Bodies carry passwords, OTPs, signature images and document content, and "redact the known keys" fails the moment a new key appears. | **ENFORCED** — explicit serializer allowlists; no body serializer exists |
| **INV-122** | Shutdown is idempotent and bounded, closes HTTP before the database, and continues past a failing target. | SIGTERM then SIGINT is ordinary; closing the pool first fails in-flight requests that were about to succeed. | **ENFORCED** — three unit tests |
| **INV-123** | API and worker are separate process roles. The API never starts a worker or a queue. | Coupling them means one cannot be scaled, deployed or restarted without the other. | Documented → review (worker not yet implemented) |
| **INV-124** | Operational logs are not signing evidence, and an evidence requirement is never satisfied by logging it. | Logs rotate, get sampled and may be shipped to a third party. Evidence is append-only with its own retention. | **ENFORCED** - no log/metric/telemetry table may be created; schema asserted |
| **INV-125** | Signing evidence never carries stack traces, SQLSTATE, retry counts or process diagnostics. | The reverse of INV-124. Debug detail is not business history. | Documented -> review |
| **INV-126** | `core` and `application` import no logging or metrics library. | A domain rule that logs cannot be tested without a logger; a use case taking one cannot be called by the worker. | **ENFORCED** - architecture test; probed |
| **INV-127** | Secrets, credentials, OTPs, signature content and document bytes are never logged, at any level including `trace`. | A lower level is not an exemption. | **ENFORCED** - capture tests per category; probed |
| **INV-128** | Request and response bodies are never logged by default. | A body may carry a password, an OTP, a signature image or field values, and "redact the known keys" fails on the first new key. | **ENFORCED** - no body serializer exists |
| **INV-129** | Redaction covers any depth and any key name, and scrubs error messages. | Pino path redaction misses top-level keys, depth beyond one level, and messages entirely - all three were proven to leak. | **ENFORCED** - deep walk + message hook; 7 tests fail when disabled |
| **INV-130** | Redaction must not over-match diagnostic fields. `code`, `errorCode`, `statusCode` and similar must survive. | Over-broad redaction destroys the logs it was meant to protect, and `errorCode` is what alerts branch on. | **ENFORCED** - asserted for nine field names |
| **INV-131** | Logs prefer stable resource IDs over PII-rich display values. | An ID is stable, less PII-rich, and unchanged when a customer renames something. A document title can reveal the legal matter. | Documented -> review |
| **INV-132** | Metric labels contain no unbounded identifier - `requestId`, `workspaceId`, `userId`, `documentId`, `signingRequestId`, `verificationId`, `email`, `ipAddress`. | One series per tenant per resource is a cardinality explosion, and metrics should carry no PII at all. | **ENFORCED** - catalog audit test; probed |
| **INV-133** | Metric names are a closed union. Dynamic names are impossible. | A per-tenant metric name is unbounded cardinality expressed as a name; a string parameter cannot prevent it. | **ENFORCED** - type system |
| **INV-134** | Application use cases stay observability-provider independent. Instrumentation wraps from the outside. | A logger parameter makes every caller supply one, including the worker. | **ENFORCED** - no use-case signature takes a logger |
| **INV-135** | Observability context is never the source of authorization or tenant enforcement. | If the store vanished, no query may change behaviour and no permission may change answer - only the logs get less useful. | **ENFORCED** - no `@lagda/db` file reads it; consumers are an exact named set |
| **INV-136** | Database telemetry emits no SQL parameters. Repository and method names identify an operation. | Parameters carry customer data; `repository`/`operation` answers the same question. | Documented -> review (no repository wrapped yet) |
| **INV-137** | Sealing telemetry exposes no PDF bytes, field values or signature content. | Duration, sizes, scheme and category answer every operational question without content. | Documented -> review (no sealing caller yet) |
| **INV-138** | Unexpected errors are operationally visible while public responses stay sanitized. | The request ID connects the safe body to the log line that has the stack. | **ENFORCED** - asserted from both sides |
| **INV-139** | Observability never changes a transaction boundary, and never writes telemetry to PostgreSQL on the request path. | It risks recursion during the outage it exists to explain. | **ENFORCED** - no telemetry table may exist |
| **INV-140** | An observability wrapper returns the original result and rethrows the original error, unchanged. | A wrapper that swallows a failure to log it converts an outage into a silent wrong answer. | **ENFORCED** - identity assertion; probed |
| **INV-141** | Logs, metrics and evidence keep separate semantic and retention models. | Three different retention decisions; conflating them means the strictest wins or, in practice, the loosest. | Documented -> BACKEND-55/66 |
| **INV-142** | Every process role emits structured records. No `console.*` in runtime code. | An unstructured line is invisible to aggregation, and migration failures are what people search for at 3am. | **ENFORCED** - architecture test across all packages and the migration runner |
| **INV-143** | Browser authentication uses opaque server-managed sessions, never a JavaScript-readable bearer credential. | A token JavaScript can read is a token XSS can exfiltrate, and a stateless token cannot be revoked. | **ENFORCED** - cookie policy + tests; no JWT dependency exists |
| **INV-144** | Raw session and CSRF tokens are never persisted. Only a domain-separated SHA-256 digest is stored. | A database dump must not yield working credentials. | **ENFORCED** - CHECK constraint rejects non-hex; integration test attempts a raw insert |
| **INV-145** | Session and CSRF digests are domain-separated. | Without it, the readable CSRF token could be submitted as a session cookie and match a stored session hash. | **ENFORCED** - tested; probed by removing the prefix |
| **INV-146** | Session cookies are HttpOnly always, and Secure in production. | HttpOnly is the control that stops XSS stealing the session. Production start fails if Secure is disabled. | **ENFORCED** - config rejects it; probed |
| **INV-147** | Session credentials never appear in a URL, a JSON body, a log or a metric. | URLs leak through history, referrers and logs. | **ENFORCED** - log-capture test; no route returns a token |
| **INV-148** | A session identifies a USER and confers no workspace authorization. | A user belongs to several workspaces, and a credential carrying authorization carries STALE authorization. | **ENFORCED** - actor key set asserted; no workspace column, asserted against the live schema |
| **INV-149** | Session rotation issues a fresh credential and invalidates the previous one. | The defence against session fixation. | **ENFORCED** - tested |
| **INV-150** | State-changing requests authenticated by cookie require a CSRF token bound to that session. | A token that merely matches a cookie is controlled by anyone who can set a cookie. | **ENFORCED** - cross-session test; probed |
| **INV-151** | CSRF tokens are cryptographically unpredictable and compared timing-safely. | String equality short-circuits and leaks how much of a guess was correct. | **ENFORCED** - timingSafeEqual; format tested |
| **INV-152** | GET and HEAD perform no business mutation, which is what makes their CSRF exemption safe. | A GET logout endpoint silently defeats the whole mechanism. | Documented -> review (no product mutation routes exist yet) |
| **INV-153** | A session-store failure is reported as a dependency failure, never as invalid authentication. | A mass logout during a database outage trains users to re-enter credentials exactly when something is wrong. | **ENFORCED** - resolver propagates; HTTP test asserts not-401 |
| **INV-154** | Every session rejection reason collapses to one public error. | Distinguishing expired from unknown is an oracle for which tokens exist. | **ENFORCED** - tested |
| **INV-155** | CORS and SameSite are defence in depth and never replace CSRF. | A cross-origin POST is still sent; CORS only governs reading the reply. | Documented -> review |
| **INV-156** | External recipient signing authentication is a separate model and never reuses the user session cookie. | An external signer is not an account holder; overloading the cookie would hand them a user session. | Documented -> BACKEND-34 |
| **INV-157** | The session table is GLOBAL_AUTHENTICATION - no workspace column, no RLS - and that is deliberate, not an omission. | Authentication precedes any workspace context, so a tenant policy could never be satisfied when it is needed. | **ENFORCED** - asserted against the live schema |
| **INV-158** | No development authentication bypass exists. | A security boundary that is real only in production is not a boundary. | **ENFORCED** - no environment-conditional auth path; fixtures are test-only |
| **INV-159** | Retry-sensitive operations use durable PostgreSQL-backed idempotency. No in-memory or Redis state is required for correctness. | A Map in one process protects nothing across instances or restarts. | **ENFORCED** - PostgreSQL is the only store; real two-transaction concurrency test |
| **INV-160** | Request IDs and idempotency keys are distinct. A request ID is never used as an idempotency key. | Request ID is new every attempt; an idempotency key is deliberately the same across retries. | **ENFORCED** - separate types; request ID excluded from the fingerprint |
| **INV-161** | An idempotency key authorizes nothing. Authentication, CSRF and authorization all still apply, including on replay. | The record is not a capability. A user who has lost access must not replay protected data. | Documented -> feature routes must order auth before replay |
| **INV-162** | Idempotency identity always includes scope and operation, never the key alone. | A raw key is not globally unique - two clients legitimately send the same UUID. | **ENFORCED** - unique constraint on all four parts; no repository method queries by key alone |
| **INV-163** | The same key with a different canonical fingerprint fails. It neither executes nor replays. | Replaying the first result for different input is a wrong answer, not a retry. | **ENFORCED** - tested; probed |
| **INV-164** | Fingerprints exclude session tokens, CSRF tokens, request IDs, IPs and user agents. | A session rotation between retries must not make the second look like a different business request. | **ENFORCED** - fingerprint takes the validated logical request only |
| **INV-165** | Canonicalization sorts object keys and PRESERVES array order. | Sorting arrays would make [A,B] and [B,A] recipient orders replay each other. | **ENFORCED** - tested; probed by sorting |
| **INV-166** | Concurrent duplicate claims are resolved atomically by PostgreSQL. No check-then-insert. | Two processes both see nothing and both proceed. | **ENFORCED** - single conditional INSERT; probed by removing ON CONFLICT |
| **INV-167** | Business mutation and idempotency completion commit atomically for PostgreSQL-contained operations. | Otherwise a crash leaves a mutation with no claim, or a claim with no mutation. | **ENFORCED** - real transaction test with a second table |
| **INV-168** | Transient failures and unexpected 5xx are never stored as completed results. | A defect or outage must be retryable, not cached as truth. | **ENFORCED** - rollback removes the claim; no try/catch around execution |
| **INV-169** | Raw idempotency keys and original request bodies are never persisted. | Lookup needs only a digest, and conflict detection needs only a fingerprint. | **ENFORCED** - schema has no such column, asserted against the live database |
| **INV-170** | Raw idempotency keys are never logged and never used as metric labels. | Client-supplied, unbounded, and identifying. | **ENFORCED** - redaction covers the header; catalog audit covers labels |
| **INV-171** | Stored replay results are bounded and contain no headers - no cookies, no CORS, no security headers, no request ID. | Replaying a stored Set-Cookie would re-issue an old session; a replay must get its own request ID. | **ENFORCED** - stored shape has no header field; 64 KiB bound tested |
| **INV-172** | Idempotency does not imply exactly-once external delivery. | An email or billing provider may still receive a retry. Provider adapters need their own keys. | Documented -> review |
| **INV-173** | Rate limiting is defence in depth and never substitutes for authentication, authorization, tenancy, CSRF or idempotency. | A limit answers "too much, too fast" and nothing about who you are. | Documented -> review |
| **INV-174** | Security-sensitive limits use durable shared state, never process memory. | With N instances, a local counter turns "5 per minute" into "5N per minute". | **ENFORCED** - PostgreSQL only; real concurrency test |
| **INV-175** | Concurrent increments are atomic. No read-then-write. | Two callers receiving the same count let one extra request through, and that one is the attacker. | **ENFORCED** - single upsert; probed by replacing it |
| **INV-176** | IP scope comes from proxy-aware observed request metadata. Client-supplied headers never select a bucket. | Otherwise an attacker picks a fresh bucket per request and the limiter is decorative. | **ENFORCED** - spoofing tested both ways |
| **INV-177** | Rate-limit scope identities come from trusted context - resolved session, authorized workspace, validated signing access - never a request body. | A body field lets an attacker choose which bucket to fill. | **ENFORCED** - typed scopes; no body-derived construction exists |
| **INV-178** | Personal-data scope keys are digested before storage. | An IP or an email is personal data the counter table only compares, so it need not hold it reversibly. | **ENFORCED** - tested; probed by storing raw |
| **INV-179** | Every threshold records its source. | A number nobody chose is one nobody can defend when it blocks a customer. | **ENFORCED** - validation rejects an empty source |
| **INV-180** | A limit below 1 is rejected at startup. | A limit of 0 silently disables the operation it protects. | **ENFORCED** - tested |
| **INV-181** | Rate limiting runs BEFORE idempotency and before the business transaction. | Everything after costs something an attacker would otherwise get free. | **ENFORCED** - a rate-limited request never claims an idempotency key |
| **INV-182** | An idempotency replay is still counted. | Exempting replays makes the key a rate-limit bypass. | **ENFORCED** - tested |
| **INV-183** | Rate-limited responses are canonical 429s carrying no policy, count, remaining or scope. | "2 attempts left" is a gift to an attacker; a policy name tells them which dimension to rotate. | **ENFORCED** - leak test |
| **INV-184** | A limiter store failure is reported as a dependency failure, never as a rate limit. | A caller must not be told "slow down" when the truth is "the control is broken". | **ENFORCED** - distinct error, maps to 503 |
| **INV-185** | Pre-auth limits apply whether or not the account exists. No permanent lockout. | An "unknown account is unlimited" path enumerates accounts; a permanent lockout is a denial-of-service weapon. | Documented -> BACKEND-20/22/23 |
| **INV-186** | Rate-limit metrics and logs contain no IP, account, digest, count or unbounded identifier. | Metrics are the surface that should carry no personal data at all. | **ENFORCED** - catalog audit; log capture test |
| **INV-187** | Counters have bounded operational retention and are not a request log or signing evidence. | One row per window, not per request. | **ENFORCED** - schema; bounded cleanup tested |
| **INV-188** | Rate limiting does not claim exactly-once execution or network-layer DDoS protection. | An application counting requests is already past where a volumetric flood does its damage. | Documented -> review |
| **INV-189** | A new public or credential-guarding operation requires an explicit rate-limit policy. | The absence of a policy is the absence of a control. | Documented -> feature commands |

## Rules for future `BACKEND-XX` commands

1. Read this file and the applicable ADRs before changing backend code.
2. Preserve package boundaries.
3. Report — do not silently proceed — if the requested task would violate an
   invariant.
4. Modify an invariant only when explicitly instructed, and record the change in
   an ADR.
5. Add or update tests for any architectural behaviour touched.
6. Avoid opportunistic unrelated refactors.
7. When an invariant's enforcement column says "Documentation", and the command
   creates the package that governs it, **make it executable in that command**.

## Worker and background jobs (BACKEND-16)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-190** | API and worker are separate process roles. The API may not import the queue driver or `@lagda/worker`; the worker may not import a web framework or `@lagda/api`. | The two scale, restart and fail for different reasons. An API process able to call `boss.work` can start a consumer inside a web process, and then neither can be restarted alone. | **ENFORCED** - ESLint, probed in both directions with negative controls |
| **INV-191** | A job enqueued alongside a business write joins that write's transaction. | Otherwise a job survives a rolled-back mutation and the worker acts on state that never existed, or a committed write loses its follow-up. Both are silent. | **ENFORCED** - commit and rollback integration tests; probed by removing the `db:` override |
| **INV-192** | Job payloads carry identifiers, never resource content, personal data or credentials, and are bounded at 16 KiB. | A payload is a durable database row that appears in backups. It also goes stale: a name copied at enqueue is wrong when a delayed job runs. | **ENFORCED** - size refused at enqueue and probed; content rule documented |
| **INV-193** | Every job declares a bounded retry policy, and the bound is applied to the queue as well as to each send. | Nothing retries forever. Recurring jobs are sent by pg-boss's own scheduler and never pass through LAGDA's scheduler, so a per-send bound would not reach them. | **ENFORCED** - queue row asserted; registry test requires a bound on every definition |
| **INV-194** | Job payloads are validated at execution time. Malformed input is terminal, not retried. | A queue holds rows written by a previous deployment, and possibly by an operator by hand. Retrying a malformed payload burns the attempt budget and delays the real signal. | **ENFORCED** - five malformed shapes rejected; probed by skipping validation |
| **INV-195** | A handler failure is logged and rethrown. It is never converted into success. | Catching and returning normally reports success to the queue and silently discards the work - the failure mode this layer exists to prevent. | **ENFORCED** - test through the real registration path; probed by swallowing |
| **INV-196** | Every job definition declares how a duplicate delivery stays safe. | A worker can crash between finishing work and marking a job complete. Duplicate delivery is inherent to a durable queue, not a defect. | **ENFORCED** - required field, registry test; the strategy itself is prose |
| **INV-197** | A job type name is a persistence contract and is never renamed. | Renaming strands every row already enqueued under the old name. They are never consumed and never fail - they simply sit there. | Documented -> review; closed union makes addition cheap |
| **INV-198** | Job logs carry the result shape, never `job.data`. | A payload may carry resource identifiers, and a full dump is how they reach log aggregation. | **ENFORCED** - the wrapper spreads only an object result |
| **INV-199** | The worker runs no migrations. | Migration is an explicit deployment step. A worker that migrated on boot would race every API replica during a rolling deploy. | **ENFORCED** - no migrator in the worker; boot verified |
| **INV-200** | Recurring work is registered through the queue's schedule table, not a process timer, and always in UTC. | `setInterval` disappears on restart and duplicates across instances. A server-local cron shifts silently with the deployment timezone. | **ENFORCED** - idempotent-registration test; `tz: "UTC"` asserted in the schedule row |
| **INV-201** | Time-dependent horizons are read at execution time, not baked in at enqueue. | A job delayed by an outage would otherwise delete using a stale cutoff. | **ENFORCED** - `before` deliberately absent from the payload schema |
| **INV-202** | Tenant scope is an explicit discriminant on every job, never an optional workspace id where `undefined` means global. | That shape makes the most dangerous value the easiest to produce by accident. | **ENFORCED** - closed union; non-optional `workspaceId` on the workspace context |

## Object storage (BACKEND-17)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-203** | Provider SDK types never appear outside `packages/storage`. Application, core and contracts work with the `ObjectStorage` port. | A vendor type in the application layer spreads to every caller and makes the provider unreplaceable. The production provider is not yet chosen. | **ENFORCED** - ESLint names + wildcard, probed in 7 packages with 4 negative controls |
| **INV-204** | The binary abstraction is `AsyncIterable<Uint8Array>`, chosen once. | A Node `Readable` or an SDK stream wrapper in a port signature is a provider type by another name. | **ENFORCED** - the port compiles against no Node or SDK stream type |
| **INV-205** | Object keys are DERIVED from authorized identifiers and never accepted from a client. Accepted artifacts are never silently overwritten. | The whole tenancy argument for storage rests on the key being unreachable. Overwriting destroys the evidence that earlier bytes existed. | **ENFORCED** - branded type with one validating constructor; overwrite refused and probed |
| **INV-206** | Provider ETag is never treated as LAGDA's SHA-256. | ETag is an MD5, or a digest-of-digests for multipart, or provider-specific. Treating it as the artifact digest would make integrity checks pass on wrong bytes. | **ENFORCED** - typed as `providerEntityTag`; test asserts inequality and length difference |
| **INV-207** | Storage references are persisted as internal keys, never as URLs or presigned URLs. Presigned URLs are bearer credentials: never logged, persisted or queued. | A presigned URL expires and grants access to whoever holds it; a permanent URL implies a public bucket. | **ENFORCED** - branded key column; leak test; redactor scrubs signatures. Signed URLs not implemented |
| **INV-208** | Zones are a closed union. The application never names a bucket. | An arbitrary bucket string lets a typo write documents somewhere with no access policy. | **ENFORCED** - `StorageZone` union; bucket mapping only in the adapter |
| **INV-209** | Customer filenames never appear in object keys or provider metadata. | Provider logs, consoles and billing exports are outside LAGDA's control, and a filename can name the subject of a legal document. | **ENFORCED** - keys built from identifiers only; probed by injecting a filename |
| **INV-210** | Provider errors are translated into LAGDA categories by structure, never by message text. | Provider wording differs per implementation and changes without notice; a substring match is a detector that silently stops detecting. | **ENFORCED** - mapped by name, code and status; induced against a real service |
| **INV-211** | Production object-storage traffic uses TLS with validated certificates. | Documents and signing evidence travel this connection. | **ENFORCED** - plaintext endpoint refused; the escape hatch is itself refused in production |
| **INV-212** | Storage credentials are externalized and never logged. The loggable projection is an allowlist. | A denylist leaks the first field added without thinking, and the config object holds a secret key. | **ENFORCED** - allowlist projection; probed by adding the secret |
| **INV-213** | Storage adapters do not query PostgreSQL, write evidence, enqueue jobs or modify PDFs. | Each would couple a replaceable adapter to something that must outlive it - and a storage layer that rewrites a PDF invalidates every signature. | **ENFORCED** - import bans; byte-exactness tested by digest |
| **INV-214** | Object storage performs no authorization. Tenant access is resolved before a storage call, and a workspace prefix is organisation only. | Anyone holding a key can read the object. What prevents cross-tenant access is that the key is never reachable. | **ENFORCED** - cross-tenant test proves B obtains no reference |
| **INV-215** | Object storage and PostgreSQL are not transactionally atomic, and no code claims they are. | Believing otherwise is how an artifact row ends up pointing at bytes that were never written. | Documented -> BACKEND-18/38 own the choreography |
| **INV-216** | Document buckets are private. No public-read ACL, no permanent public URL. | Every document is a contract, an affidavit or an identity record. | **PARTIALLY ENFORCED** - no ACL in code (audited); bucket-level public-access block is a deployment requirement |

## Secure upload (BACKEND-18)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-217** | Every client-supplied file is untrusted until inspection AND malware scanning both pass. The pipeline fails closed at every branch. | A workspace owner can upload malware by accident as easily as an attacker can on purpose. Role, filename and prior uploads change nothing. | **ENFORCED** - ordering test; probed by making failure mean clean (4 tests fail) |
| **INV-218** | Untrusted bytes enter quarantine before accepted-artifact storage, and a quarantine object is never an accepted artifact. | Bytes in the immutable zone before validation is the whole failure this boundary exists to prevent. | **ENFORCED** - order asserted; probed by writing to the artifact zone first (13 tests fail) |
| **INV-219** | Client filename, extension, media type, declared size and any client hash are never authoritative. Type is DETECTED from content and confirmed by a real parser. | All five are attacker-chosen strings. | **ENFORCED** - detection + parse; probed by trusting the client type (3 tests fail) |
| **INV-220** | Content inspection and malware scanning are separate controls; neither substitutes for the other. | A structurally perfect PDF can carry a dropper; a corrupt file is unusable whether or not it is infected. | **ENFORCED** - a valid PDF containing EICAR passes inspection and is caught only by the scanner |
| **INV-221** | Malware scanning is mandatory for acceptance and fails closed. Scanner unavailability is NEVER interpreted as clean. | An outage that silently disabled malware protection would be worse than having none, because everyone would believe it was on. | **ENFORCED** - real ClamAV; unreachable-scanner test; probed in two places |
| **INV-222** | An unscannable file is never accepted. A file exceeding the scanner's own limit is refused, not skipped. | Otherwise the largest files - the interesting ones - would be the unscanned ones. | **ENFORCED** - refused before sending; tested against a real limit |
| **INV-223** | SHA-256 is computed by the backend from the exact received bytes. Client hashes and provider ETags never substitute for it. | The digest is the integrity claim every later verification rests on. | **ENFORCED** - chunk-independence test; digest re-verified at promotion |
| **INV-224** | Workspace and actor identity for an upload come from the authenticated context, never from multipart fields. | A body field is chosen by the client; reading identity from it is a complete tenancy bypass. | **ENFORCED** - the route never reads the body for identity, and `attachFieldsToBody` is false so there is no body to read |
| **INV-225** | Accepted artifacts are byte-identical to what was uploaded. No silent transformation, re-save or sanitization. | Rewriting bytes invalidates the digest, and therefore every signature derived from it. | **ENFORCED** - byte-exact round trip through real storage |
| **INV-226** | Artifact metadata is never committed before the accepted bytes exist, and an accepted upload always names its artifact. | A row claiming an artifact whose bytes do not exist is a document the product believes it has. | **ENFORCED** - ordering test plus a database CHECK constraint |
| **INV-227** | No PostgreSQL transaction spans file transfer, PDF inspection or malware scanning. | Those take seconds. A transaction held across them holds a connection and its locks for the length of an antivirus scan. | **ENFORCED** - one short transaction at acceptance; reviewed and documented |
| **INV-228** | Rejected or failed uploads create no accepted artifact and no signing evidence. | An upload rejection is not an event in a signing transaction. | **ENFORCED** - failure tests assert zero artifacts; no evidence write exists in this path |
| **INV-229** | Document bytes, malware payloads, storage credentials and scanner secrets are never logged, and never placed in queue payloads. | A log aggregator is a wider audience than the workspace that owns the document. | **PARTIALLY ENFORCED** - no logging exists in this path yet; classification documented, queue rule inherited from INV-198 |
| **INV-230** | Quarantine cleanup is driven by database records, is bounded by a horizon, and is idempotent. | A cleanup job that lists a bucket to decide what to delete is a job that can delete something real. | **ENFORCED** - row-driven; run twice in a test; in-flight rows excluded |

## Account identity and registration (BACKEND-19)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-231** | ONE canonical email normalization rule (trim + lowercase) is used by every auth flow, and no adapter normalizes independently. | Two flows disagreeing about identity produces either a duplicate account or an authentication bypass. | **ENFORCED** - single normalizer, repository-boundary assertion, probed by removing lowercasing |
| **INV-232** | Normalized email uniqueness is enforced by PostgreSQL. An application pre-check is behaviour, never the guarantee. | Two concurrent registrations both pass a pre-check; only the constraint decides. | **ENFORCED** - unique constraint; six simultaneous registrations produce one row |
| **INV-233** | Plaintext passwords are ephemeral, never persisted, never logged, and never altered by trimming or normalization. | A stored or logged password is a breach; an altered one means two inputs authenticate one account. | **ENFORCED** - marker tests across DB, logs and response; probed by trimming |
| **INV-234** | Password hashing runs after cheap validation and abuse controls, and outside any database transaction. | Argon2id is memory-hard by design; unlimited it is a DoS primitive, and inside a transaction it holds locks for its full duration. | **ENFORCED** - ordering tests; probed by hashing first |
| **INV-235** | Public registration never overwrites an existing account or replaces an existing password. | It would be an account takeover primitive: anyone could reset any account by re-registering it. | **ENFORCED** - the repository has no update path; verified against real PostgreSQL |
| **INV-236** | Account identity is GLOBAL, not workspace-scoped, and this is deliberate rather than a tenancy bypass. | A user exists before any workspace and may belong to several; requiring a tenant would make login impossible. | Documented + **ENFORCED** by schema - no workspace_id, no RLS, stated in the migration |
| **INV-237** | Email-verification credentials are cryptographically random, and only a DIGEST is stored. | The raw token is a bearer credential; storing it would let a database read verify every pending account. | **ENFORCED** - digest column with a shape CHECK; probed by storing the raw token |
| **INV-238** | Password hashes and verification digests never appear in a public projection or API response. | A field absent from the type cannot be serialized by accident. | **ENFORCED** - `UserRecord` has no hash field; closed response schema, probed |
| **INV-239** | Verification, session and CSRF tokens are domain-separated before digesting. | Otherwise one credential's digest could match another's stored value, and a token minted for one purpose would be accepted for the other. | **ENFORCED** - distinct prefixes; digest allowlist test |
| **INV-240** | The user row and its verification challenge commit atomically. | A user with no challenge can never verify; a challenge with no user references nothing. | **ENFORCED** - one transaction; rollback tested against real PostgreSQL |
| **INV-241** | A new account is unverified. Verification is never implied by successful registration. | Registration proves someone typed an address, not that they receive mail at it. | **ENFORCED** - repository always writes NULL; probed |
| **INV-242** | Password-hashing libraries are confined to the security adapter. | Everything above works with the PasswordHasher port, which is what makes the algorithm replaceable. | **ENFORCED** - one importing file; port used everywhere else |
| **INV-243** | Stored credentials must be Argon2id. | A bug that wrote a plaintext password or a weaker hash is refused by the database rather than discovered at login. | **ENFORCED** - database CHECK, tested by direct insert |
| **INV-244** | Verification URLs are built from configured application URLs, never an incoming Host header. | A Host-derived link sends the user's token to an attacker's domain. | **ENFORCED** - URL builder takes a configured base; tested |
| **INV-245** | Registration input may not set ids, roles, verification state, timestamps or account status. | Mass assignment. | **ENFORCED** - closed request schema plus explicit field mapping; probed |
| **INV-246** | Registration issues no session and no session-bound CSRF credential. | The frontend requires email verification before authenticated use; a session here would authenticate an unproven mailbox. | **ENFORCED** - no cookie set, asserted by test |
| **INV-247** | Registration never claims a verification email was sent unless a durable delivery path exists. | A false claim sends a user to wait for mail that will never arrive. | **ENFORCED** - the response carries `nextAction`, never a sent claim; asserted |

## Login and sessions (BACKEND-20)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-248** | Login resolves accounts through the SAME canonical email normalizer registration used. | A login that normalized differently would fail to find accounts registration created. | **ENFORCED** - one normalizer; every casing tested against a real registered account |
| **INV-249** | An unknown account and a wrong password produce identical public results - status, code, body and absence of metadata. | Any difference is an account-existence oracle. | **ENFORCED** - responses compared byte for byte; probed by splitting them |
| **INV-250** | An unknown account still runs a real Argon2id verification against a fixed dummy hash, unless rejected earlier by validation or rate limiting. | Returning early makes response time an existence oracle. | **ENFORCED** - probed by removing the dummy path |
| **INV-251** | Account eligibility is checked AFTER password verification, never before. | Checking first lets anyone learn verification state without knowing the password. | **ENFORCED** - probed by reordering |
| **INV-252** | Raw session and CSRF tokens exist only long enough to write cookies. They are never persisted, never logged, never serialized into JSON. | A credential in a log or a response body is a credential in someone else's hands. | **ENFORCED** - digest-only storage; marker tests over logs and bodies; closed response schema, probed |
| **INV-253** | Successful login always issues a FRESH session credential. An incoming cookie is never promoted to authenticated state. | Session fixation: an attacker plants a cookie, waits for the victim to log in, and inherits the session. | **ENFORCED** - planted-cookie test |
| **INV-254** | Login input may not set ids, roles, verification state or session identity. | Mass assignment. | **ENFORCED** - closed schema, probed |
| **INV-255** | Login-CSRF is defended by SameSite, exact-origin CORS and a server-side Origin check - not by declaring the route public. | A forged login authenticates a victim's browser as the ATTACKER, who then observes what they do. | **ENFORCED** - cross-site origin rejected before credential work; probed |
| **INV-256** | Browser authentication never returns a JWT, access token, refresh token or script-readable session credential. | A token a script can read is a token XSS can steal, and the session cookie already solves the problem. | **ENFORCED** - closed response schema; no such dependency exists |
| **INV-257** | Logout revokes server-side session state. Clearing a cookie alone is never sufficient, and a failed revocation is never reported as success. | A copied credential keeps authenticating until the server refuses it. | **ENFORCED** - revocation tested against real PostgreSQL; probed twice |
| **INV-258** | Logout is a POST and carries CSRF protection. | A GET logout can be fired by an image tag on any page on the internet. | **ENFORCED** - GET is not routed; CSRF is the standard authenticated-mutation hook |
| **INV-259** | Login is not wrapped in the generic idempotency replay framework. | Replaying a stored response would replay a session credential. | Documented - no idempotency wrapper on this route |
| **INV-260** | Session identity authenticates a user and implies no workspace authorization. | Permissions in a cookie cannot be revoked. | **ENFORCED** - the login response carries no workspace data |

## Email verification (BACKEND-21)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-261** | Verification credentials are opaque, cryptographically random, single-purpose and expiring, and their strength does not depend on rate limiting. | If entropy were weak, limits would only slow an attacker down. | **ENFORCED** - 60 bits, rejection-sampled; probed by shortening the code |
| **INV-262** | Raw verification codes and full verification URLs are never persisted or logged. | A stored code means a database read verifies every pending account. | **ENFORCED** - digest-only schema; marker tests over trace-level logs |
| **INV-263** | Challenge lookup uses a domain-separated digest, never a scan and never the raw value. | Domain separation stops a code minted for one purpose being accepted for another. | **ENFORCED** - unique indexed digest; probed by removing the prefix |
| **INV-264** | Successful verification transactionally consumes the challenge and sets the verification timestamp, and never rewrites an existing one. | The first verification time is account-security history. | **ENFORCED** - conditional updates, tested directly at the repository; probed twice |
| **INV-265** | A consumed, expired or superseded credential can never perform first-time verification. | Otherwise single-use and rotation mean nothing. | **ENFORCED** - probed for each state independently |
| **INV-266** | Resend rotates and supersedes prior active credentials rather than accumulating valid ones, and at most one challenge per user is active. | An unbounded set of live codes multiplies the chance one leaks. | **ENFORCED** - partial unique index; concurrent-resend test; probed |
| **INV-267** | A resend that cannot durably schedule delivery may not invalidate the currently usable credential. | Otherwise a queue outage strands an account with no way in. | **ENFORCED** - scheduling inside the rotation transaction; failure test |
| **INV-268** | Public resend never reveals whether an account exists, is verified, or is unverified. | Unlike registration the caller has asserted nothing about the address. | **ENFORCED** - one response shape; the route discards the reason; probed |
| **INV-269** | Provider delivery never verifies an email. Only redeeming a credential sets the verification timestamp. | A delivery receipt proves a message reached a server, not that the owner read it. | Documented - nothing in this path writes verification state from delivery |
| **INV-270** | A verification credential authorizes only email verification - never a session, a password reset, or workspace access. | Credential-purpose confusion is how one flow becomes an entry to another. | **ENFORCED** - verification issues no session, tested; digest domain separation |
| **INV-271** | Verification links are built from configured application URLs, never an incoming Host header. | A Host-derived link sends the code to an attacker's domain. | **ENFORCED** - builder takes a configured base; tested |
| **INV-272** | Verification state is server-authoritative and is never an immutable client-side claim. | A cached claim goes stale exactly when it matters. | **ENFORCED** - read from the account on every login |
| **INV-273** | Link-preview and security scanners cannot consume a verification credential: consumption is a POST, and the product collects a typed code. | Scanners fetch every link in a message before a human sees it. | **ENFORCED** - GET is not routed; tested |

## Password recovery (BACKEND-22)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-274** | Password-recovery requests use the same canonical email normalization as registration and login. | An address that resolves to one account at login and another at recovery is a way to request a reset for someone else. | **ENFORCED** — one normalizer, tested with mixed case and whitespace |
| **INV-275** | Public password-recovery requests never reveal whether an account exists. | Unlike registration, the caller has asserted nothing — and this oracle also emails the account it confirms. | **ENFORCED** — identical status, body and header set; the route discards the reason; probed |
| **INV-276** | Password-reset credentials are distinct from verification, session, CSRF, signing, OTP and API-key credentials. | A credential valid for two purposes is a way to convert proof of one thing into authority to do another. | **ENFORCED** — separate table, separate brand, separate digest domain; probed |
| **INV-277** | Reset credentials are opaque, cryptographically random, expiring and single-use, and their strength does not depend on rate limiting. | If entropy were weak, limits would only slow an attacker down. | **ENFORCED** — 256 bits; probed by shortening to 4 bytes |
| **INV-278** | Reset challenge lookup uses a domain-separated digest, never a scan and never the raw value. | Without the prefix, a verification code and a reset token that were the same string would digest identically. | **ENFORCED** — unique indexed digest; probed, and the test was rewritten after the probe caught nothing |
| **INV-279** | Raw reset credentials and full reset URLs are never persisted or logged. | A stored reset token means a database read is an account-takeover primitive for every pending reset. | **ENFORCED** — digest-only schema; the row is scanned for the raw value in a test |
| **INV-280** | At most one reset credential is active per account. | Every additional live link is another chance for the most dangerous credential in the system to be read by the wrong person. | **ENFORCED** — partial unique index; five concurrent requests leave one |
| **INV-281** | A replacement challenge and its durable delivery intent commit atomically, so a failed resend never invalidates the prior usable challenge. | Otherwise a queue outage leaves the old link dead and the new one unsent — an unrecoverable account. | **ENFORCED** — scheduling inside the transaction; failure test; probed |
| **INV-282** | Reset submission is rate-limited and cheaply validated before Argon2. | Argon2 is ~50ms of dedicated CPU by design; hashing before checking is a one-request-per-core denial of service. | **PARTIALLY ENFORCED** — ordering enforced and probed; the limiter is defined but not bound |
| **INV-283** | New reset passwords use the same server-side policy and Argon2id configuration as registration. | A reset path with its own rule is how an account ends up with a password its own signup form would have refused. | **ENFORCED** — the policy is imported, not restated; probed |
| **INV-284** | Reset links are built from configured application URLs, never an incoming Host header. | A Host-derived link makes LAGDA itself email a victim a link pointing at the attacker's domain. | **ENFORCED** — the builder takes a configured base; probed |
| **INV-285** | Successful reset transactionally replaces the password hash, consumes the challenge and revokes the required sessions. | A password change that leaves a stolen session alive achieves nothing — and the stolen session is usually why the user is resetting. | **ENFORCED** — one transaction; probed for each part |
| **INV-286** | A consumed, superseded or expired reset credential can never change a password, and two concurrent uses of one token produce exactly one change. | Otherwise single-use means nothing and the surviving password may be one the user never chose. | **ENFORCED** — conditional UPDATE; 4 concurrent resets verify exactly one candidate; probed |
| **INV-287** | Password reset never verifies an account email. | Possession of a reset link proves mailbox access, but verification is a separate account fact with separate consequences. | **ENFORCED** — `replacePasswordHash` sets one column; probed by making it set both |
| **INV-288** | Browser GET navigation to a reset page never mutates the password or consumes the credential. | Mail security scanners fetch every link in a message before a human sees it. | **ENFORCED** — GET is not routed on either endpoint; tested |

## Multi-factor authentication (BACKEND-23)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-289** | MFA credentials form a security domain separate from verification, reset, session, CSRF, signing and API credentials. | A credential valid for two purposes converts proof of one thing into authority to do another. | **ENFORCED** — own tables, own brands, own digest domains; a reset token presented as a code is refused |
| **INV-290** | The implemented factor is what the product actually has, and unsupported factors are neither implemented nor documented as available. | Claiming SMS or email MFA that does not exist misdescribes the product's security posture to whoever reads the docs. | **ENFORCED** — closed factor type with a database CHECK; inventory documents each classification |
| **INV-291** | TOTP secrets are stored encrypted, never in plaintext, and the key never enters the database. | This is the one credential the server must recover rather than compare; a dump of plaintext secrets is every user's second factor. | **ENFORCED** — AES-256-GCM; the row is scanned for plaintext in a test |
| **INV-292** | Every credential digest is domain-separated. | Seven credential types now digest to 64 hex characters; without prefixes any could resolve another's row. | **ENFORCED** — distinct prefixes and distinct brands |
| **INV-293** | OTP verification attempts are counted durably and atomically, per ceremony. | A 6-digit code is 10^6 possibilities; a read-modify-write counter gives an attacker many guesses for the price of one. | **ENFORCED** — increment computed by PostgreSQL; 8 concurrent attempts cost exactly 5; probed |
| **INV-294** | An exhausted ceremony can never authenticate, even with a correct code. | Otherwise the limit is a pause rather than a bound. | **ENFORCED** — ceiling in the UPDATE's WHERE clause; probed |
| **INV-295** | A code valid for its time window may be used once. | A code stays valid ~90 seconds; without a watermark one observed over a shoulder works again. | **ENFORCED** — conditional watermark advance; probed |
| **INV-296** | A malformed second-factor submission still costs an attempt. | Otherwise unlimited guesses are available by varying the length. | **ENFORCED** — tested |
| **INV-297** | Login MFA issues no code and sends nothing before the first factor succeeds. | Trivially satisfied by TOTP, which issues nothing at all — and the requirement stands for any future delivered factor. | **ENFORCED** — no ceremony is created for a failed password; tested |
| **INV-298** | An account requiring MFA receives no normal session until every factor succeeds. | A password alone must not produce access when the account has said it should not. | **ENFORCED** — the `mfa-required` result type has nowhere to put credentials; probed |
| **INV-299** | Pending MFA authentication uses a distinct, short-lived, restricted credential and is never a normal session. | Overloading one cookie makes "is this browser authenticated?" a question every middleware must answer correctly forever. | **ENFORCED** — separate cookie name, `Path=/auth`, 10-minute absolute life |
| **INV-300** | The final session credential is freshly generated after MFA and never reuses the pre-auth credential. | Promoting it is session fixation with extra steps. | **ENFORCED** — probed |
| **INV-301** | A pre-authentication credential cannot access normal application routes. | A half-finished ceremony is not authorization. | **PARTIALLY ENFORCED** — the cookie's `Path=/auth` means browsers do not transmit it to application routes, and the settings routes refuse it; but no auth route is composed into `createApp`, so this is not demonstrated in a running application (OD-069) |
| **INV-302** | MFA requirement and factor state are server-authoritative and cannot be set through any client-supplied field. | A client that could assert `mfaRequired: false` would have no second factor. | **ENFORCED** — read from the factor table; closed request schemas |
| **INV-303** | Password reset invalidates pending authentications based on the prior password proof, and does not disable MFA. | A ceremony started with the old password must not complete after a reset; and a weaker credential must not strip a stronger one. | **ENFORCED** — both probed |
| **INV-304** | Removing a second factor requires re-proving the password, not merely a session. | Disabling MFA is the first thing an attacker with a stolen session tries. | **ENFORCED** — probed |
| **INV-305** | Codes, secrets, provisioning URIs, recovery codes, pre-auth credentials, encryption keys and attempt counts never appear in logs, metrics or response bodies. | Any one of them in a log is the factor itself. | **ENFORCED** — response-body tests; no log statement on these paths |

## Account and profile (BACKEND-24)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-306** | Generic account mutations may alter only explicitly approved profile and preference fields. | `users` holds the display name and the password hash in one table; only the set of columns a repository names keeps them apart. | **ENFORCED** — three repository methods, explicit column lists, no generic patch; probed |
| **INV-307** | Authentication-security state can never be mass-assigned through account APIs. | A profile form that could set `emailVerified` or `role` is a privilege-escalation primitive. | **ENFORCED** — closed request schemas; fourteen distinct payloads each rejected; probed |
| **INV-308** | The current user for `/me` and every account mutation comes exclusively from the validated session. | An id taken from a request body is an authorization check waiting to be written wrongly. | **ENFORCED** — no `:userId` path segment and no `userId` field exists to supply one |
| **INV-309** | Pre-authentication credentials cannot reach account APIs. | A half-finished MFA ceremony has proved a password and nothing more. | **PARTIALLY ENFORCED** — the route refuses it and the pre-auth cookie is scoped to `/auth`, but no route is composed, so this runs through a test double (OD-069) |
| **INV-310** | Account identity is global and separate from workspace membership and authorization. | Collapsing user and tenant is the mistake that makes multi-workspace impossible later. | **ENFORCED** — no workspace field on any account type or schema |
| **INV-311** | Account projections never expose password hashes, normalized identity, token digests, session or CSRF credentials, OTP verifiers, or MFA secrets. | One careless field is a credential in a response body. | **ENFORCED** — `CurrentUser` has no such fields; an attempt to add one does not compile |
| **INV-312** | Email identity changes use a dedicated security workflow and are never a generic profile edit. | Replacing a login identifier on a `PATCH` is account takeover by typo. | **ENFORCED by absence** — email is not on any mutation schema, and the product has no self-service change (documented for the future) |
| **INV-313** | Password change requires re-proving the current credential, not merely a session. | Changing a password from a stolen session locks the owner out using nothing but the theft. | **ENFORCED** — probed |
| **INV-314** | Password change uses the same policy and Argon2id configuration as registration and reset. | A settings path with its own rule is how an account gets a password its own signup form would refuse. | **ENFORCED** — the policy is imported; probed |
| **INV-315** | Sensitive account changes revoke the sessions the change is meant to protect against. | A password change that leaves other sessions alive achieves nothing when the reason is a suspected intruder. | **ENFORCED** — every other session revoked, current preserved; probed both ways |
| **INV-316** | Session management exposes safe projections only and scopes every operation to the authenticated user. | A revoke keyed on a session id alone lets anyone who learns one sign another account out. | **ENFORCED** — user-scoped WHERE clauses; probed |
| **INV-317** | Personal preferences use closed vocabularies and IANA time zones, never raw offsets. | An offset is wrong twice a year and cannot be corrected without knowing its origin zone — and `Intl` accepts one. | **ENFORCED** — shape check plus runtime check plus database CHECKs; probed |
| **INV-318** | Profile text accepts Unicode names and rejects only control characters. | An ASCII allowlist rejects a large share of this product's own users, and punctuation rejection is not an XSS defence. | **ENFORCED** — tested against real names; probed |
| **INV-319** | Mutable account and profile state can never rewrite historical signing evidence. | Someone renaming themselves must not change who signed a document last year. | **ENFORCED ARCHITECTURALLY** — no account code imports or reaches evidence tables |

## Workspace lifecycle (BACKEND-25)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-320** | A workspace is the primary tenant boundary for workspace-owned LAGDA resources, and `WorkspaceId` is opaque, server-generated and immutable. | A tenant key derived from a name, an email or a sequence is guessable, and one that changes on rename breaks every historical reference to it. | **ENFORCED** — generated from a port before the transaction opens; no request schema has a field for one; a rename leaves the id and `created_at` untouched (integration) |
| **INV-321** | Global `UserId` and `WorkspaceId` are separate identities, connected only through a workspace membership. | Collapsing them makes multi-workspace impossible later, and makes "which user is this" and "may they enter here" one question with one answer. | **ENFORCED** — one account row across several memberships, asserted in integration; no workspace field on any account type |
| **INV-322** | A successful workspace creation atomically persists the workspace and the creator's OWNER membership; a rollback leaves neither. | A workspace with no membership is an inaccessible orphan — no endpoint can reach it and no endpoint can delete it. | **ENFORCED** — one unit of work; the rollback case is forced with PostgreSQL's own foreign key and both rows are asserted absent |
| **INV-323** | Client input can never choose the workspace creator or owner. | A client-supplied owner is a workspace created in someone else's name without their knowledge. | **ENFORCED** — the creator comes from `AuthenticatedActor`; `ownerUserId` is not a field on the use-case input or the route schema, and ten privileged payloads are each rejected |
| **INV-324** | A workspace membership represents ACTUAL access; a pending invitation is never a membership. | A status column on the authorization table means every authorization query needs a filter that one caller eventually forgets. | **ENFORCED BY ABSENCE** — `workspace_memberships` has no status column; invitations get their own table in BACKEND-26 |
| **INV-325** | A user has at most one membership per workspace. | Two rows means two roles, and no rule for which one authorizes. | **ENFORCED** — `UNIQUE(workspace_id, user_id)`; a duplicate insert is refused by PostgreSQL |
| **INV-326** | Membership references both a real workspace and a real account, and neither reference cascades on delete. | A membership naming an account nobody can authenticate as authorizes nobody; a cascade would answer an unresolved retention question destructively. | **ENFORCED** — two foreign keys, both `ON DELETE RESTRICT`; both refusals tested |
| **INV-327** | A valid login session authenticates a user and never by itself authorizes access to any workspace. | A credential carrying authorization keeps granting it after the authorization is revoked. | **ENFORCED** — `AuthenticatedActor` has no workspace field and `user_sessions` has no workspace column; membership is read per request |
| **INV-328** | Every workspace-scoped operation validates current, authoritative membership. | Existence is not authorization, and a check performed once is a check that goes stale. | **ENFORCED** — `resolveWorkspaceAccess` on every route; a membership deleted out of band is refused on the very next call |
| **INV-329** | Active/current workspace selection is navigation context, never an authorization claim, and changing it requires no new session. | This is the architectural benefit the whole model exists to buy. | **ENFORCED ARCHITECTURALLY** — the path segment is the only source; no workspace cookie, no session field, no server-side preference |
| **INV-330** | Membership and role changes take effect without regenerating a login session. | Otherwise removing someone from one workspace means signing them out of every other. | **ENFORCED** — integration test removes a membership and the next request is refused, with the same session |
| **INV-331** | Workspace IDs, slugs, headers, names and URLs grant nothing without membership. | Knowing an identifier is not access. | **ENFORCED** — tenant context only restricts; the lookup names `user_id` explicitly; no slug and no workspace header exist |
| **INV-332** | Cross-tenant lookups never fall back to a global query, and unauthorized access is indistinguishable from non-existence. | A 403 confirms the resource exists, which makes the endpoint an enumeration oracle. | **ENFORCED** — 404 for both; real-foreign and fictional responses compared field by field |
| **INV-333** | A user-scoped read path exists for a user's own memberships, and it is incapable of writing. | Answering "which workspaces do I belong to" must not require `BYPASSRLS` or an application-side filter over every tenant. | **ENFORCED** — `runForUser` sets user context only; the policies are `FOR SELECT`; an UPDATE affects zero rows and an INSERT raises |
| **INV-334** | RLS reinforces explicit repository scoping and never replaces it, and the runtime role holds neither superuser nor BYPASSRLS. | A query relying on an invisible policy is one nobody can review by reading it. | **ENFORCED** — every repository names its predicate; the role's privileges are asserted before any other tenancy assertion runs |
| **INV-335** | Tenant and user context are transaction-local and never leak to the next pooled connection. | A session-level SET rides a pooled connection into the next request. | **ENFORCED** — `set_config(..., true)` only; a fresh transaction after a user-scoped read sees nothing |
| **INV-336** | Workspace metadata changes can never rewrite historical signing evidence. | Renaming a workspace must not change who signed a document last year, or under what tenant. | **ENFORCED ARCHITECTURALLY** — no workspace module imports or reaches an evidence table; the stable `WorkspaceId` is the historical link, and a rename leaves it and `created_at` untouched |
| **INV-337** | Workspace roles and memberships can never be mass-assigned through account or profile APIs, and no workspace mutation alters authentication state. | A profile form that could set a role is a privilege-escalation primitive. | **ENFORCED** — closed request schemas on both sides; no workspace module imports an account repository |
| **INV-338** | Hard workspace deletion is not exposed before retention and erasure semantics permit it, and archive — if added — is never described as deletion. | A workspace will own documents, evidence and certificates; "delete" is a different question for each. | **ENFORCED** — no DELETE route (audited, 8 absent paths); both foreign keys refuse a workspace or account that still has a membership |
| **INV-339** | Workspace names are business-sensitive: never in a routine log, never a metric label, never globally unique. | A name can disclose a client, a matter, a counterparty or the existence of a transaction — and a uniqueness error discloses which names are taken. | **ENFORCED** — the full captured log output is searched for a distinctive name; metric labels are asserted by exact set; two users create the same name in integration |

## Workspace invitations (BACKEND-26)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-340** | A workspace invitation is a tenant-scoped authorization OFFER and is never a membership before acceptance. | A pending invitation in the authorization table makes every authorization query depend on a status filter that one caller eventually forgets. | **ENFORCED** — separate table; `workspace_memberships` has no status column; creation writes no membership, asserted |
| **INV-341** | Invitation target identity uses the same canonical email normalization as account identity. | An address that resolves to one identity at login and another at invitation is a way to have someone else's invitation delivered to a mailbox you control. | **ENFORCED** — one `normalizeEmail`; a database CHECK requires the stored key to be lower case; mixed-case duplicate refused in tests |
| **INV-342** | Invitation creation authority comes from CURRENT workspace membership and can never be supplied by the client. | A stale session, or a body field naming an inviter, is authorization the server did not check. | **ENFORCED** — `requireWorkspaceAccess` then `canManageInvitations`, both before the address is parsed; no inviter field on the input type or the route schema |
| **INV-343** | The membership role an invitation grants is a server-validated closed value that the invitee cannot alter during acceptance. | Otherwise the recipient chooses their own authority. | **ENFORCED** — role read from the persisted invitation; no `role` on the accept schema; a body carrying one is 422 |
| **INV-344** | An invitation can never grant OWNER. | A workspace has exactly one owner, and an emailed link is not an ownership transfer — it would break the invariant days later in a transaction the inviter is absent for. | **ENFORCED** — `owner` absent from `INVITABLE_WORKSPACE_ROLES` so the schema union cannot express it, plus an independent database CHECK |
| **INV-345** | Raw invitation credentials and full invitation URLs are never persisted or logged. | A stored invitation token means a database read is a tenant-access primitive for every pending invitation. | **ENFORCED** — digest-only schema; the stored row is scanned for the raw value in a test; no route returns either |
| **INV-346** | Invitation credentials are opaque, cryptographically random, expiring, purpose-separated and single-use. | Eight credential types now digest to 64 hex characters; without domain separation any could resolve another's row. | **ENFORCED** — 256 bits; `lagda.workspace-invitation:` prefix; 7-day expiry; conditional UPDATE for single use |
| **INV-347** | An invitation credential authorizes only narrow preview and an acceptance attempt, and grants no workspace access before a membership exists. | The holder is not a member and must not become one by reading. | **ENFORCED** — the credential RLS policy is `FOR SELECT` on one row; preview returns four fields; no tenant repository is reachable before `enterWorkspace` |
| **INV-348** | Acceptance requires an authenticated account whose CURRENT canonical email matches the invitation. | This is what makes a forwarded or stolen link worthless. | **ENFORCED** — read from the account at acceptance time, never from the session or the request; probed with a wrong account and with an account that changed its address |
| **INV-349** | Accepting an invitation never verifies the account email, creates an account, issues a session, or bypasses login or MFA. | An invitation is an authorization offer, not an authentication ceremony. | **ENFORCED** — `email_verified_at` asserted unchanged; accept lives inside the authenticated scope so a pre-auth credential is refused before any lookup |
| **INV-350** | Membership creation and invitation consumption are transactionally coordinated. | Either half alone leaves an invitation consumed with no access, or access with a live credential still outstanding. | **ENFORCED** — one transaction; a forced membership failure leaves the invitation live, asserted |
| **INV-351** | Concurrent use of one invitation cannot create duplicate memberships, and PostgreSQL uniqueness remains the final defence. | Two acceptances in flight both observe a pending invitation. | **ENFORCED** — conditional UPDATE serializes them; `UNIQUE(workspace_id, user_id)` behind it; two concurrent acceptances yield one membership |
| **INV-352** | At most one live invitation exists per workspace and invitee address. | A stack of simultaneously valid links is a stack of ways in, and revoking one leaves the rest. | **ENFORCED** — partial unique index; create refuses a live duplicate and supersedes an expired one explicitly |
| **INV-353** | Explicit resend rotates and supersedes the credential; provider delivery retries do not. | Rotating on an SMTP retry would invalidate the link already in the recipient's mailbox. | **ENFORCED** — rotation happens only in `resendWorkspaceInvitation`; the delivery seam receives a token and issues none |
| **INV-354** | A failed resend or delivery-intent transaction never invalidates the prior still-valid credential. | The worst available outcome is the old link dead and the replacement unsent. | **ENFORCED** — rotation and scheduling share a transaction; probed by failing the scheduler and re-previewing the old token |
| **INV-355** | Invitation create and resend are rate-limited against outbound-email abuse, per user AND per workspace. | These are the only endpoints where the server mails an address the caller chose. | **PARTIALLY ENFORCED** — four fail-closed policies defined, validated at startup and bound in the handlers; no dedicated 429 test yet |
| **INV-356** | Token-based invitation lookup is a narrow security path and is never implemented through a broad RLS bypass. | The alternative defeats the tenancy mechanism entirely. | **ENFORCED** — `FOR SELECT` policy on equality against a UNIQUE column; no `BYPASSRLS`, no `SECURITY DEFINER`; a predicate-free SELECT returns one row and an UPDATE affects zero |
| **INV-357** | Invitation management operations remain workspace-scoped and tenant-authorized. | An invitation names a person and a tenant; leaking one leaks both. | **ENFORCED** — scoped repository with no workspace parameter, plus `tenant_isolation`; cross-tenant read tested |
| **INV-358** | Accepting an invitation requires no login-session regeneration. | Workspace authorization is server-side membership state, so there is nothing in the credential to refresh. | **ENFORCED** — end-to-end test: same cookie, workspace unreachable before and reachable after |
| **INV-359** | Invitation security events never carry a credential, a full invitation URL, or the invitee's email address. | An address is personal data and a token is a tenant credential; routine logs are where neither belongs. | **ENFORCED BY CONSTRUCTION** — event builders take IDs and outcomes only; metric labels are a closed three-value set |
| **INV-360** | Invitation history is not erasable by the application, and revocation is a state transition rather than a deletion. | Who was offered access to a tenant, by whom, and whether they took it is security history. | **ENFORCED** — no `DELETE` grant on `workspace_invitations` for the runtime role; asserted |

## Roles and permissions (BACKEND-27)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-361** | Workspace roles and capabilities are centrally defined, and route handlers never encode role-based authorization. | A rule spelled in each route is a rule with no single answer, and BACKEND-25/26 each guessed it wrong. | **ENFORCED** — an architecture guard greps every package for a role comparison and permits four files, each with a stated reason; a second guard covers route files specifically |
| **INV-362** | Tenancy and authorization are separate controls: `workspace_id`/RLS bounds the tenant, capability policy bounds the action. | RLS cannot express "a member may not remove someone", and a capability cannot express "this row belongs to another tenant". | **ENFORCED** — the complete role matrix is deliberately absent from RLS; both layers tested independently |
| **INV-363** | Session credentials authenticate the global user and carry no authoritative workspace role or capability. | A credential carrying authorization keeps granting it after the authorization changes. | **ENFORCED** — an architecture test asserts `AuthenticatedActor` contains no role, capability, permission or workspace field |
| **INV-364** | Workspace role is resolved from authoritative server state for every authorization decision. | A role read at login is a role that may be wrong by the next request. | **ENFORCED** — read per operation; role change and demotion both take effect on the next call, tested |
| **INV-365** | Capabilities are code-defined and default-deny: unknown role, unknown capability and unmapped combination are all refused. | A permissive default in an authorization function is a bug that grants access. | **ENFORCED** — 70 exhaustive assertions plus explicit unknown-role and unknown-capability cases |
| **INV-366** | Clients may never supply capabilities, permissions, ownership or administrative state as authorization input. | Any of them would be the caller asserting their own authority. | **ENFORCED** — closed request schemas; no capability or permission field exists on any schema |
| **INV-367** | Role-grant authority is evaluated through one centralized grant policy, separate from the capability that permits the operation. | Holding "may change roles" is not "may assign any role", and conflating them lets an administrator mint an owner. | **ENFORCED** — `canGrantRole` / `canGrantInvitationRole`; 49 exhaustive assertions |
| **INV-368** | No role may grant OWNER, through invitation or role change. | A workspace has exactly one owner and `assertExactlyOneOwner` depends on it. | **ENFORCED** — four layers: both grant functions, the invitable list, and a database CHECK |
| **INV-369** | A member can never self-promote, and generic role mutation cannot target the actor. | Self-demotion through a role patch would also bypass the leave and transfer rules. | **ENFORCED** — the actor is compared to the target before the grant rule runs; tested from every role |
| **INV-370** | Operations that could reduce the owner count enforce the last-owner invariant inside the transaction that performs them. | A count read before the transaction is a check against state that may have changed by the time the write lands. | **ENFORCED** — `countOwners()` read in the mutation transaction; conditional writes; concurrency tested |
| **INV-371** | A live workspace can never reach zero owners. | It would be unadministrable and unrecoverable. | **ENFORCED** — demotion and removal of the last owner both refused; two concurrency tests, one running ten rounds |
| **INV-372** | Sensitive member mutations resolve the ACTOR's current authority inside their own transaction. | An administrator demoted mid-flight must not commit under authority they no longer hold. | **ENFORCED** — proved against PostgreSQL by demoting the actor and asserting the next write fails |
| **INV-373** | Member removal affects only workspace membership: never the account, never a login session, never another workspace. | Revoking sessions would sign someone out of every other workspace to remove them from one. | **ENFORCED** — all three asserted in integration |
| **INV-374** | Role changes take effect without login-session regeneration. | Permissions are not in the credential, so there is nothing to refresh. | **ENFORCED** — promotion and demotion both change behaviour on the next call with the same session |
| **INV-375** | Background and system actors are never represented as human workspace memberships. | A worker holding a fake `owner` row would be a real membership with real authority. | **ENFORCED ARCHITECTURALLY** — an architecture test asserts `SystemActor` carries no role |
| **INV-376** | RLS may reinforce tenant isolation but is never the implementation of the role matrix. | Two implementations in two languages, deployed differently, drift. | **ENFORCED** — no capability appears in any policy; the matrix exists once, in core |
| **INV-377** | Future workspace-scoped commands declare and check capabilities rather than comparing role names. | Otherwise the answer to "who may do what" becomes a search. | **ENFORCED** — the architecture guard fails on a fifth role-comparing file |
| **INV-378** | No per-member capability column, permission blob or authorization override is persisted. | A second authority that drifts from the policy, and an override nobody reviewed. | **ENFORCED** — an architecture test scans the schema for `can_*`, `permissions` and `capabilities` |
| **INV-379** | Mutable role and membership state can never rewrite historical signing evidence. | Someone demoted today must not change who signed a document last year. | **ENFORCED ARCHITECTURALLY** — no authorization or member module imports an evidence repository |
| **INV-380** | Platform and system-administration authority is distinct from workspace roles, and no support or super-admin role exists. | Tenant authority and platform authority are different security realms. | **ENFORCED BY ABSENCE** — `WORKSPACE_ROLES` contains no such value; BACKEND-59 owns platform administration |

## Contacts and the address book (BACKEND-28)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-381** | Every contact belongs to exactly one workspace, and tenant isolation is enforced at the database. | An address book is a customer's counterparty list; leaking one leaks who they do business with. | **ENFORCED** — `workspace_id` NOT NULL, `tenant_isolation` with `FORCE ROW LEVEL SECURITY`; cross-tenant read, update and archive all probed as the runtime role |
| **INV-382** | A contact is address-book data and is never an identity, an account, a membership or an invitation. | The system would otherwise be asserting a person's identity on the basis of someone having typed it. | **ENFORCED** — no `user_id`/`membership_id`/`invitation_id`/`verified_at` column; the contact module reaches no identity repository; asserted behaviourally with a contact whose email is a real member's |
| **INV-383** | Creating, editing or archiving a contact never creates or modifies a user, membership, invitation or credential. | One reasonable-sounding feature request is all it takes for a contact form to provision accounts. | **ENFORCED** — architecture guard over the use-case module; behavioural test asserts the membership, invitation and digest stores are untouched |
| **INV-384** | The contact email comparison key is a distinct type from the account identity key, and no code path converts one to the other. | The two folds are byte-identical, so only a type can separate them. | **ENFORCED AT COMPILE TIME** — `ContactEmailKey` and `NormalizedEmail` are mutually unassignable brands; `findUserByNormalizedEmail(contact.emailKey)` does not compile; asserted with `expectTypeOf` |
| **INV-385** | The contact comparison key never leaves the backend. | A client that received it would eventually compare it to a user's address. | **ENFORCED** — absent from `ContactSummary` and from the response schema; the exact response key set is pinned by test |
| **INV-386** | Contacts are removed by reversible archiving, and the runtime database role cannot delete one. | A repository omitting a method is a convention; a missing grant is enforcement. | **ENFORCED** — no `DELETE` grant on `contacts`; a raw `DELETE` as `lagda_app` is refused, asserted; `information_schema` shows exactly SELECT, INSERT, UPDATE |
| **INV-387** | Contact state is derived from `archived_at` and never stored as a status column. | Two representations of one fact drift, and the denormalised one is the one that drifts. | **ENFORCED** — no `status` column; derivation is a null check; epoch-0 case tested |
| **INV-388** | Duplicate contacts are detected and reported, never prevented, and no unique constraint exists on the contact address. | A shared inbox is legitimately several business contacts, and the product's own screens exist to review duplicates. | **ENFORCED** — architecture test asserts no unique index; integration test inserts two active contacts sharing an address |
| **INV-389** | Contact operations are governed by capabilities in the central policy, and no contact file compares a role. | The correct role set here is not the administrative one — `sender` and `template_administrator` hold every contact capability and no membership capability. | **ENFORCED** — four capabilities in `ROLE_CAPABILITIES`; the BACKEND-27 role-comparison guard covers the new files unchanged; 7 × 14 exhaustive matrix |
| **INV-390** | Editing or archiving a contact can never rewrite signing-recipient identity or eSignature evidence. | A document signed in March must not silently name someone at an address created in June. | **ENFORCED BY ABSENCE TODAY** — nothing references `contacts`; the snapshot requirement and the compound-key target are specified for BACKEND-30 in CONTACT_RECIPIENT_BOUNDARY.md |
| **INV-391** | Contact personal data never appears in logs, metric labels or error messages. | It is personal data about a third party who is not a LAGDA user and consented to nothing. | **ENFORCED** — two architecture guards over the route source; a behavioural test serializes a real log line and asserts every fixture field is absent; metric labels are a closed three-value set |
| **INV-392** | Contact listing is bounded, and its sort keys are a closed whitelist with supporting indexes. | An arbitrary sort column is an injection surface, and an un-indexed one only fails under real volume. | **ENFORCED** — `perPage` bounded at the schema; three sort fields, each indexed; unknown sort refused with 422 |
| **INV-393** | Contact search matches user input literally, with LIKE metacharacters escaped. | Unescaped, a search for `%` returns every contact in the workspace. | **ENFORCED** — `%`, `_` and `\` escaped; probed against PostgreSQL |
| **INV-394** | Contact mutations are conditional on current state, never read-then-write. | Two concurrent requests would otherwise both write, and one would resurrect a record the other archived. | **ENFORCED** — `updateIfActive`, `archiveIfActive`, `restoreIfArchived`; zero rows is ambiguous and reported as one answer |
| **INV-395** | A contact update names every field it may change and can never move a contact between tenants or rewrite its creation time. | The mass-assignment shape INV-306 banned on accounts. | **ENFORCED** — `ContactUpdate` has no `workspaceId`, `contactId` or `createdAt`; the request schema is closed and rejects them with 422 |
| **INV-396** | The contact domain adds no RLS bypass, no `SECURITY DEFINER` and no new transaction scope. | Every contact caller is an authenticated member; a narrower scope would be a mechanism nobody asked for. | **ENFORCED** — architecture guard over the migration and the contact files |

## Documents and artifacts (BACKEND-29)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-397** | A Document is a stable workspace-owned business resource, distinct from the immutable byte-level Artifact identity. | A document outlives every set of bytes attached to it; collapsing them means either overwriting evidence or fragmenting one matter into several records. | **ENFORCED** - separate tables, separate ids; asserted by a use-case test that keeps a DocumentId across acquiring an artifact |
| **INV-398** | `DocumentId`, `ArtifactId`, the storage key and the content digest are four identifiers with four semantics, none derived from another. | Any conflation makes a mutable handle stand in for an immutable one. | **ENFORCED** - `documents` has no artifact, storage, digest, size or media-type column; architecture guard over the migration and the contract |
| **INV-399** | A document may only reference an artifact accepted by the secure upload pipeline and belonging to the same workspace. | The alternative is a document whose bytes were never scanned, or another tenant's. | **ENFORCED** - the only writer of an `original` artifact is `processUpload`; compound FK `(workspace_id, document_id)`; no storage path exists in the document domain |
| **INV-400** | Cross-workspace Document->Artifact relationships are refused by the DATABASE, not only by application code. | Application checks are one refactor from being skipped. | **ENFORCED** - migration 016's compound foreign key; probed as the runtime role with a B-context insert naming an A document |
| **INV-401** | A document has at most one ORIGINAL artifact. | Two originals leave nothing able to choose between them, and a retried upload would produce exactly that. | **ENFORCED** - partial unique index `document_artifacts_one_original_idx`; probed |
| **INV-402** | Accepted original bytes are never overwritten or rewritten by a document-domain operation. | The digest a completion certificate names must keep meaning the same bytes. | **ENFORCED** - the domain imports no storage client, no PDF library and no sealer; an integration test compares the whole artifact row before and after a rename |
| **INV-403** | Any future byte-changing operation creates a distinct derived artifact with a new id and digest. | Version history is the artifact chain; overwriting destroys it. | **ENFORCED ARCHITECTURALLY** - `source_artifact_id` provenance exists since migration 003; no update path to `digest` or `storage_reference` is reachable from the document domain |
| **INV-404** | Document title is mutable display metadata and never determines storage identity or artifact integrity. | A rename is a label change, not a byte change. | **ENFORCED** - `rename` writes one column; the storage key is derived from ids, never from the title; asserted |
| **INV-405** | Clients may not supply storage keys, digests, scan states, sizes, media types, page counts or any other authoritative artifact metadata. | Every one of them is a claim only the server can make honestly. | **ENFORCED** - one-property request schemas with `additionalProperties: false`; ten forbidden properties probed, each 422 |
| **INV-406** | Document repositories and the artifact relation are explicitly workspace-scoped and RLS-protected. | A document library is a customer's whole caseload. | **ENFORCED** - `tenant_isolation` + FORCE RLS; no repository method takes a workspace argument; cross-tenant read, list and rename all probed |
| **INV-407** | Document authorization uses centralized workspace capabilities, never a role comparison. | The correct role set here is a third distinct shape, and a role check would have got it wrong. | **ENFORCED** - three capabilities in the frozen policy; the BACKEND-27 guard covers the new files unchanged; 7 x 17 exhaustive matrix |
| **INV-408** | Document lifecycle state is distinct from SigningRequest lifecycle state, and no signing status is stored on a document. | A document may back several transactions; one status column would be wrong as soon as there are two. | **ENFORCED** - no `status` or `archived_at` column; a guard fails on any signing-status literal in the domain |
| **INV-409** | A document is never deleted, and deletion semantics can never erase signing or evidence history. | A document is referenced by immutable artifacts and, from BACKEND-32, by evidence. | **ENFORCED** - no DELETE grant for the runtime role; no method, no route; probed with a raw DELETE |
| **INV-410** | No document operation cascades into artifacts, evidence or object storage. | A cascade would let one delete destroy the record that a set of bytes existed. | **ENFORCED** - every reference is ON DELETE RESTRICT; a guard asserts the migration contains no CASCADE or SET NULL |
| **INV-411** | Document metadata changes can never rewrite historical signing evidence, completion certificates or finalized artifacts. | A document renamed after signing must leave the certificate matching what it certifies. | **ENFORCED BY ABSENCE TODAY** - nothing references a document's title; the snapshot requirement is specified for BACKEND-32 in DOCUMENT_ARTIFACT_MODEL.md |
| **INV-412** | The document domain imports no PDF manipulation or parsing library and never invokes the sealer. | Byte handling belongs to the inspection and sealing boundaries; anywhere else it is unreviewed. | **ENFORCED** - architecture guard over every file in the domain, covering `pdf-lib`, `pdfjs`, `pdf-parse`, `PDFDocument` and `DocumentSealer` |
| **INV-413** | Document content, titles, filenames, storage keys and presigned URLs are never routinely logged or used as metric labels. | A legal matter name identifies the client, the counterparty and the transaction. | **ENFORCED** - `titleLength` is logged instead, computed before the call; two source guards plus a live serialized-log assertion |
| **INV-414** | Future preparation and signing flows reference the stable DocumentId while producing distinct immutable artifacts. | It is what makes preparation possible without touching the original. | **DOCUMENTED** - specified for BACKEND-30 in DOCUMENT_ARTIFACT_MODEL.md; the guards that would catch a violation are in place |

## Document preparation (BACKEND-30)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-415** | Document preparation is mutable authoring metadata attached to a workspace Document, distinct from both Document identity and SigningRequest state. | A document may back more than one signing request; a status here would be wrong as soon as there are two. | **ENFORCED** - separate tables; no signing column; an architecture guard fails on any signing-status literal in the domain |
| **INV-416** | Every preparation targets ONE exact trusted immutable source ArtifactId. | Coordinates authored against artifact A may be meaningless for artifact B even within one document. | **ENFORCED** - `source_artifact_id` NOT NULL with a compound FK; asserted in the use case and integration |
| **INV-417** | Field geometry is valid only for the exact source artifact and page geometry it was created against. | It is the reason naming the artifact matters rather than saying "the current PDF". | **DOCUMENTED** - PREPARATION_ARCHITECTURE.md; OD-115 records what a source replacement must do |
| **INV-418** | Adding, moving, resizing, assigning or deleting a field never mutates the accepted ORIGINAL artifact. | The digest a completion certificate names must keep meaning the same bytes. | **ENFORCED** - the domain imports no PDF library, no storage client and no sealer; the whole artifact row is compared before and after a save |
| **INV-419** | Any preparation operation producing different PDF bytes creates a NEW ArtifactId and never overwrites an existing artifact. | Overwriting destroys the record that the earlier bytes existed. | **ENFORCED BY ABSENCE** - preparation is metadata-only and writes no artifact at all; a guard asserts it |
| **INV-420** | DocumentSealer is never invoked from document preparation. | Only completion may seal; preparation is not completion. | **ENFORCED** - architecture guard over every file in the domain |
| **INV-421** | Preparation and field records are workspace-scoped and protected by repository predicates, tenant-aware foreign keys and RLS. | A layout says where every signature on a contract goes. | **ENFORCED** - `tenant_isolation` + FORCE on both tables; compound FKs to document, artifact and preparation; probed as the runtime role |
| **INV-422** | Preparation authorization uses centralized workspace capabilities, never a raw role check. | The correct role set is the product's `prepare_documents`, not a guess. | **ENFORCED** - `document.prepare`; the BACKEND-27 guard covers the new files unchanged; 7 x 18 exhaustive matrix |
| **INV-423** | Field coordinates use exactly ONE canonical PDF coordinate model, shared with the frontend. | A second model produces a valid PDF with the signature in the wrong half of the page, passing every byte-level assertion. | **ENFORCED** - the BACKEND-09 model reused; a guard fails on `bottom-left`, `pixels`, `points` or `pageIndex` in the contract |
| **INV-424** | Browser viewport pixels, zoom state and device pixel ratio are never stored as authoritative geometry without canonical conversion. | The same field would persist differently on two monitors and correctly on neither. | **PARTIALLY ENFORCED** - the contract accepts only normalized 0-1 and the backend cannot tell a correct conversion from a lucky one; frontend fixtures are OD-126 |
| **INV-425** | Page numbers and geometry are validated against trusted server-inspected artifact metadata. | A client-supplied page count is a client-chosen bound. | **ENFORCED** - the ceiling comes from `document_artifacts.page_count`; no request schema accepts a page count |
| **INV-426** | Unknown field types and unknown type-specific configuration default to rejection. | Default-deny. | **ENFORCED** - closed union, `additionalProperties: false` (422), and a database CHECK |
| **INV-427** | Every preparation field type can be rendered onto a signed PDF. | A field that can be placed but never shown is a promise the system cannot keep. | **ENFORCED** - `renderTypeFor` is total onto `SealableFieldType`, asserted for every type |
| **INV-428** | Preparation stores field requirements and geometry, never actual signer-submitted values. | Preparation records the request; the ceremony records the response. | **ENFORCED** - no value column of any kind; the API rejects `value`, `signatureValue` and `signedAt` with 422 |
| **INV-429** | Contacts are never preparation field assignees; assignment uses a stable participant identity. | A contact is address-book data, not a signing participant. | **ENFORCED (strengthened by BACKEND-31)** - the opaque slot is dropped; a field names a `RecipientId` on a three-column FK, and a guard still asserts no `ContactId` reaches the preparation domain |
| **INV-430** | Mutable preparation may never be used to reconstruct the layout of an already-created signing request; future requests snapshot it. | A sender editing on Tuesday must not change what a recipient saw on Monday. | **DOCUMENTED** - PREPARATION_EDITABILITY.md specifies the snapshot for BACKEND-32; nothing reads preparation from outside the domain yet |
| **INV-431** | Field mutations are prohibited when the preparation is non-editable, and the check happens inside the write. | A freeze committing between a check and a write is a real race. | **ENFORCED** - `locked_at is null` is a predicate of the claiming UPDATE; probed by locking a row directly |
| **INV-432** | Preparation layouts, labels, coordinates, assignee identity, PDF content and storage internals are not routinely logged or used as metric labels. | A layout reconstructs a document's structure and names its parties. | **ENFORCED** - counts only; two architecture guards plus a live serialized-log assertion; reads are not logged at all |
| **INV-433** | Fields may not be placed on a document whose pages are rotated, or whose rotation is unknown. | The renderer works in unrotated space while the viewer renders rotated, so every field would land wrong with no error. | **ENFORCED** - the inspector records a rotated page count; `canPlaceFields` refuses both rotated and unknown; OD-124 |

## Signing recipients (BACKEND-31)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-434** | A signing recipient is its own identity, distinct from UserId, ContactId, WorkspaceMemberId and InvitationId. | Four plausible wrong answers were already in the schema; each conflates a participant with something that outlives the document. | **ENFORCED** - separate table, opaque server-generated `recipient_id`; an architecture guard fails on any account or membership call in the domain |
| **INV-435** | A recipient's name, email and organization are SNAPSHOT at creation and never re-read from the source contact. | A document signed in March must keep saying who signed it after everyone's details change. | **ENFORCED** - the module contains exactly ONE `uow.contacts.*` call, asserted by count; four refresh identifiers forbidden across six files |
| **INV-436** | `source_contact_id` is provenance only, is never dereferenced for a value, and is not editable by a caller. | A pointer that is read is a reference wearing a snapshot's name. | **ENFORCED** - `RecipientUpdate` names every mutable field and excludes it; the PATCH schema rejects it with 422 |
| **INV-437** | Deleting a contact leaves every recipient derived from it intact, losing only the provenance. | Erasing an address-book entry must not erase a party to an agreement. | **ENFORCED BY THE DATABASE** - `on delete set null (source_contact_id)`; the column list is load-bearing and a bare clause would null `workspace_id`; probed in integration |
| **INV-438** | A recipient email is a delivery address and is never resolved to a LAGDA account. | Resolving one leaks user existence and implies an authentication that has not happened. | **ENFORCED BY TYPES** - `RecipientEmailKey` is mutually unassignable with `NormalizedEmail` and `ContactEmailKey`; the cross-domain call does not compile; guard forbids `findByNormalizedEmail` in every recipient file |
| **INV-439** | No recipient verification, token, authentication or signature state exists to be written. | A column would be a claim LAGDA cannot support. | **ENFORCED BY ABSENCE** - eleven forbidden identifiers across the table, the port, the contract and the routes |
| **INV-440** | At most one recipient per delivery address per preparation. | Two invitations to one mailbox for one document give one person two signing positions and no way to tell which they are. | **ENFORCED** - `UNIQUE (workspace_id, preparation_id, normalized_recipient_email)`; two genuinely concurrent inserts, exactly one survives |
| **INV-441** | A preparation field may only be assigned to a recipient of the SAME preparation. | Tenant isolation cannot catch this: both rows are legitimately visible to one tenant. | **ENFORCED BY THE DATABASE** - a THREE-column foreign key; probed with a sibling preparation in the same workspace |
| **INV-442** | Recipients whose type cannot hold fields may not be assigned any, and may not be demoted into that state while holding some. | A viewer with a required signature is a participant the workflow both waits for and does not. | **ENFORCED** - `canHoldFields` total over six types; demotion refused with the assigned count |
| **INV-443** | Deleting a recipient that still holds fields is refused, never cascaded. | A sender who removes the wrong party must not silently lose positioned signature blocks. | **ENFORCED** - `ON DELETE RESTRICT` plus an application count for the message; the constraint is what makes the check race-safe |
| **INV-444** | Recipient display order is dense, 0-based and server-assigned; routing order is separate and untouched by reordering. | Reordering a list for readability must not rewrite the signing sequence. | **ENFORCED** - `normalizeOrder`; the API rejects a client `orderIndex` with 422; a test asserts routing survives a reorder |
| **INV-445** | Recipient names, email addresses, organizations and source contact ids are never logged and never used as metric labels. | A recipient row is the name and address of a party to a legal agreement. | **ENFORCED** - counts, ids and vocabulary only; whole serialized log lines asserted against real PII fixtures; the metric label set is pinned by a guard |
| **INV-446** | Recipient records are workspace-scoped and protected by repository predicates, tenant-aware foreign keys and RLS. | The recipient list IS the parties to a contract. | **ENFORCED** - `tenant_isolation` + FORCE; every query scoped by workspace AND preparation; probed as the runtime role, which holds no BYPASSRLS |
| **INV-447** | Recipient authorization uses centralized workspace capabilities and adds none of its own. | A `recipient.manage` would create a role that may place a signature field but not name who signs it. | **ENFORCED** - `document.view` / `document.prepare`; a guard asserts no `recipient.*` capability exists |
| **INV-448** | A preparation recipient set must be SNAPSHOT by a signing request rather than referenced live. | A sender editing a draft on Tuesday must not change who a recipient saw on Monday. | **DOCUMENTED** - RECIPIENT_SNAPSHOT_MODEL.md specifies it for BACKEND-32; nothing outside the domain reads recipients yet |

## Signing requests (BACKEND-32)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-449** | A SigningRequest is a workspace-owned immutable signing-workflow configuration snapshot, distinct from Document, DocumentPreparation and Artifact identities. | Four things that look alike and outlive each other differently. | **ENFORCED** - own tables, four distinct brands; a test asserts the request id equals none of the other three |
| **INV-450** | Request creation snapshots ONE coherent preparation state and never combines recipients, fields, assignments or routing from different revisions. | A half-old, half-new workflow is one nobody agreed to. | **ENFORCED** - all five reads and all three writes on one unit of work; both possible interleavings with a concurrent layout save produce a coherent result; atomicity probed in integration |
| **INV-451** | Request configuration never dynamically depends on later changes to DocumentPreparation, PreparationRecipient, PreparationField, Contact or mutable Document metadata. | A document signed in March must keep saying who signed it. | **ENFORCED** - eight independence tests; an architecture guard slices the use-case file at the read function and forbids every mutable repository after it |
| **INV-452** | Request recipients use request-scoped identities distinct from mutable preparation RecipientIds. | A preparation recipient can be edited, deleted or reused by a second request; a workflow cannot have its participants change underneath it. | **ENFORCED** - distinct brand, new id per snapshot row, asserted not equal to the source |
| **INV-453** | Request fields use request-scoped identities distinct from mutable preparation FieldIds. | A preparation field id is deliberately REUSED across saves, so it names a different rectangle after every drag. | **ENFORCED** - distinct brand, asserted not equal to the source |
| **INV-454** | A request field's assignee is a recipient of the SAME request. | Tenant isolation cannot catch this: two requests in one workspace are both legitimately visible. | **ENFORCED BY THE DATABASE** - a three-column foreign key; probed with two requests in one workspace |
| **INV-455** | Every request captures the exact immutable source ArtifactId its field geometry applies to. | Coordinates authored against artifact A may be meaningless for artifact B. | **ENFORCED** - resolved from `preparation.source_artifact_id`, compound FK, RESTRICT; no input field exists for it |
| **INV-456** | Clients may not select or override the source artifact, request recipients, field snapshots, the document title, the creator or the initial state. | A client-authored snapshot is a workflow that does not match the document anyone reviewed. | **ENFORCED** - an EMPTY closed creation body; twelve rejection cases at 422 |
| **INV-457** | Request creation performs no email delivery, credential issuance, OTP creation, recipient authentication, PDF mutation or sealing. | Creating is not sending. | **ENFORCED BY ABSENCE** - no provider, no mailer, no queue, no PDF import, no sealer; guarded across six files, and no column exists to write any of it to |
| **INV-458** | Request creation is idempotent, so a network retry cannot create a duplicate workflow. | The retry of a lost response would give one agreement two sets of invitations. | **ENFORCED** - required key, `signingRequest.create`, claim inside the business transaction; two concurrent creations produce exactly one request against real PostgreSQL |
| **INV-459** | Idempotent replay returns the ORIGINALLY created request even if the preparation changes after the first success. | Otherwise a retry after an edit reports a conflict the caller cannot act on, or creates a second workflow. | **ENFORCED** - the fingerprint is the document alone; the exact T0-T3 sequence is a test |
| **INV-460** | Request snapshot rows are immutable after creation through normal application APIs; later workflow changes are explicit state transitions on the request row. | A snapshot that can be edited is not a snapshot. | **ENFORCED BY PRIVILEGE** - the runtime role holds no UPDATE grant on either snapshot table; proven by catalog read AND by an attempted write returning permission denied |
| **INV-461** | SigningRequest is tenant-scoped and protected by membership, centralized capabilities, explicit repository scoping, tenant-aware foreign keys and RLS. | It is the parties to a contract and where each signs. | **ENFORCED** - `tenant_isolation` + FORCE on all three tables; compound FKs to document, artifact and preparation; probed as the runtime role, which holds no BYPASSRLS |
| **INV-462** | Historical recipient identity is read from request snapshots and never reconstructed from mutable Contacts. | A join to the current contact would silently undo the entire aggregate. | **ENFORCED** - no recipient file references `uow.contacts` or `ContactId` at all; the read path is guarded positionally |
| **INV-463** | Historical field geometry is read from request snapshots and never reconstructed from mutable PreparationFields. | Same. | **ENFORCED** - same guard; geometry asserted byte-identical to its source and unaffected by later moves |
| **INV-464** | Request creation requires a fully authenticated session and cannot be performed with an MFA-pending credential. | An unfinished login must not commit anyone to a signature. | **ENFORCED BY COMPOSITION** - the authenticated scope's hook rejects a pending credential before any handler runs; tested once centrally |
| **INV-465** | Request telemetry does not carry recipient PII, field layouts, document titles or storage credentials. | A request log line would otherwise name the parties to every LAGDA agreement. | **ENFORCED** - counts and ids only; the whole serialized line asserted against real PII fixtures; reads unlogged; metric labels pinned by a guard |
| **INV-466** | Creating a request does not transition it to SENT and creates no recipient ceremony state. | Nothing has left the building. | **ENFORCED** - a CHECK admitting only `draft`; an UPDATE to `sent` is a constraint violation today |

## Signing request send (BACKEND-33)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-467** | Sending a SigningRequest is an explicit idempotent workflow-state transition, never a synchronous email-provider call. | A signing workflow's correctness must not depend on a third party's uptime. | **ENFORCED** - no provider exists in any send file; the transition and all durable work commit in one transaction with no network call |
| **INV-468** | Send reads recipient identity, field configuration, routing and the source artifact exclusively from the immutable SigningRequest snapshot. | A contact renamed after creation must not change where an invitation goes. | **ENFORCED** - an architecture guard forbids the mutable ports by MODULE PATH; four independence tests |
| **INV-469** | Mutable Contacts, PreparationRecipients, PreparationFields and current authoring state never influence an already-created request during Send. | Same, from the other direction. | **ENFORCED** - the same guard, plus tests that mutate every one of them between creation and send |
| **INV-470** | Only a send-eligible unsent request may transition to SENT. | Sending twice sends twice. | **ENFORCED** - `where state = 'draft'` inside the claiming UPDATE; two concurrent transitions probed, exactly one wins |
| **INV-471** | SENT means the workflow was durably committed for recipient access. It does NOT mean an email was delivered, opened, viewed, authenticated against or signed. | Provider semantics must not redefine workflow state. | **ENFORCED BY ABSENCE** - no delivery, viewed, signed or authenticated column on any table; the event is named `sent`, not `delivered` |
| **INV-472** | The state transition, `sentAt`, initial activation, required access grants, durable delivery intents and idempotency completion commit consistently. | A request marked sent whose recipients hold no way in is worse than one that failed. | **ENFORCED** - one transaction; a multi-recipient partial failure test asserts total rollback |
| **INV-473** | A request may not be marked SENT if the durable access or delivery work for its initially active recipients failed to persist. | Same, stated as the ordering rule. | **ENFORCED** - the transition is the LAST statement; asserted positionally by a guard |
| **INV-474** | No email-provider network call occurs inside the Send database transaction. | A held transaction plus a third-party timeout is an outage. | **ENFORCED BY ABSENCE** - no provider anywhere; guarded across seven files |
| **INV-475** | Provider retries operate on the same durable DeliveryIntent and may not generate new signing-access credentials. | A retry that minted a credential would leave two live links per recipient. | **ENFORCED STRUCTURALLY** - `UNIQUE (grant_id)` on the intent and one active grant per recipient make it unrepresentable. No worker exists yet to exercise it |
| **INV-476** | Signing-access bootstrap credentials are opaque, high-entropy, recipient- and request-bound, and raw values never appear in ordinary access-grant tables or logs. | A bearer secret for a legal document. | **ENFORCED** - 256 bits, digest-only column with a shape CHECK, three-column FK, redaction guards |
| **INV-477** | A bootstrap credential authorizes only the START of the recipient-access ceremony and does not prove identity or signing authority. | Email scanners open links. | **DOCUMENTED** - SIGNING_ACCESS_PROVISIONING.md; BACKEND-34 owns the runtime control |
| **INV-478** | An access grant may reference only a SigningRequestRecipientId of the same request and workspace. | Tenant isolation cannot catch a cross-REQUEST grant. | **ENFORCED BY THE DATABASE** - three-column foreign key; probed |
| **INV-479** | Sequential routing provisions access only for the currently active cohort; later recipients hold no long-lived credential before activation. | A secret minted for a turn days away sits in a database for no reason. | **ENFORCED** - `planActivation` separates `active` from `provision`; a test asserts one grant for a three-cohort request |
| **INV-480** | Parallel routing provisions every currently eligible recipient inside one atomic Send. | | **ENFORCED** - tested for 1,1,1 and for the mixed 1,1,2 case |
| **INV-481** | Client input may not choose active recipients, credentials, the source artifact, recipient data or request state. | | **ENFORCED** - an EMPTY closed send body; routing policy derives the cohort |
| **INV-482** | Send requires a fully authenticated workspace actor holding the centralized `signing-request.send` capability, resolved against CURRENT membership. | A demoted sender must not send. | **ENFORCED** - membership read inside the transaction; removed-member test |
| **INV-483** | Send is idempotent, so retries cannot duplicate the transition, credentials or delivery intents. | | **ENFORCED** - required key, `signingRequest.send`, claim inside the transaction |
| **INV-484** | A retry with the original key returns the original result and creates no new credentials. | | **ENFORCED** - a test asserts one grant, one intent and ONE issued credential after two calls |
| **INV-485** | Bootstrap tokens, signing URLs, recipient PII, delivery secrets and provider payloads are not routinely logged or used as metric labels. | | **ENFORCED** - payload-scoped guard; the only bounded label is a three-value routing shape |
| **INV-486** | Send performs no signing ceremony, signature submission, PDF merge, certificate generation or sealing. | | **ENFORCED BY ABSENCE** - guarded across seven files |
| **INV-487** | A raw signing credential is persisted only in encrypted, purpose-separated, key-versioned form, and Send fails rather than storing plaintext. | It is the one recoverable secret besides a TOTP seed. | **ENFORCED** - AES-256-GCM `SecretBox` with its own key; an absent key throws before the transition, asserted by test |
| **INV-488** | Signing links are built from configured canonical base, never from an inbound request header. | A link from a Host header is a link an attacker chose, sent over LAGDA's reputation. | **ENFORCED** - the builder takes no request and cannot see one; guarded |

## Recipient signing access (BACKEND-34)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-489** | A signing bootstrap credential resolves exactly one SigningRequestRecipient and is not a user session, a workspace credential, an OTP or a signing submission. | Four things it could be mistaken for, each with different authority. | **ENFORCED** - its own digest domain, its own brand, its own transaction setting; a guard forbids reusing any other constant |
| **INV-490** | Recipient signing access never requires WorkspaceMembership and grants no workspace capability. | An external signer has no account. | **ENFORCED** - the routes sit outside the authenticated scope; no workspace authorization symbol appears in the surface; the context carries no role or membership |
| **INV-491** | A bootstrap credential authorizes only the START of the recipient-access ceremony for its bound recipient. | It is a bearer secret in an email. | **ENFORCED** - it produces a session and nothing else; no viewed, consent, signature or completion is written |
| **INV-492** | Possession is recorded as the exact configured method and is never described as legal identity verification. | Rounding up an assurance level is the failure mode. | **ENFORCED** - the session records `link-only`; no `identity_verified` field exists anywhere; RECIPIENT_AUTHENTICATION_POLICY.md states what it does not prove |
| **INV-493** | Signing-access lookup uses a secure digest and a narrow credential-based path with no blanket RLS bypass. | | **ENFORCED BY THE DATABASE** - four FOR SELECT policies on unique columns; unfiltered selects return 1 of 2; no BYPASSRLS, no SUPERUSER |
| **INV-494** | A credential is valid only while its grant is active, unexpired, unrevoked, bound to the recipient and request, and the recipient is routing-eligible. | | **ENFORCED** - five conditions in one place; each has a test |
| **INV-495** | A GET or link preview may not consume a credential, create a session, or record authentication, viewing, consent or signing. | Email gateways fetch every link. | **ENFORCED** - the only GET is a context read; a guard asserts the bootstrap route is a POST and the file has no `:token` parameter |
| **INV-496** | Where additional authentication is required, bootstrap creates a PENDING ceremony rather than a session. | | **NOT APPLICABLE** - LINK_ONLY. The seam exists; OD-140 lists what OTP would need |
| **INV-497** | Recipient OTP, when enabled, uses the canonical OTP architecture with a keyed verifier, durable attempt limits, expiry, resend limits, purpose binding and zero logging. | | **DOCUMENTED ONLY** - no OTP is implemented, and a guard asserts no challenge, verifier or attempt counter exists |
| **INV-498** | Recipient OTP destinations come exclusively from the immutable recipient snapshot. | | **NOT APPLICABLE** today, and structurally guaranteed: nothing in the surface reads a contact or resolves by email |
| **INV-499** | Successful authentication creates a FRESH opaque session credential, distinct from the bootstrap credential and from any pre-auth credential. | A session derived from an emailed link inherits the link's exposure. | **ENFORCED** - generated server-side; tests assert non-equality and non-containment both ways |
| **INV-500** | Session credentials are stored only as digests and delivered only through secure HttpOnly cookies. | | **ENFORCED** - digest columns with shape CHECKs; no raw value in any response shape |
| **INV-501** | A session is scoped to exactly the request and recipient it was authenticated for. | | **ENFORCED BY THE DATABASE** - a three-column foreign key; two recipients sharing an email get two sessions that cannot cross |
| **INV-502** | Recipient and user session cookies are separate realms and may coexist without conferring each other's permissions. | | **ENFORCED** - five distinct cookie names, distinct digest domains, distinct resolvers |
| **INV-503** | Recipient sessions may not reach workspace, member, contact or document-management APIs, and user sessions do not automatically act as recipients. | | **ENFORCED BY COMPOSITION** - workspace routes live inside a scope whose hook does not read this cookie; bootstrap reads no session cookie at all |
| **INV-504** | Recipient CSRF is session-bound and separate from user CSRF. | | **ENFORCED** - a separate digest domain and an independent draw; a CHECK refuses equal digests. NOT YET ENFORCED AT A ROUTE - no recipient mutation exists |
| **INV-505** | Recipient authentication never depends on mutable Contacts or PreparationRecipients. | | **ENFORCED** - guards forbid the imports and the lookup shapes |
| **INV-506** | Authentication facts and operational logs are separate; no raw token, OTP, URL, session credential or digest appears in either evidence or telemetry. | | **ENFORCED** - the session row is the fact; payload-scoped guards cover the logs |
| **INV-507** | Recipient authentication alone submits no field value, signature, consent, decline, PDF mutation, completion or sealing. | | **ENFORCED BY ABSENCE** - guarded across five files and the migration |
| **INV-508** | A recipient session says who is asking, not that the request is still signable; eligibility is never cached in a cookie. | A session outlives the state that justified it. | **ENFORCED** - resolution deliberately does not re-check request state, and a test asserts it. BACKEND-35 must revalidate |

## BACKEND-35 — the signing ceremony

| # | Invariant | Failure it prevents | Enforcement |
|---|---|---|---|
| **INV-509** | Ceremony access requires a valid `RecipientSigningContext` and never `WorkspaceMembership`, a workspace role or a workspace capability. | A recipient authorized as a member, or a member acting as a recipient. | **ENFORCED** - guards forbid every workspace authorization symbol and all 21 capability names across six files |
| **INV-510** | A ceremony is scoped to exactly one immutable SigningRequest and one SigningRequestRecipient, both taken from the session. | Cross-request or cross-recipient access. | **ENFORCED BY THE TYPE AND THE DATABASE** - the repository's read methods take no identifying argument, and six restrictive policies bind every read to the session's recipient |
| **INV-511** | Ceremony data derives exclusively from immutable SigningRequest recipient and field snapshots and the exact `source_artifact_id`. | A signing transaction that changes under the signer. | **ENFORCED** - no method exists for a contact or a preparation; deletion tests prove indifference |
| **INV-512** | Mutable Contacts, DocumentPreparations, PreparationRecipients, PreparationFields, current Document artifacts and profile data may never alter an existing ceremony. | Authoring state reaching a sent request. | **ENFORCED** - guards plus three mutation-independence tests |
| **INV-513** | Recipient document access uses a dedicated recipient-authorized path and never workspace document authorization as its basis. | One weaker definition of who may read a document winning over the stronger. | **ENFORCED** - a separate use case, a separate repository, no shared authorization code |
| **INV-514** | A recipient may not retrieve arbitrary workspace documents or arbitrary SigningRequests through a recipient session. | Enumeration inside a tenant. | **ENFORCED BY THE DATABASE** - 1 request of 2, 1 artifact of 2, measured as the runtime role |
| **INV-515** | A recipient receives only the fields assigned to their SigningRequestRecipientId. | One signer learning what another was asked for. | **ENFORCED** at three layers; the other signer's name is asserted absent |
| **INV-516** | The canonical request field geometry and PDF coordinate model pass through the ceremony API unchanged. | Two conflicting sources of truth about where a signature goes. | **ENFORCED** - normalized 0-1 values returned verbatim; the frontend already converts |
| **INV-517** | Entering or viewing the ceremony submits no signature, completes no field, establishes no consent, advances no routing and completes no request. | A page load that means something legally. | **ENFORCED BY ABSENCE** - guarded across six files and the migration, and asserted directly |
| **INV-518** | Email scanners and link previews cannot produce ceremony-entry, viewed, consent or signing events. | A mail gateway signing a document. | **ENFORCED** - entry needs an HttpOnly session cookie and a CSRF token; after a bootstrap exchange both ceremony tables are empty |
| **INV-519** | Any ENTERED or VIEWED record has a documented operational meaning and may not claim that a human read any page. | Evidence that overstates itself. | **ENFORCED BY DOCUMENTATION AND SHAPE** - CEREMONY_VIEW_EVENT.md states the meaning; no field exists that could hold a stronger claim |
| **INV-520** | Where consent is required, acceptance is an explicit recipient-session-authenticated action bound to an exact type and version, timestamped by the backend clock. | An acceptance nobody can identify the subject of. | **ENFORCED** - unique on (workspace, request, recipient, type, version); the clock is the only source |
| **INV-521** | Consent acceptance is separate from recipient authentication and separate from signature submission. | Consent inferred from arriving. | **ENFORCED** - three distinct records, and tests assert each leaves the others untouched |
| **INV-522** | Ceremony state-changing mutations use recipient-session CSRF and may not be satisfied by a user-session CSRF credential. | Cross-realm forgery. | **ENFORCED BY DERIVATION** - separate digest domains; a workspace token cannot match |
| **INV-523** | Recipient document access always uses the exact immutable `source_artifact_id` and never a mutable current-artifact pointer. | Serving different bytes than were sent. | **ENFORCED** - the accessor takes no artifact parameter, the query joins from the request, and a restrictive policy admits only the frozen artifact |
| **INV-524** | Ceremony PDF access exposes no storage key and persists or logs no ephemeral document URL. | A credential or an internal path in a log. | **ENFORCED** - the key never leaves the application layer; no URL is issued at all |
| **INV-525** | The ceremony performs no PDF mutation and never invokes DocumentSealer. | A read path that writes bytes. | **ENFORCED BY ABSENCE** - guarded across six files |
| **INV-526** | Ceremony reads and writes remain tenant, request and recipient scoped through trusted context, explicit predicates, tenant-aware foreign keys and RLS. | Any one layer's bug becoming a leak. | **ENFORCED** - four mechanisms, independently tested |
| **INV-527** | Ceremony telemetry contains no PDF content, field layout, recipient PII, consent text, session credential or document URL. | Sensitive content in the widest-replicated store in the stack. | **ENFORCED** - payload-scoped and label-scoped guards |
| **INV-528** | Authoritative signature and field-value persistence belongs to BACKEND-36 and may not be inferred from ceremony UI state. | In-progress browser state treated as a signature. | **ENFORCED BY ABSENCE** - no table, no port, no validation path; the product itself holds input in memory |
| **INV-529** | A recipient-realm row-level security policy that narrows access must be RESTRICTIVE. | A policy intended to narrow that widens, because permissive policies OR. | **ENFORCED** - all six asserted `polpermissive = false`, in the architecture suite and again against the live catalog |

## BACKEND-36 — signature submission

| # | Invariant | Failure it prevents | Enforcement |
|---|---|---|---|
| **INV-530** | Authoritative signing submission requires a valid `RecipientSigningContext` and may only affect fields assigned to that exact recipient. | One signer signing another's field. | **ENFORCED BY THE DATABASE** - a four-column FK to the field ASSIGNMENT; a cross-recipient value has no referent, proven against a bypassed application layer |
| **INV-531** | Submission operates exclusively on immutable snapshot identities and never on mutable Contact, PreparationRecipient or PreparationField data. | A signing act that changes when the sender edits. | **ENFORCED** - guards over seven files; deletion tests prove indifference |
| **INV-532** | Recipient identity, request identity, field ownership, field geometry and source artifact identity may never be client-overridden during submission. | A client choosing what it signs and where. | **ENFORCED** - the body schema has no member for any of them and `additionalProperties: false` |
| **INV-533** | Final submission is idempotent; a network retry may not create a duplicate or conflicting accepted signing act. | Double-signing on a lost response. | **ENFORCED** - `signature.submit` under recipient scope; same key replays the original `acceptedAt` |
| **INV-534** | Accepted `SigningFieldValue` rows are immutable and may not be updated, replaced or deleted through normal signing APIs. | A signature quietly becoming a different signature. | **ENFORCED BY PRIVILEGE** - SELECT and INSERT only; `permission denied`, asserted |
| **INV-535** | Under the atomic model either all of a recipient's final values commit or none do. | A half-signed recipient. | **ENFORCED** - one transaction, one repository call; asserted on a mid-set failure |
| **INV-536** | A second concurrent or deliberate submission may not overwrite an accepted `RecipientSubmission`. | Two devices racing to define one signing act. | **ENFORCED** - unique per recipient; exactly one survives, in fakes and in real PostgreSQL |
| **INV-537** | Every accepted value is tenant-, request-, recipient-, submission- and field-bound through application validation and database integrity. | Any one layer's bug becoming a leak. | **ENFORCED** - four FKs and three predicates |
| **INV-538** | Signing values use strict field-type-specific contracts; untyped payloads are prohibited. | A checkbox arriving as the string "yes". | **ENFORCED** - a discriminated union plus explicit typed columns; a guard forbids `any` |
| **INV-539** | Server-owned fields derive from backend-authoritative state and a client value for one is REJECTED, not ignored. | A client-chosen signing date nobody notices. | **ENFORCED** - `field-server-owned`; the contract has no member for these types |
| **INV-540** | Submission revalidates request signability, routing eligibility, session validity and required consent at commit time. | A page loaded twenty minutes ago authorizing a signature now. | **ENFORCED** - all five inside the transaction |
| **INV-541** | Required consent may not be asserted by a client boolean; it comes from the authoritative record. | Consent claimed rather than given. | **ENFORCED** - the contract has no `consentAccepted`; the record is read |
| **INV-542** | Submission timestamps are backend-authoritative, and one instant covers the whole act. | Two times for one signature. | **ENFORCED** - one Clock read; `accepted_at` is the only timestamp on the row |
| **INV-543** | A signature representation is sensitive signing content: never routinely logged, never a public asset, never reused across unrelated requests by default. | A signature image in a log or a shared library. | **ENFORCED** - payload guards; no cross-request reuse exists |
| **INV-544** | Binary signature assets are bounded, format-validated and stored privately, and any object-storage model must acknowledge non-atomicity with the database. | An accepted submission referencing bytes that are not there. | **ENFORCED** - stored in PostgreSQL, so the window does not exist; bounds in the validator AND a CHECK |
| **INV-545** | Submission persists logical signing values and performs no PDF mutation. | Coupling a legal record to a rendering. | **ENFORCED BY ABSENCE** - guarded across seven files |
| **INV-546** | Submission never invokes DocumentSealer. | | **ENFORCED BY ABSENCE** |
| **INV-547** | Submission does not activate the next recipient, complete the request, generate a certificate or seal the document. | A signing act silently advancing a workflow nobody transitioned. | **ENFORCED** - guarded, and asserted directly after acceptance |
| **INV-548** | `RecipientSubmission.acceptedAt` is the authoritative input for BACKEND-37's recipient signed-state transition; BACKEND-37 may not invent a separate signing time. | A signing time that is really a scheduling delay. | **DOCUMENTED ONLY** - BACKEND-37 must honour it; nothing here can enforce it |
| **INV-549** | Signing evidence references submission and value identities and does not duplicate raw signature payloads or credentials. | The most sensitive bytes in the system living in two places. | **ENFORCED** - no evidence write duplicates content; the digest is the reference |
| **INV-550** | Workspace/user CSRF may not authorize recipient signing submission. | Cross-realm forgery of a signature. | **ENFORCED BY DERIVATION** - separate digest domains |
| **INV-551** | Signature values, field values, recipient PII, credentials, asset locations and observed request data are never metric labels or routine log payloads. | A salary figure in a log line. | **ENFORCED** - payload-scoped and label-scoped guards |

## BACKEND-37 — the signing workflow state machine

| ID | Invariant | The failure it prevents | Status |
|---|---|---|---|
| **INV-552** | Signing workflow state is a PROJECTION of authoritative domain facts and never replaces `RecipientSubmission` or `SigningFieldValue` evidence. | Reconstructing what somebody signed from a status column. | **ENFORCED** — the state names the submission through a four-column FK; nothing derives evidence from it |
| **INV-553** | A recipient may transition to `signed` only from an accepted `RecipientSubmission` for the SAME request and recipient. | A signature the workflow claims and nobody made. | **ENFORCED** — transition table (`active` is the only source), CHECK, and the four-column FK |
| **INV-554** | `recipient.signedAt` equals `RecipientSubmission.acceptedAt`; BACKEND-37 generates no second signing timestamp. | A scheduling delay presented as a signing time. INV-548 discharged. | **ENFORCED** — `markSignedFromSubmission` takes the value and there is no clock on the path; asserted by test |
| **INV-555** | State transitions are explicit semantic operations; a generic client-controlled `setStatus`/`setState` API does not exist at any layer. | `PATCH /signing-requests/:id { state }`. | **ENFORCED** — architecture guard over every signing source file and every route |
| **INV-556** | Request and recipient workflow state live apart from the immutable recipient and field snapshots. | A record of what was asked becoming mutable. | **ENFORCED** — no UPDATE grant on either snapshot table |
| **INV-557** | Authentication, ceremony entry and consent acceptance do not by themselves mark a recipient signed or advance routing. | A viewed document counting as a signature. | **ENFORCED** — asserted directly in the access and ceremony suites |
| **INV-558** | Under sequential routing, only completion of EVERY required signer in the active cohort may activate the next. | A later signer receiving the document before an earlier one acted. | **ENFORCED** — `planWorkflowAdvance`, asserted exhaustively |
| **INV-559** | Next-cohort activation reuses the canonical BACKEND-33 provisioner and invents no access-link or token logic. | Two credential implementations drifting on TTL or digest domain. | **ENFORCED** — guard asserts the call and the absence of `tokens.issue` / `sealer.seal` |
| **INV-560** | Under parallel routing, one recipient's completion neither blocks nor deactivates the others. | A parallel workflow silently serialized. | **ENFORCED** — asserted in core and application suites |
| **INV-561** | Every required signing participant must be `signed` before the request becomes `completion-ready`. | A document completed with an outstanding signature. | **ENFORCED** — explicit participant set, never a count |
| **INV-562** | Non-signing recipient types may not block completion. | A carbon-copy stalling a transaction forever. | **ENFORCED** — `canHoldFields && isRequired`, asserted per type |
| **INV-563** | `completion-ready` means every recipient obligation is satisfied and the final artifact and seal have NOT been produced. | Claiming a document that does not exist. | **ENFORCED** — separate state, separate timestamp, separate command |
| **INV-564** | BACKEND-37 does not produce `completed`. | A terminal legal state reached before the work that earns it. | **ENFORCED** — one inbound edge from `completion-ready`; guard asserts the string is absent from the workflow module |
| **INV-565** | Recipient progression, routing, provisioning and delivery intents follow an explicit recoverable consistency model. | An accepted signature with no consequence and no way to notice. | **ENFORCED** — intent commits with the signature; reconciler applies it |
| **INV-566** | Reprocessing the same `RecipientSubmission` duplicates no transition, activation, grant, intent or readiness. | A second invitation, or two credentials for one person. | **ENFORCED** — conditional UPDATEs plus a unique intent key |
| **INV-567** | Concurrent completions converge: the next cohort activates at most once and readiness is established at most once. | Two final signers both completing the request. | **PARTIALLY ENFORCED** — conditional UPDATEs make it true; not yet proven against real PostgreSQL |
| **INV-568** | An accepted `RecipientSubmission` is never discarded because downstream provisioning or delivery failed. | Losing a legally binding act to a transient error. | **ENFORCED** — the post-commit advance is best-effort by construction |
| **INV-569** | Asynchronous progression is durably and automatically reconcilable until applied. | Manual repair as the recovery plan. | **ENFORCED** — `reconcileSigningWorkflow`, same code path as the ordinary advance |
| **INV-570** | Terminal request states prevent further signing, routing and access regardless of existing sessions or grants. | A forwarded link outliving a cancellation. | **ENFORCED** — state check first, plus grant and session revocation |
| **INV-571** | Workflow transitions stay workspace- and request-scoped, through bound repositories, tenant-aware foreign keys and RLS. | A cross-tenant state change. | **ENFORCED** — no method takes a workspace argument; restrictive policies added by 024 |
| **INV-572** | Signing-state telemetry carries no recipient PII, signature or field values, credentials, or high-cardinality identifiers. | A recipient's address in a metric label. | **ENFORCED** — bounded outcome plus counts; architecture guard |
| **INV-573** | BACKEND-37 performs no PDF merge, produces no signed document or certificate, and never invokes `DocumentSealer`. | A command quietly acquiring the completion pipeline. | **ENFORCED** — guard over imports and source |

## BACKEND-38 — the completion pipeline foundation

| ID | Invariant | The failure it prevents | Status |
|---|---|---|---|
| **INV-574** | Completion may begin only from the canonical `completion-ready` state. | Producing a document for a request somebody still owes a signature on. | **ENFORCED** — one state, not a list; asserted over every other state |
| **INV-575** | Completion revalidates the authoritative submissions and values rather than trusting the state column. | A corrupt projection producing a legally final document. | **ENFORCED** — `assessCompletionEligibility` |
| **INV-576** | Completion reads only immutable snapshots and the exact `sourceArtifactId`; contacts, preparations and the document's current artifact may never influence it. | A document sealed against bytes nobody agreed to. | **PARTIALLY ENFORCED** — the rule takes the source artifact as an input fact; the loader that would prove it is BACKEND-38's next run |
| **INV-577** | One logical `CompletionRun` per request; retries and duplicate triggers reuse it. | Two pipelines producing two final documents. | **ENFORCED** — `UNIQUE (workspace_id, signing_request_id)` |
| **INV-578** | One accepted result per logical completion step. | Two accepted seals whose bytes differ because a certificate carries a timestamp. | **ENFORCED** — `UNIQUE (completion_run_id, step)` |
| **INV-579** | Completion run state is separate from `SigningRequest.state`, which stays `completion-ready` while production is pending. | Claiming a user-visible document state that has not succeeded. | **ENFORCED** — separate table; no `COMPLETING` state introduced |
| **INV-580** | A request may not become `completed` merely because all recipients signed. | The failure BACKEND-37 created `completion-ready` to prevent. | **ENFORCED** — the request CHECK does not admit `completed` at all |
| **INV-581** | `completedAt` is backend-authoritative pipeline-success time, distinct from the last recipient's `acceptedAt`. | A scheduling delay recorded as a legal completion time. | **ENFORCED** — a separate column on a separate table |
| **INV-582** | A successful completion is unique per request and immutable. | A final artifact silently repointed. | **ENFORCED** — primary key plus NO UPDATE grant |
| **INV-583** | Completion failures are recorded as a closed code, never a raw exception. | A field value or document title in a business table. | **ENFORCED** — CHECK-constrained vocabulary |
| **INV-584** | Retryable and terminal failures are distinguished, and retries are bounded. | Retrying deterministic corruption forever. | **ENFORCED** — total classification record + `mayAttemptAgain` |
| **INV-585** | Completion uses the established `DocumentSealer` seam; no second sealing architecture exists. | Twenty call sites a remote signer would have to reproduce. | **ENFORCED** — three steps, one `seal`; no new sealing port |
| **INV-586** | The completion orchestrator contains no PDF-library type or logic. | The boundary ADR-005 was built around. | **ENFORCED** — core imports contracts only |
| **INV-587** | No fake or no-op completion adapter may cause a successful completion. | Green tests standing in for an unbuilt pipeline. | **ENFORCED** — no adapter exists, and the request CHECK cannot reach `completed` |
| **INV-588** | The recipient realm cannot read completion runs, steps or completions. | A signer learning that finalization is failing. | **ENFORCED** — restrictive deny on all three tables |
| **INV-589** | A `completion-ready` request must be automatically discoverable when it has no runnable work. | A stranded request nobody notices. | **DOCUMENTED ONLY** — the trigger is not wired; OD-158 |
| **INV-590** | Completion certificate generation consumes ONLY immutable snapshots, accepted submissions, ceremony/consent facts and accepted artifact integrity metadata. | Mutable account data rewriting historical signing facts. | **ENFORCED** — one query over immutable tables; no contact/preparation/profile read exists |
| **INV-591** | The certificate may never use Contacts, PreparationRecipients, PreparationFields, current user profiles, current workspace membership or current Document authoring state as historical truth. | Renaming a contact changing who a completed document says signed it. | **ENFORCED** — `listCertifiedParticipants` touches none of them; import/query audit |
| **INV-592** | The certificate is a CURATED versioned projection, not a dump of evidence or log records. | A user agent or internal id reaching a page forwarded to third parties. | **ENFORCED** — `CompletionCertificateModelV1` is a whitelist; the renderer receives nothing else |
| **INV-593** | The certificate describes authentication using the exact mechanism established, and may never present link possession or email OTP as legal identity verification. | An assurance overclaim on a document read as evidence. | **ENFORCED** — total `Record` of wording; builder and renderer both fail closed |
| **INV-594** | Signer timestamps derive from `RecipientSubmission.acceptedAt` and are never regenerated at certificate time. | A certificate time diverging from the signature it describes. | **ENFORCED** — model requires `signedAt`; builder refuses a missing one |
| **INV-595** | Ceremony/view timestamps use their exact documented meaning and may not claim a human read the document. | "Recipient read the document" asserted from a session-entry event. | **ENFORCED** — rendered as "Signing session entered"; wording fixed in the renderer |
| **INV-596** | Represented consent references the exact accepted type/version and backend-authoritative time, all-or-nothing. | Certifying "consented" without saying to what or when. | **ENFORCED** — builder throws `incomplete-consent` on a partial record |
| **INV-597** | Raw OTPs, tokens, session credentials, signature representations, field values, storage keys and provider details may never appear in certificate content. | Secrets distributed inside a document handed to counterparties. | **ENFORCED** — the model has no field for any of them |
| **INV-598** | IP, user-agent and device metadata are omitted from the certificate by default. | Privacy leakage through an artifact designed to be forwarded. | **ENFORCED** — no model field; LAGDA stores no IP as evidence at all |
| **INV-599** | The certificate may not contain a final sealed digest before the final artifact exists, nor any self-referential hash. | A circular or fabricated integrity claim. | **ENFORCED** — no model field; the certificate digest is artifact metadata |
| **INV-600** | The certificate may not state the request is COMPLETED before BACKEND-41 finalization succeeds. | A document asserting a completion that has not happened. | **ENFORCED** — no `completedAt` in the model; "Completed"/"Sealed" removed from the renderer, asserted by a source scan |
| **INV-601** | Certificate schema semantics use `certificateVersion`; layout semantics use `rendererVersion`. | A layout change indistinguishable from a change in certified facts. | **ENFORCED** — two constants, both recorded on the artifact and rendered |
| **INV-602** | Certificate ordering, time format, pagination, typography and Unicode handling are server-controlled and deterministic. | Output varying by host locale or installed fonts. | **ENFORCED** — vendored fonts, fixed UTC format, immutable ordering; determinism proven |
| **INV-603** | Certificate generation produces a distinct immutable artifact with a new ArtifactId, private storage, server-observed size and server-computed SHA-256. | A certificate that cannot be distinguished from its inputs. | **ENFORCED** — `completion-certificate` kind; size and digest taken from the bytes |
| **INV-604** | The certificate artifact is not the source, merged candidate or final sealed artifact. | Composition consuming the wrong file. | **ENFORCED** — four distinct artifact kinds; inputs asserted unchanged |
| **INV-605** | Once a CERTIFICATE step output is accepted, retries reuse it rather than replacing it. | Two authoritative certificates differing only by generation time. | **ENFORCED** — accepted output short-circuits before the generator is called |
| **INV-606** | A CERTIFICATE step is not successful if its artifact record is missing or fails integrity checks. | A step reporting success over nothing. | **PARTIALLY ENFORCED** — the artifact row and step are written in one transaction; object re-verification on reuse is BACKEND-41's |
| **INV-607** | BACKEND-40 may not invoke `DocumentSealer`, transition the request to `completed`, create public verification, or send notifications. | A step exceeding its boundary. | **ENFORCED** — no sealer import; audited |
| **INV-608** | `seal()` renders neither fields nor the certificate; each is produced by its own step. | A field or certificate produced twice, composed ambiguously. | **ENFORCED** — OD-162 and OD-167; both removed from `SealResult` |
| **INV-609** | BACKEND-41 final completion is the ONLY business boundary permitted to invoke `DocumentSealer`. | Twenty call sites a remote signer would have to reproduce. | **ENFORCED** — repository audit: one caller, `final-seal.ts` |
| **INV-610** | Final sealing consumes only ACCEPTED immutable FIELD_MERGE and CERTIFICATE outputs of the same run, request and workspace. | Composing another run's document. | **ENFORCED** — resolved by identity from the run's step rows; workspace-scoped repositories |
| **INV-611** | Final sealing never regenerates or modifies accepted values, submissions, snapshots, merged output or certificate output. | A retry rewriting a signature. | **ENFORCED** — the step has no write path to any of them |
| **INV-612** | Input bytes are verified against their recorded digests before anything is uploaded or persisted. | Sealing a restored object or a partial write as authoritative. | **ENFORCED** — digests returned by the sealer, compared before upload |
| **INV-613** | The provider ETag is never LAGDA's integrity digest. | A provider-specific value standing in for a document hash. | **ENFORCED** — SHA-256 computed server-side over the stored bytes |
| **INV-614** | Final sealing never overwrites the source, merged, certificate or a previously accepted final artifact. | Destroying the inputs a retry and a verifier need. | **ENFORCED** — new ArtifactId and key per attempt; asserted unchanged by test |
| **INV-615** | Every final document receives a distinct immutable ArtifactId and a backend-generated private storage identity. | Two completions sharing an object. | **ENFORCED** |
| **INV-616** | A request may become `completed` ONLY after the final artifact, seal metadata, verification record and completion record are durably persisted. | A completed request with no document. | **ENFORCED** — one transaction, transition last, rollback on conflict |
| **INV-617** | `completedAt` is generated by the finalization transaction and is distinct from every recipient's `signedAt`. | A signing time presented as a completion time. | **ENFORCED** — clock read inside the transaction; test uses a signing time a day earlier |
| **INV-618** | A successful completion is unique per signing request. | Two authoritative final documents. | **ENFORCED** — `UNIQUE (signing_request_id)` plus no UPDATE/DELETE grant |
| **INV-619** | Retries may not create competing authoritative final artifacts once a FINAL_SEAL output is accepted. | A second sealed document from a lost response. | **ENFORCED** — run claim, step unique key, completion unique key |
| **INV-620** | An uploaded final object whose transaction failed leaves the request non-completed and is a reconciliation candidate, never deleted on uncertainty. | Destroying a real artifact on an ambiguous outcome. | **ENFORCED** — no delete path; OD-160 sweeps |
| **INV-621** | A `completed` request whose final object is missing or fails its digest is a critical integrity incident and is never silently downgraded or resealed. | Quietly rewriting a legally significant record. | **ENFORCED BY ABSENCE** — no code path from `completed` to any other state; migration 028's `down` refuses |
| **INV-622** | No PDF-library or provider type crosses the `DocumentSealer` seam; composition inputs are semantic bytes. | A seam a remote signer cannot implement. | **ENFORCED** — architecture guard; `completionCertificate` is a byte input, not `appendPages()` |
| **INV-623** | The seal is described only as `hash-evidence` v1 / SHA-256 and may not claim PAdES, X.509, PNPKI, RFC 3161, TSA or HSM properties. | Overclaiming cryptographic assurance on a legal document. | **ENFORCED** — none implemented, none referenced; LAGDA_SEAL_SEMANTICS.md lists the absences |
| **INV-624** | A VerificationId is a public-safe opaque lookup identifier, never a bearer credential or download token, and exactly one exists per completed request. | A verification link used as document access. | **ENFORCED** — created in the finalization transaction; uniqueness from the completion row |
| **INV-625** | Once `completed`, no signing submission, routing activation or signing-access bootstrap may succeed. | Signing a document that is already final. | **ENFORCED** — `SIGNABLE_REQUEST_STATES` allow-list; plus explicit revocation, tested independently |
| **INV-626** | Final sealing sends no notifications and implements no public verification. | A command exceeding its boundary. | **ENFORCED** — no such code; BACKEND-42/44/45 own them |
| **INV-627** | Final-sealing telemetry never logs document bytes, certificate content, signing values, credentials, storage keys or recipient PII. | Evidence leaking through logs. | **ENFORCED** — bounded failure codes only; no byte or model logging |
