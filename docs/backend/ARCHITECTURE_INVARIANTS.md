# LAGDA Backend — Architecture Invariants

Rules every `BACKEND-XX` command checks before modifying backend code.

**Enforcement column is honest.** "Documentation" means a human or a reviewing
command must catch a violation — nothing fails automatically. This repository has
already shipped one contract that existed but was never consumed
(`RouteMeta.status`, declared on 225 routes, read by no code, drifted until three
routes misreported themselves). Invariants become executable as the packages they
govern are created; until then the column says so rather than implying safety
that does not exist.

**Current enforcement is tracked in [`ENFORCEMENT_MATRIX.md`](./ENFORCEMENT_MATRIX.md)**,
which BACKEND-01 introduced. That file is authoritative for *what executes*; this
one is authoritative for *what the rules are*.

| ID | Invariant | Enforcement today |
|---|---|---|
| **INV-001** | No PDF library import outside `packages/sealing`. | **ENFORCED** — ESLint, backend + frontend |
| **INV-002** | Only the signing-completion use case may invoke `DocumentSealer`. | Documented only → BACKEND-09 |
| **INV-003** | Workspace-owned data access must be workspace scoped at the repository boundary. | Documented only → BACKEND-06/07 |
| **INV-004** | API routes may not contain primary domain logic. | Documented only → BACKEND-11 |
| **INV-005** | `core` may not depend on infrastructure packages. | **ENFORCED** — ESLint (BACKEND-01) |
| **INV-006** | Frontend source files are not backend runtime dependencies. | **ENFORCED** — architecture test (BACKEND-01) |
| **INV-007** | Shared API/domain contracts originate from `@lagda/contracts`. | **Partially enforced** — ESLint bars infrastructure from `contracts`; origination needs BACKEND-02 |
| **INV-008** | Public API types may not expose infrastructure-library types. | Documented only → BACKEND-09/11 |
| **INV-009** | eNotary implementation is outside current scope. | Documentation + frontend disclosure tests |
| **INV-010** | Original accepted uploaded documents are immutable. | Documented only → BACKEND-05 |
| **INV-011** | Operational logs and evidence/audit records are separate concerns. | Documented only → BACKEND-11/12 |
| **INV-012** | Retry-sensitive operations must support durable idempotency. | Documented only → BACKEND-03 |
| **INV-013** | Business state transitions must be explicit and validated. | Documented only → BACKEND-07 |
| **INV-014** | Tenant isolation is tested, not assumed. | Documented only → BACKEND-07 |
| **INV-015** | Later architectural changes require an ADR or an explicit backend command. | Process |

## Additional invariants from repository inspection

These were derived from findings in
[`architecture.md` §7](./architecture.md#7-repository-findings), not from the
generic rule set.

| ID | Invariant | Why | Enforcement today |
|---|---|---|---|
| **INV-016** | `WorkspaceId` is a branded type and is used for every workspace-owned reference in `@lagda/contracts`. | The tenant key is currently a plain `string` in ~78% of its declarations (F-1). Branding it is what makes INV-003 checkable by the compiler rather than by review. | Documentation → **BACKEND-02 blocker** |
| **INV-017** | The backend redactor matches sensitive keys by substring/pattern and recurses through arrays. | The frontend redactor does neither: `resetToken` and `sessionToken` pass through, and arrays are returned unredacted (F-2). Server logs are persistent, so porting it verbatim would be worse than having none. | Documentation → unit test at BACKEND-01 |
| **INV-018** | Status transition rules are owned by `core`. Frontend `*Status` unions are vocabulary, not lifecycle authority. | 50 status unions exist with zero transition tables (F-3). There is no state machine to port. | Documentation → `core` |
| **INV-019** | No invariant may be recorded here without an enforcement plan naming the command that makes it executable. | Prevents this file becoming the next `RouteMeta.status` (F-4). | This table's enforcement column |
| **INV-020** | No package dependency cycles, and `package.json` workspace dependencies must agree with TypeScript project references. | The graph is declared in two places. If they drift, `tsc --build` orders packages by one graph while the code depends on the other, and a cycle makes the build order non-deterministic. Added by BACKEND-01 when the packages became real. | **ENFORCED** — architecture test |
| **INV-021** | Shared contract types are DERIVED from runtime schemas (`Static<typeof XSchema>`), never declared alongside them. | A hand-written type and a hand-written validator for the same structure drift, and the drift is invisible until a request is rejected in production. ADR-002. | Documented → review |
| **INV-022** | `@lagda/contracts` stays browser-compatible: no Node built-ins, no environment reads, no side effects on import. | The frontend is a first-class consumer. A `node:crypto` import would break its build, and a side effect on import would make a contract package do something. | **PARTIALLY ENFORCED** — ESLint bars infrastructure; a browser bundle check needs the frontend to consume it (OD-005) |
| **INV-023** | Public response contracts set no field that the operation does not require, and separate public from authenticated shapes as distinct types. | Verification is unauthenticated. One shape guarded by a boolean has to be checked correctly at every call site forever; two types cannot leak into each other. | **ENFORCED for verification** — `additionalProperties: false` plus tests asserting the public schema rejects owner-only fields |
| **INV-024** | Contracts contain no non-JSON values — no `Date`, `BigInt`, `Map`, `Set`, `Buffer`, or class instances. | A `Date`-typed field describes something the wire never carries; the receiver always gets a string. Making the string the contract removes the asymmetry. | **ENFORCED for verification** — round-trip test |
| **INV-025** | A serialized status value is an API contract. Changing one is a compatibility change, not a refactor. | `pending` → `awaiting` is invisible to TypeScript and breaks every stored record and client. | Documented → review |
| **INV-026** | Every API error response uses the canonical `ApiError` envelope from `@lagda/contracts/api`. | One shape, or every client writes a parser per endpoint. | **ENFORCED for the schema** — runtime schema + tests; route adoption needs BACKEND-11 |
| **INV-027** | Clients branch on `error.code`, never on `error.message`. HTTP mapping never inspects a message either. | Copy and localization change; `message.includes("not found")` is how status mapping silently breaks. | Documented → review |
| **INV-028** | Unknown properties in mutation request bodies are rejected. | Silently stripping hides stale clients and typos, and rejecting is the cheapest defence against mass assignment. | **ENFORCED** — `additionalProperties: false` + tests |
| **INV-029** | List sort keys are closed typed unions per endpoint, never free strings. | A free sort key reaching a repository becomes `ORDER BY ${input}`. | **ENFORCED** — `sortSchema()` + test rejecting `id; DROP TABLE documents` |
| **INV-030** | API timestamps are RFC 3339 UTC strings, never `Date` objects. | `JSON.stringify` produces a string, so a `Date`-typed field describes something the wire never carries. | **ENFORCED for extracted contracts** — pattern + tests |
| **INV-031** | Request and response contracts stay distinct types where server-owned or write-only fields exist. | Structural reuse is how `createdAt` becomes settable and a password reaches a response. | Documented → review |
| **INV-032** | Route handlers map domain results into declared response contracts. A persistence row is never serialized directly. | It is how persistence-only and secret fields leak. | Documented → BACKEND-11 |
| **INV-033** | Core never reads the current time. Time-dependent rules take `now` as a parameter. | A hidden clock read makes expiry logic untestable without stubbing globals, and non-reproducible across dates. | **ENFORCED** — purity test |
| **INV-034** | Core never generates identifiers, tokens, or randomness. | Identity comes from the application layer; a domain that invents IDs cannot be replayed. | **ENFORCED** — purity test |
| **INV-035** | Lifecycle state changes go through named actions. No generic `setStatus`/`setState`/`setWorkspaceId`. | A generic setter makes the transition table decorative — every invariant becomes bypassable in one line. | **ENFORCED** — purity test |
| **INV-036** | Terminal signing states never transition back to an active state. | A completed transaction is legally significant history. Corrections create a new transaction. | **ENFORCED** — every terminal state × every action |
| **INV-037** | The frontend does not depend on `@lagda/core`. | Core is backend-only. Shared needs belong in `@lagda/contracts`. | Documented → review |
| **INV-038** | Domain events and errors carry no infrastructure concerns — no HTTP status, log level, severity, queue name, or delivery detail. | Core must not decide how a failure is presented or recorded. | **ENFORCED for errors** — `DomainError` has no such fields |
| **INV-039** | Application use cases may not import concrete infrastructure, including LAGDA's own `@lagda/db`, `@lagda/storage` and `@lagda/sealing`. | Those packages implement application's ports; importing them inverts the dependency and creates a cycle. Only composition roots import both sides. | **ENFORCED** — ESLint |
| **INV-040** | Workspace-owned repository ports require workspace scope in their signature. | Tenant isolation must not depend on a caller remembering to filter. A method resolving a member from any workspace is a cross-tenant read waiting to happen. | **ENFORCED by port shape** — no unscoped lookup exists |
| **INV-041** | Application business time comes from `Clock`; generated entity IDs come from explicit generators. | Hidden `Date.now()` or `randomUUID()` makes a use case untestable and non-reproducible. | **ENFORCED** — tests assert both |
| **INV-042** | Application errors contain no HTTP status codes, headers, or log levels. | One use case must serve an HTTP route, a worker, and a future partner API. | **ENFORCED** — test asserts no status property |
| **INV-043** | Use cases receive resolved actor context, never raw HTTP, session, cookie, or token objects. | A worker must be able to invoke the same use case without fabricating a request. A token in an actor object reaches logs. | Documented → review |
| **INV-044** | A tenant-scoped lookup for another workspace's resource is indistinguishable from one that does not exist. | Distinguishing them confirms the existence of another tenant's data to anyone who can guess an ID. | **ENFORCED** — anti-enumeration test |
| **INV-045** | External side effects are not treated as durable simply because a transaction committed. | Publishing before commit describes state that may not exist; after commit without an outbox, the event is lost if the process dies. | Documented → BACKEND-06/16 |
| **INV-046** | Database access is confined to `@lagda/db`. Kysely, `pg` and row types never appear elsewhere. | The query layer is an implementation choice, not part of the domain. Application must survive replacing it. | **ENFORCED** — ESLint |
| **INV-047** | Workspace-owned relationships preserve tenant identity at the DATABASE level via compound keys where practical. RLS, if adopted, is defence in depth and never the only boundary. | A child FK referencing only a parent ID lets a row in workspace A reference a parent in workspace B, with nothing but application code preventing it. | **PARTIALLY ENFORCED** — the compound-key target exists; referencing tables arrive in BACKEND-07 |
| **INV-048** | Source-controlled migrations are the sole schema-change mechanism, and applied migrations are immutable. | Manual production changes are drift with no audit trail. Editing an applied migration makes history unreproducible. | **ENFORCED** — CI migrates from zero |
| **INV-049** | Business-significant timestamps are persisted explicitly from the application `Clock`, distinct from technical row timestamps. | Two clocks for one business event is a contradiction discovered later, in evidence. | Documented → review |
| **INV-050** | Uploaded document bytes are not stored in PostgreSQL. The database holds metadata, references and hashes. | Binaries in a transactional database wreck backup, replication and restore time. Changing it needs an ADR. | Documented → BACKEND-17 |
| **INV-051** | SQL values are always parameterized; dynamic identifiers come from code-level whitelists. | The shortest path from a query parameter to SQL injection. | Documented → review |
| **INV-052** | Database integration tests use real PostgreSQL, and must not mutate schema. | SQLite would not exercise timestamptz, compound constraints or SQLSTATE. TRUNCATE clears rows but not DDL, so a schema-mutating test leaks a broken schema into later runs. | **ENFORCED** — real PostgreSQL in CI |
| **INV-053** | Database error classification uses SQLSTATE, never message text. | `message.includes("duplicate key")` breaks on an upgrade or a non-English locale, silently. | **ENFORCED** — SQLSTATE helpers, tested |
| **INV-054** | Every workspace-owned repository method requires workspace scope AND the transaction context. No optional tenant parameter, no `skipTenantCheck`, no bypass flag. | Optional tenancy defeats the invariant, and a bypass flag becomes the security trap everyone reaches for. Reads take the transaction because RLS context is transaction-local. | **ENFORCED** — port shape + RLS |
| **INV-055** | Tenant context is transaction-local (`SET LOCAL`), established in exactly one place, and never session-level. | A session-level setting rides a pooled connection into the next request — a silent, intermittent, load-dependent cross-tenant read. | **ENFORCED** — leak tests incl. after rollback |
| **INV-056** | Missing tenant context fails CLOSED: no rows, not all rows. | If absent context meant unrestricted, every bug that loses context becomes a full cross-tenant read. | **ENFORCED** — tested |
| **INV-057** | The runtime database role owns no tenant tables and lacks `BYPASSRLS`; tenant tables use `FORCE ROW LEVEL SECURITY`. | An owner bypasses RLS unless forced. Testing as a superuser gives false confidence while production leaks. | **ENFORCED** — asserted before every RLS test |
| **INV-058** | RLS is defence in depth and never replaces explicit repository predicates. | A query relying on an invisible policy is one nobody can review by reading it. | **ENFORCED** — both layers tested separately |
| **INV-059** | `workspace_id` is immutable for tenant-owned resources; there is no generic reassignment. | Moving a row between tenants is a privileged audited operation, not an ordinary update. | **ENFORCED** — RLS `WITH CHECK` rejects it |
| **INV-060** | A cross-tenant lookup is indistinguishable from an absent resource. | Any difference confirms the resource exists elsewhere. | **ENFORCED** — application + DB |
| **INV-061** | Adding a workspace-owned table requires updating TENANCY_MODEL.md and TENANCY_TEST_MATRIX.md, with RLS, compound FK, tenant index and cross-tenant tests. | Tenancy applied to some tables and forgotten on others is the realistic failure mode. | Process → review |
| **INV-062** | Repository ports are declared by `@lagda/application`; `@lagda/db` implements them. No row or query-library type escapes `@lagda/db`. | An interface defined in `db` and imported by `application` inverts the dependency the architecture rests on. | **ENFORCED** — ESLint + package boundaries |
| **INV-063** | Tenant-owned repositories are obtained from a unit of work and bound to one workspace. Scope is never a method parameter. | A workspace parameter makes a cross-tenant call *typeable*; binding makes it unsayable. A control depending on a runtime policy to catch a compile-time-expressible error is weaker than one where the error cannot be written. | **ENFORCED** — port shape |
| **INV-064** | A repository refuses to persist a record whose workspace differs from its scope, and never rewrites it to match. | Silently rewriting turns a programmer error into a data-corruption event, moving a record between tenants. | **ENFORCED** — contract suite, both adapters |
| **INV-065** | Every repository in one unit of work shares the same database transaction. | Independently built repositories can hold the pool while another holds the transaction — atomicity that looks correct and is not. | **ENFORCED** — rollback test against PostgreSQL |
| **INV-066** | Persistence errors are classified by SQLSTATE and constraint name, never message text; unknown errors pass through untranslated. | Message parsing breaks on an upgrade or locale. Downgrading an unknown error to "conflict" or "not found" tells a caller data is absent when the database is unreachable. | **ENFORCED** — tested |
| **INV-067** | State-sensitive writes use conditional updates rather than read-then-write. | Two concurrent readers of the same value both write, and the second silently overwrites the first. | **ENFORCED** — stale-writer test |
| **INV-068** | Repositories persist and retrieve only. No email, queue, webhook, sealing or audit side effects. | Hidden side effects make every caller's behaviour unpredictable. | Documented → review |
| **INV-069** | The repository behavioural contract runs against both the in-memory fake and PostgreSQL. | Divergence means either the fake is lying to application tests or the adapter is wrong. Only the PostgreSQL run is security proof. | **ENFORCED** — both suites |
| **INV-070** | `DocumentSealer` exposes exactly ONE operation, `seal()`. Merging, hashing and certificate rendering stay private to `@lagda/sealing`. | Every extra method is a call site a future remote signing service must reproduce. A seam becomes a rewrite when twenty callers each learned a different piece of it. | **ENFORCED** — method count asserted; probed |
| **INV-071** | `DocumentSealer` is declared in exactly one file, and no second copy exists in any package. | Two definitions typecheck, drift, and give the two layers different contracts. | **ENFORCED** — exact-path assertion |
| **INV-072** | Document bytes cross the seam as `Uint8Array`, never Node's `Buffer`. | `Buffer` is Node-only; a signing service in another runtime could not satisfy a contract demanding one. | **ENFORCED** — asserted on comment-stripped source; probed |
| **INV-073** | The final artifact's digest is computed AFTER every byte-changing step, from the exact bytes returned. | A digest taken from an intermediate buffer fails against the document people actually hold, and fails silently — both values look like valid digests. | **ENFORCED** — compared against an independently computed digest |
| **INV-074** | Hashes are named for the artifact they identify (`preparedDocumentHash`, `signedDocumentHash`). Never one field or column called `hash`. | An ambiguous name survives exactly as long as one person remembers which artifact it meant. | Documented → review (see OD-022: `Sha256Digest` is unbranded, so the two are mutually assignable) |
| **INV-075** | The sealer never mutates the caller's document buffer. | pdf-lib may write through the array it is given; the caller still needs those bytes to match the recorded digest. | **ENFORCED** — byte-equality test |
| **INV-076** | Every sealed artifact records `sealScheme`, `sealVersion` and `digestAlgorithm` from the first record written. | Retrofitting is impossible — the information is gone. A second scheme makes every earlier artifact ambiguous. | **ENFORCED** — exact metadata assertion |
| **INV-077** | The completion certificate is a SEPARATE artifact, never appended to the sealed document. | Appending changes the signed document's page count, so the file delivered is no longer the file participants signed. Handoff §15 stores them separately. | **ENFORCED** — page-count and inequality tests |
| **INV-078** | The sealer reads no clock, no randomness and no environment. `sealedAt` and `verificationId` are inputs. | A hidden clock makes output non-reproducible, and a sealer that mints verification IDs hands LAGDA's identifier namespace to a future third-party service. | **ENFORCED** — source scan on comment-stripped source |
| **INV-079** | No pdf-lib error, message or type reaches a caller. Failures are LAGDA-owned types with a stable `code` and an explicit `retryable`. | A caller that must understand pdf-lib's exceptions could not be satisfied by a Java or .NET signer. `retryable` is what stops the pipeline retrying a permanently malformed document forever. | **ENFORCED** — tested, including the raw `TypeError` path |
| **INV-080** | SHA-256 hashing happens in exactly one file. | Several implementations across layers is how one ends up base64 and another hex, and a verification comparison silently never matches. | **ENFORCED** — exact-path assertion on `node:crypto` imports |
| **INV-081** | The PDF coordinate flip happens in exactly one function. | A miscomputed rectangle does not crash — it places a signature in the wrong half of a structurally valid page. | **ENFORCED** — three boundary tests on `toPdfRect` |
| **INV-082** | Sealing is never invoked inside a database transaction. | It is slow, external, and cannot be rolled back when the commit later fails. | Documented → review (no consumer exists yet) |
| **INV-083** | Every architectural boundary test is verified by deliberately violating it, including a negative control proving the permitted case still passes. | A boundary test that cannot fail proves nothing. This command's probes exposed two dead detectors and four dead lint rules. | **ENFORCED** — by process; probe results recorded in SEALING_TEST_MATRIX.md |
| **INV-084** | Signing evidence is append-only. The runtime database role holds INSERT and SELECT on evidence tables and is explicitly revoked UPDATE and DELETE. | Handoff §32 requires an append-only store. Repository discipline alone is one careless method from being untrue. | **ENFORCED** — `has_table_privilege` asserted, with a negative control; probed by granting UPDATE |
| **INV-085** | Evidence immutability is an OPERATIONAL control and must never be described as cryptographic non-repudiation. | It does not prove a database administrator could not alter a row. Overstating a security property is worse than not having it. | Documented → review |
| **INV-086** | Every evidence, artifact, seal and verification row carries `workspace_id` as a first-class column. Tenancy is never inferred through a join. | Evidence is the most sensitive data LAGDA holds, and a join is one refactor from being an outer join that leaks. | **ENFORCED** — schema + RLS + tests |
| **INV-087** | An evidence event type is a historical FACT, never a status. | `document-viewed` is permanent; the recipient's status may still be awaiting-signature. Conflating them makes the timeline and the state machine fight. | **ENFORCED** — no unique constraint on (request, type); repeat events tested |
| **INV-088** | A recipient actor is not a `UserId`. Actor identity is a discriminated union, and a `system` actor carries no ID. | External signers have no LAGDA account. A synthetic user for the expiry worker makes "who did this" unanswerable for every automated action. | **ENFORCED** — DB CHECK + union type |
| **INV-089** | `occurred_at` comes from the application `Clock`; `recorded_at` is stamped by the database and is not application-settable. | "When this row landed" is not something a caller can honestly claim to know, and a client-supplied occurrence time is not evidence. | **ENFORCED** — tested with a past `occurred_at` |
| **INV-090** | Evidence timelines are ordered by `(occurred_at, evidence_event_id)`. A timestamp alone is not a total order. | Two recipients can act in the same millisecond, leaving the planner free to choose. Insertion order is never a timeline. | **ENFORCED** — tie test; probed by dropping the tiebreak |
| **INV-091** | One artifact row per byte-distinct artifact. A digest, size, storage reference or type is never overwritten. | An overwritten digest destroys the only evidence that the earlier bytes existed. | **ENFORCED** — no UPDATE privilege |
| **INV-092** | Artifact digests are constrained by an ALGORITHM-AWARE check, never a bare length rule. | A fixed 64-character check must be *dropped* to add SHA-512, and dropping a CHECK is how historical rows stop being validated. | **ENFORCED** — malformed and uppercase digests rejected |
| **INV-093** | No `UNIQUE` on an artifact digest. | Two identical PDFs legitimately share a SHA-256; the digest is content identity, not row identity. | **ENFORCED** — duplicate-digest test |
| **INV-094** | Artifact provenance is a compound-FK relation, tenant-safe, and an artifact is never derived from itself. | Ancestry inferred from naming conventions is not ancestry. | **ENFORCED** — cross-tenant and self-source rejected |
| **INV-095** | One finalization per signing request, enforced on BOTH `document_seals` and `verification_records`. | Resealing is not a product feature; a completion retry must converge rather than mint a competing verification identity. | **ENFORCED** — each constraint isolated by raw SQL and probed independently |
| **INV-096** | `seal_version` is constrained `> 0`, never to the current version. | Pinning it to 1 makes introducing version 2 a schema change under load. | **ENFORCED** — version 7 accepted, 0 rejected |
| **INV-097** | An unknown seal scheme is rejected at write time, never silently read back as the current scheme. | A misinterpreted historical artifact is worse than a rejected write. | **ENFORCED** — CHECK constraint tested |
| **INV-098** | A verification hash has exactly ONE writable home. Denormalized copies are forbidden. | Two independently writable copies drift on the first partial write, and the verification page and seal record would disagree about what was signed. | **ENFORCED** — schema; verification reaches hashes through `seal_id` |
| **INV-099** | Public verification exposes an explicit ALLOWLIST of named columns, never a filtered row type. | `Omit<Row, "secret">` silently starts exposing whatever a later command adds. | **ENFORCED** — exact key set asserted; probed by leaking `workspace_id` |
| **INV-100** | The public verification lookup returns `null` for absent AND restricted, indistinguishably. | A caller that could tell them apart would have an oracle for which verification IDs exist. | **ENFORCED** — tested |
| **INV-101** | A verification ID is not authentication. It never grants document or signing access. | It is published by design; treating a public value as a credential is how a verification page becomes a document leak. | Documented → review (BACKEND-42) |
| **INV-102** | Evidence relations are `ON DELETE RESTRICT`. Signing evidence never cascades away with a parent. | Evidence must survive the deletion of the document it describes. Retention and erasure are policy (BACKEND-55), not a cascade. | **ENFORCED** — schema |
| **INV-103** | Evidence rows carry no binary content and no secrets. `details` is bounded, versioned, and validated per event type. | Evidence is not a document store, and a payload with no cap quietly becomes one. | **ENFORCED** — 8 KiB CHECK; typed payload |
| **INV-104** | Evidence is created by the SERVER from the authenticated actor, the server clock, observed request context and authoritative state — never from a client-supplied event payload. | A client that can name its own event type, timestamp and IP is describing itself, not being observed. | Documented → BACKEND-11/36 must enforce at the route boundary |
| **INV-105** | Client IP is recorded only from server-observed connection context after trusted proxy configuration exists. | An `X-Forwarded-For` recorded before then is attacker-controlled text wearing the costume of evidence. | **ENFORCED** — column exists, nothing writes it; BACKEND-11/56 owns the writer |
| **INV-106** | HTTP frameworks are confined to `@lagda/api`. Application, core, contracts and db never import Fastify. | A use case that imports Fastify cannot be called by the worker, and a domain rule that knows about a Reply cannot be tested without one. | **ENFORCED** — architecture test with a negative control; probed |
| **INV-107** | Importing `@lagda/api` starts no listener. Server start is an explicit `startServer()` call. | An import that binds a port does so in every test run and in any tool that merely typechecks the package. | **ENFORCED** — no call at statement position in the entry point; probed |
| **INV-108** | Route modules may not import `@lagda/db`, `kysely`, `pg`, `@lagda/sealing` or `@lagda/storage`. The composition root may. | A route able to reach the query builder can issue an unscoped query, bypassing the unit of work and the tenant context it carries. | **ENFORCED** — directory-scoped ESLint; 9 probes including 3 negative controls |
| **INV-109** | Every route declares a response schema, and Fastify serializes through it. | Response schemas are a security boundary, not documentation: an undeclared field cannot escape. TypeScript cannot provide this. | **ENFORCED** — leaky-handler test; schema-count architecture test |
| **INV-110** | Unknown properties in request bodies are REJECTED, never stripped. | Stripping hides stale clients and typos; rejecting is also the cheapest defence against mass assignment. | **ENFORCED** — `removeAdditional: false`; probed by flipping it |
| **INV-111** | Framework and validator errors never reach a client. Every failure passes through one mapper. | A public contract that depends on Ajv's internals makes a validator upgrade a breaking API change. | **ENFORCED** — no `instancePath`/`schemaPath`/`ajv` in responses |
| **INV-112** | Unexpected errors return a generic body with no stack, SQL, path or library message. Detail goes to the log with the same request ID. | The envelope has no field to put a secret in; the request ID is what lets support find the line that does. | **ENFORCED** — credential-leak test; probed by returning `error.message` |
| **INV-113** | Every request carries one server-generated request ID, on every response including 404s, in the error body, and in every log line. | Support cannot correlate a reported failure if the failing response is the one without an ID. | **ENFORCED** — tested on success, 404 and 500 |
| **INV-114** | A client-supplied request ID is ignored. | It flows into logs, and a client-chosen value can carry CRLF into a response header. | **ENFORCED** — `requestIdHeader: false`; CRLF injection tested; probed |
| **INV-115** | Forwarded client IP is trusted only under explicit proxy configuration. `TRUST_PROXY=true` is rejected outright. | `true` trusts the whole chain, so any client could choose the IP recorded as signing evidence. | **ENFORCED** — config rejects it; spoofing tested both ways; probed |
| **INV-116** | Observed request metadata carries explicit provenance and is never read from a request body. | A client that can name its own IP is describing itself, not being observed. | **ENFORCED** — `IpProvenance` on the type; INV-104 governs the writer |
| **INV-117** | CORS origins are matched by exact equality. No wildcard, no substring, and never a wildcard with credentials. | `origin.includes("lagda.io")` admits `lagda.io.attacker.example`. | **ENFORCED** — lookalike origins tested; probed by substituting a substring check |
| **INV-118** | API startup never applies database migrations. | Every replica would race to alter the schema during a rolling deploy. | **ENFORCED** — architecture test on migration symbols |
| **INV-119** | Health and readiness are separate. Liveness never touches the database; readiness signals through the status code. | Restarting the API does not fix the database. A 200 carrying `not-ready` means every probe passes forever. | **ENFORCED** — both tested, including the database-down case |
| **INV-120** | Unauthenticated infrastructure routes disclose no environment, version, hostname or dependency detail. | Each helps fingerprint a deployment and none helps an orchestrator decide anything. | **ENFORCED** — health body key set asserted; readiness failure leak-tested |
| **INV-121** | Request and response bodies are never logged. Sensitive headers are removed, not masked. | Bodies carry passwords, OTPs, signature images and document content, and "redact the known keys" fails the moment a new key appears. | **ENFORCED** — explicit serializer allowlists; no body serializer exists |
| **INV-122** | Shutdown is idempotent and bounded, closes HTTP before the database, and continues past a failing target. | SIGTERM then SIGINT is ordinary; closing the pool first fails in-flight requests that were about to succeed. | **ENFORCED** — three unit tests |
| **INV-123** | API and worker are separate process roles. The API never starts a worker or a queue. | Coupling them means one cannot be scaled, deployed or restarted without the other. | Documented → review (worker not yet implemented) |
| **INV-124** | Operational logs are not signing evidence, and an evidence requirement is never satisfied by logging it. | Logs rotate, get sampled and may be shipped to a third party. Evidence is append-only with its own retention. | **ENFORCED** - no log/metric/telemetry table may be created; schema asserted |
| **INV-125** | Signing evidence never carries stack traces, SQLSTATE, retry counts or process diagnostics. | The reverse of INV-124. Debug detail is not business history. | Documented -> review |
| **INV-126** | `core` and `application` import no logging or metrics library. | A domain rule that logs cannot be tested without a logger; a use case taking one cannot be called by the worker. | **ENFORCED** - architecture test; probed |
| **INV-127** | Secrets, credentials, OTPs, signature content and document bytes are never logged, at any level including `trace`. | A lower level is not an exemption. | **ENFORCED** - capture tests per category; probed |
| **INV-128** | Request and response bodies are never logged by default. | A body may carry a password, an OTP, a signature image or field values, and "redact the known keys" fails on the first new key. | **ENFORCED** - no body serializer exists |
| **INV-129** | Redaction covers any depth and any key name, and scrubs error messages. | Pino path redaction misses top-level keys, depth beyond one level, and messages entirely - all three were proven to leak. | **ENFORCED** - deep walk + message hook; 7 tests fail when disabled |
| **INV-130** | Redaction must not over-match diagnostic fields. `code`, `errorCode`, `statusCode` and similar must survive. | Over-broad redaction destroys the logs it was meant to protect, and `errorCode` is what alerts branch on. | **ENFORCED** - asserted for nine field names |
| **INV-131** | Logs prefer stable resource IDs over PII-rich display values. | An ID is stable, less PII-rich, and unchanged when a customer renames something. A document title can reveal the legal matter. | Documented -> review |
| **INV-132** | Metric labels contain no unbounded identifier - `requestId`, `workspaceId`, `userId`, `documentId`, `signingRequestId`, `verificationId`, `email`, `ipAddress`. | One series per tenant per resource is a cardinality explosion, and metrics should carry no PII at all. | **ENFORCED** - catalog audit test; probed |
| **INV-133** | Metric names are a closed union. Dynamic names are impossible. | A per-tenant metric name is unbounded cardinality expressed as a name; a string parameter cannot prevent it. | **ENFORCED** - type system |
| **INV-134** | Application use cases stay observability-provider independent. Instrumentation wraps from the outside. | A logger parameter makes every caller supply one, including the worker. | **ENFORCED** - no use-case signature takes a logger |
| **INV-135** | Observability context is never the source of authorization or tenant enforcement. | If the store vanished, no query may change behaviour and no permission may change answer - only the logs get less useful. | **ENFORCED** - no `@lagda/db` file reads it; consumers are an exact named set |
| **INV-136** | Database telemetry emits no SQL parameters. Repository and method names identify an operation. | Parameters carry customer data; `repository`/`operation` answers the same question. | Documented -> review (no repository wrapped yet) |
| **INV-137** | Sealing telemetry exposes no PDF bytes, field values or signature content. | Duration, sizes, scheme and category answer every operational question without content. | Documented -> review (no sealing caller yet) |
| **INV-138** | Unexpected errors are operationally visible while public responses stay sanitized. | The request ID connects the safe body to the log line that has the stack. | **ENFORCED** - asserted from both sides |
| **INV-139** | Observability never changes a transaction boundary, and never writes telemetry to PostgreSQL on the request path. | It risks recursion during the outage it exists to explain. | **ENFORCED** - no telemetry table may exist |
| **INV-140** | An observability wrapper returns the original result and rethrows the original error, unchanged. | A wrapper that swallows a failure to log it converts an outage into a silent wrong answer. | **ENFORCED** - identity assertion; probed |
| **INV-141** | Logs, metrics and evidence keep separate semantic and retention models. | Three different retention decisions; conflating them means the strictest wins or, in practice, the loosest. | Documented -> BACKEND-55/66 |
| **INV-142** | Every process role emits structured records. No `console.*` in runtime code. | An unstructured line is invisible to aggregation, and migration failures are what people search for at 3am. | **ENFORCED** - architecture test across all packages and the migration runner |
| **INV-143** | Browser authentication uses opaque server-managed sessions, never a JavaScript-readable bearer credential. | A token JavaScript can read is a token XSS can exfiltrate, and a stateless token cannot be revoked. | **ENFORCED** - cookie policy + tests; no JWT dependency exists |
| **INV-144** | Raw session and CSRF tokens are never persisted. Only a domain-separated SHA-256 digest is stored. | A database dump must not yield working credentials. | **ENFORCED** - CHECK constraint rejects non-hex; integration test attempts a raw insert |
| **INV-145** | Session and CSRF digests are domain-separated. | Without it, the readable CSRF token could be submitted as a session cookie and match a stored session hash. | **ENFORCED** - tested; probed by removing the prefix |
| **INV-146** | Session cookies are HttpOnly always, and Secure in production. | HttpOnly is the control that stops XSS stealing the session. Production start fails if Secure is disabled. | **ENFORCED** - config rejects it; probed |
| **INV-147** | Session credentials never appear in a URL, a JSON body, a log or a metric. | URLs leak through history, referrers and logs. | **ENFORCED** - log-capture test; no route returns a token |
| **INV-148** | A session identifies a USER and confers no workspace authorization. | A user belongs to several workspaces, and a credential carrying authorization carries STALE authorization. | **ENFORCED** - actor key set asserted; no workspace column, asserted against the live schema |
| **INV-149** | Session rotation issues a fresh credential and invalidates the previous one. | The defence against session fixation. | **ENFORCED** - tested |
| **INV-150** | State-changing requests authenticated by cookie require a CSRF token bound to that session. | A token that merely matches a cookie is controlled by anyone who can set a cookie. | **ENFORCED** - cross-session test; probed |
| **INV-151** | CSRF tokens are cryptographically unpredictable and compared timing-safely. | String equality short-circuits and leaks how much of a guess was correct. | **ENFORCED** - timingSafeEqual; format tested |
| **INV-152** | GET and HEAD perform no business mutation, which is what makes their CSRF exemption safe. | A GET logout endpoint silently defeats the whole mechanism. | Documented -> review (no product mutation routes exist yet) |
| **INV-153** | A session-store failure is reported as a dependency failure, never as invalid authentication. | A mass logout during a database outage trains users to re-enter credentials exactly when something is wrong. | **ENFORCED** - resolver propagates; HTTP test asserts not-401 |
| **INV-154** | Every session rejection reason collapses to one public error. | Distinguishing expired from unknown is an oracle for which tokens exist. | **ENFORCED** - tested |
| **INV-155** | CORS and SameSite are defence in depth and never replace CSRF. | A cross-origin POST is still sent; CORS only governs reading the reply. | Documented -> review |
| **INV-156** | External recipient signing authentication is a separate model and never reuses the user session cookie. | An external signer is not an account holder; overloading the cookie would hand them a user session. | Documented -> BACKEND-34 |
| **INV-157** | The session table is GLOBAL_AUTHENTICATION - no workspace column, no RLS - and that is deliberate, not an omission. | Authentication precedes any workspace context, so a tenant policy could never be satisfied when it is needed. | **ENFORCED** - asserted against the live schema |
| **INV-158** | No development authentication bypass exists. | A security boundary that is real only in production is not a boundary. | **ENFORCED** - no environment-conditional auth path; fixtures are test-only |
| **INV-159** | Retry-sensitive operations use durable PostgreSQL-backed idempotency. No in-memory or Redis state is required for correctness. | A Map in one process protects nothing across instances or restarts. | **ENFORCED** - PostgreSQL is the only store; real two-transaction concurrency test |
| **INV-160** | Request IDs and idempotency keys are distinct. A request ID is never used as an idempotency key. | Request ID is new every attempt; an idempotency key is deliberately the same across retries. | **ENFORCED** - separate types; request ID excluded from the fingerprint |
| **INV-161** | An idempotency key authorizes nothing. Authentication, CSRF and authorization all still apply, including on replay. | The record is not a capability. A user who has lost access must not replay protected data. | Documented -> feature routes must order auth before replay |
| **INV-162** | Idempotency identity always includes scope and operation, never the key alone. | A raw key is not globally unique - two clients legitimately send the same UUID. | **ENFORCED** - unique constraint on all four parts; no repository method queries by key alone |
| **INV-163** | The same key with a different canonical fingerprint fails. It neither executes nor replays. | Replaying the first result for different input is a wrong answer, not a retry. | **ENFORCED** - tested; probed |
| **INV-164** | Fingerprints exclude session tokens, CSRF tokens, request IDs, IPs and user agents. | A session rotation between retries must not make the second look like a different business request. | **ENFORCED** - fingerprint takes the validated logical request only |
| **INV-165** | Canonicalization sorts object keys and PRESERVES array order. | Sorting arrays would make [A,B] and [B,A] recipient orders replay each other. | **ENFORCED** - tested; probed by sorting |
| **INV-166** | Concurrent duplicate claims are resolved atomically by PostgreSQL. No check-then-insert. | Two processes both see nothing and both proceed. | **ENFORCED** - single conditional INSERT; probed by removing ON CONFLICT |
| **INV-167** | Business mutation and idempotency completion commit atomically for PostgreSQL-contained operations. | Otherwise a crash leaves a mutation with no claim, or a claim with no mutation. | **ENFORCED** - real transaction test with a second table |
| **INV-168** | Transient failures and unexpected 5xx are never stored as completed results. | A defect or outage must be retryable, not cached as truth. | **ENFORCED** - rollback removes the claim; no try/catch around execution |
| **INV-169** | Raw idempotency keys and original request bodies are never persisted. | Lookup needs only a digest, and conflict detection needs only a fingerprint. | **ENFORCED** - schema has no such column, asserted against the live database |
| **INV-170** | Raw idempotency keys are never logged and never used as metric labels. | Client-supplied, unbounded, and identifying. | **ENFORCED** - redaction covers the header; catalog audit covers labels |
| **INV-171** | Stored replay results are bounded and contain no headers - no cookies, no CORS, no security headers, no request ID. | Replaying a stored Set-Cookie would re-issue an old session; a replay must get its own request ID. | **ENFORCED** - stored shape has no header field; 64 KiB bound tested |
| **INV-172** | Idempotency does not imply exactly-once external delivery. | An email or billing provider may still receive a retry. Provider adapters need their own keys. | Documented -> review |
| **INV-173** | Rate limiting is defence in depth and never substitutes for authentication, authorization, tenancy, CSRF or idempotency. | A limit answers "too much, too fast" and nothing about who you are. | Documented -> review |
| **INV-174** | Security-sensitive limits use durable shared state, never process memory. | With N instances, a local counter turns "5 per minute" into "5N per minute". | **ENFORCED** - PostgreSQL only; real concurrency test |
| **INV-175** | Concurrent increments are atomic. No read-then-write. | Two callers receiving the same count let one extra request through, and that one is the attacker. | **ENFORCED** - single upsert; probed by replacing it |
| **INV-176** | IP scope comes from proxy-aware observed request metadata. Client-supplied headers never select a bucket. | Otherwise an attacker picks a fresh bucket per request and the limiter is decorative. | **ENFORCED** - spoofing tested both ways |
| **INV-177** | Rate-limit scope identities come from trusted context - resolved session, authorized workspace, validated signing access - never a request body. | A body field lets an attacker choose which bucket to fill. | **ENFORCED** - typed scopes; no body-derived construction exists |
| **INV-178** | Personal-data scope keys are digested before storage. | An IP or an email is personal data the counter table only compares, so it need not hold it reversibly. | **ENFORCED** - tested; probed by storing raw |
| **INV-179** | Every threshold records its source. | A number nobody chose is one nobody can defend when it blocks a customer. | **ENFORCED** - validation rejects an empty source |
| **INV-180** | A limit below 1 is rejected at startup. | A limit of 0 silently disables the operation it protects. | **ENFORCED** - tested |
| **INV-181** | Rate limiting runs BEFORE idempotency and before the business transaction. | Everything after costs something an attacker would otherwise get free. | **ENFORCED** - a rate-limited request never claims an idempotency key |
| **INV-182** | An idempotency replay is still counted. | Exempting replays makes the key a rate-limit bypass. | **ENFORCED** - tested |
| **INV-183** | Rate-limited responses are canonical 429s carrying no policy, count, remaining or scope. | "2 attempts left" is a gift to an attacker; a policy name tells them which dimension to rotate. | **ENFORCED** - leak test |
| **INV-184** | A limiter store failure is reported as a dependency failure, never as a rate limit. | A caller must not be told "slow down" when the truth is "the control is broken". | **ENFORCED** - distinct error, maps to 503 |
| **INV-185** | Pre-auth limits apply whether or not the account exists. No permanent lockout. | An "unknown account is unlimited" path enumerates accounts; a permanent lockout is a denial-of-service weapon. | Documented -> BACKEND-20/22/23 |
| **INV-186** | Rate-limit metrics and logs contain no IP, account, digest, count or unbounded identifier. | Metrics are the surface that should carry no personal data at all. | **ENFORCED** - catalog audit; log capture test |
| **INV-187** | Counters have bounded operational retention and are not a request log or signing evidence. | One row per window, not per request. | **ENFORCED** - schema; bounded cleanup tested |
| **INV-188** | Rate limiting does not claim exactly-once execution or network-layer DDoS protection. | An application counting requests is already past where a volumetric flood does its damage. | Documented -> review |
| **INV-189** | A new public or credential-guarding operation requires an explicit rate-limit policy. | The absence of a policy is the absence of a control. | Documented -> feature commands |

## Rules for future `BACKEND-XX` commands

1. Read this file and the applicable ADRs before changing backend code.
2. Preserve package boundaries.
3. Report — do not silently proceed — if the requested task would violate an
   invariant.
4. Modify an invariant only when explicitly instructed, and record the change in
   an ADR.
5. Add or update tests for any architectural behaviour touched.
6. Avoid opportunistic unrelated refactors.
7. When an invariant's enforcement column says "Documentation", and the command
   creates the package that governs it, **make it executable in that command**.

## Worker and background jobs (BACKEND-16)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-190** | API and worker are separate process roles. The API may not import the queue driver or `@lagda/worker`; the worker may not import a web framework or `@lagda/api`. | The two scale, restart and fail for different reasons. An API process able to call `boss.work` can start a consumer inside a web process, and then neither can be restarted alone. | **ENFORCED** - ESLint, probed in both directions with negative controls |
| **INV-191** | A job enqueued alongside a business write joins that write's transaction. | Otherwise a job survives a rolled-back mutation and the worker acts on state that never existed, or a committed write loses its follow-up. Both are silent. | **ENFORCED** - commit and rollback integration tests; probed by removing the `db:` override |
| **INV-192** | Job payloads carry identifiers, never resource content, personal data or credentials, and are bounded at 16 KiB. | A payload is a durable database row that appears in backups. It also goes stale: a name copied at enqueue is wrong when a delayed job runs. | **ENFORCED** - size refused at enqueue and probed; content rule documented |
| **INV-193** | Every job declares a bounded retry policy, and the bound is applied to the queue as well as to each send. | Nothing retries forever. Recurring jobs are sent by pg-boss's own scheduler and never pass through LAGDA's scheduler, so a per-send bound would not reach them. | **ENFORCED** - queue row asserted; registry test requires a bound on every definition |
| **INV-194** | Job payloads are validated at execution time. Malformed input is terminal, not retried. | A queue holds rows written by a previous deployment, and possibly by an operator by hand. Retrying a malformed payload burns the attempt budget and delays the real signal. | **ENFORCED** - five malformed shapes rejected; probed by skipping validation |
| **INV-195** | A handler failure is logged and rethrown. It is never converted into success. | Catching and returning normally reports success to the queue and silently discards the work - the failure mode this layer exists to prevent. | **ENFORCED** - test through the real registration path; probed by swallowing |
| **INV-196** | Every job definition declares how a duplicate delivery stays safe. | A worker can crash between finishing work and marking a job complete. Duplicate delivery is inherent to a durable queue, not a defect. | **ENFORCED** - required field, registry test; the strategy itself is prose |
| **INV-197** | A job type name is a persistence contract and is never renamed. | Renaming strands every row already enqueued under the old name. They are never consumed and never fail - they simply sit there. | Documented -> review; closed union makes addition cheap |
| **INV-198** | Job logs carry the result shape, never `job.data`. | A payload may carry resource identifiers, and a full dump is how they reach log aggregation. | **ENFORCED** - the wrapper spreads only an object result |
| **INV-199** | The worker runs no migrations. | Migration is an explicit deployment step. A worker that migrated on boot would race every API replica during a rolling deploy. | **ENFORCED** - no migrator in the worker; boot verified |
| **INV-200** | Recurring work is registered through the queue's schedule table, not a process timer, and always in UTC. | `setInterval` disappears on restart and duplicates across instances. A server-local cron shifts silently with the deployment timezone. | **ENFORCED** - idempotent-registration test; `tz: "UTC"` asserted in the schedule row |
| **INV-201** | Time-dependent horizons are read at execution time, not baked in at enqueue. | A job delayed by an outage would otherwise delete using a stale cutoff. | **ENFORCED** - `before` deliberately absent from the payload schema |
| **INV-202** | Tenant scope is an explicit discriminant on every job, never an optional workspace id where `undefined` means global. | That shape makes the most dangerous value the easiest to produce by accident. | **ENFORCED** - closed union; non-optional `workspaceId` on the workspace context |

## Object storage (BACKEND-17)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-203** | Provider SDK types never appear outside `packages/storage`. Application, core and contracts work with the `ObjectStorage` port. | A vendor type in the application layer spreads to every caller and makes the provider unreplaceable. The production provider is not yet chosen. | **ENFORCED** - ESLint names + wildcard, probed in 7 packages with 4 negative controls |
| **INV-204** | The binary abstraction is `AsyncIterable<Uint8Array>`, chosen once. | A Node `Readable` or an SDK stream wrapper in a port signature is a provider type by another name. | **ENFORCED** - the port compiles against no Node or SDK stream type |
| **INV-205** | Object keys are DERIVED from authorized identifiers and never accepted from a client. Accepted artifacts are never silently overwritten. | The whole tenancy argument for storage rests on the key being unreachable. Overwriting destroys the evidence that earlier bytes existed. | **ENFORCED** - branded type with one validating constructor; overwrite refused and probed |
| **INV-206** | Provider ETag is never treated as LAGDA's SHA-256. | ETag is an MD5, or a digest-of-digests for multipart, or provider-specific. Treating it as the artifact digest would make integrity checks pass on wrong bytes. | **ENFORCED** - typed as `providerEntityTag`; test asserts inequality and length difference |
| **INV-207** | Storage references are persisted as internal keys, never as URLs or presigned URLs. Presigned URLs are bearer credentials: never logged, persisted or queued. | A presigned URL expires and grants access to whoever holds it; a permanent URL implies a public bucket. | **ENFORCED** - branded key column; leak test; redactor scrubs signatures. Signed URLs not implemented |
| **INV-208** | Zones are a closed union. The application never names a bucket. | An arbitrary bucket string lets a typo write documents somewhere with no access policy. | **ENFORCED** - `StorageZone` union; bucket mapping only in the adapter |
| **INV-209** | Customer filenames never appear in object keys or provider metadata. | Provider logs, consoles and billing exports are outside LAGDA's control, and a filename can name the subject of a legal document. | **ENFORCED** - keys built from identifiers only; probed by injecting a filename |
| **INV-210** | Provider errors are translated into LAGDA categories by structure, never by message text. | Provider wording differs per implementation and changes without notice; a substring match is a detector that silently stops detecting. | **ENFORCED** - mapped by name, code and status; induced against a real service |
| **INV-211** | Production object-storage traffic uses TLS with validated certificates. | Documents and signing evidence travel this connection. | **ENFORCED** - plaintext endpoint refused; the escape hatch is itself refused in production |
| **INV-212** | Storage credentials are externalized and never logged. The loggable projection is an allowlist. | A denylist leaks the first field added without thinking, and the config object holds a secret key. | **ENFORCED** - allowlist projection; probed by adding the secret |
| **INV-213** | Storage adapters do not query PostgreSQL, write evidence, enqueue jobs or modify PDFs. | Each would couple a replaceable adapter to something that must outlive it - and a storage layer that rewrites a PDF invalidates every signature. | **ENFORCED** - import bans; byte-exactness tested by digest |
| **INV-214** | Object storage performs no authorization. Tenant access is resolved before a storage call, and a workspace prefix is organisation only. | Anyone holding a key can read the object. What prevents cross-tenant access is that the key is never reachable. | **ENFORCED** - cross-tenant test proves B obtains no reference |
| **INV-215** | Object storage and PostgreSQL are not transactionally atomic, and no code claims they are. | Believing otherwise is how an artifact row ends up pointing at bytes that were never written. | Documented -> BACKEND-18/38 own the choreography |
| **INV-216** | Document buckets are private. No public-read ACL, no permanent public URL. | Every document is a contract, an affidavit or an identity record. | **PARTIALLY ENFORCED** - no ACL in code (audited); bucket-level public-access block is a deployment requirement |

## Secure upload (BACKEND-18)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-217** | Every client-supplied file is untrusted until inspection AND malware scanning both pass. The pipeline fails closed at every branch. | A workspace owner can upload malware by accident as easily as an attacker can on purpose. Role, filename and prior uploads change nothing. | **ENFORCED** - ordering test; probed by making failure mean clean (4 tests fail) |
| **INV-218** | Untrusted bytes enter quarantine before accepted-artifact storage, and a quarantine object is never an accepted artifact. | Bytes in the immutable zone before validation is the whole failure this boundary exists to prevent. | **ENFORCED** - order asserted; probed by writing to the artifact zone first (13 tests fail) |
| **INV-219** | Client filename, extension, media type, declared size and any client hash are never authoritative. Type is DETECTED from content and confirmed by a real parser. | All five are attacker-chosen strings. | **ENFORCED** - detection + parse; probed by trusting the client type (3 tests fail) |
| **INV-220** | Content inspection and malware scanning are separate controls; neither substitutes for the other. | A structurally perfect PDF can carry a dropper; a corrupt file is unusable whether or not it is infected. | **ENFORCED** - a valid PDF containing EICAR passes inspection and is caught only by the scanner |
| **INV-221** | Malware scanning is mandatory for acceptance and fails closed. Scanner unavailability is NEVER interpreted as clean. | An outage that silently disabled malware protection would be worse than having none, because everyone would believe it was on. | **ENFORCED** - real ClamAV; unreachable-scanner test; probed in two places |
| **INV-222** | An unscannable file is never accepted. A file exceeding the scanner's own limit is refused, not skipped. | Otherwise the largest files - the interesting ones - would be the unscanned ones. | **ENFORCED** - refused before sending; tested against a real limit |
| **INV-223** | SHA-256 is computed by the backend from the exact received bytes. Client hashes and provider ETags never substitute for it. | The digest is the integrity claim every later verification rests on. | **ENFORCED** - chunk-independence test; digest re-verified at promotion |
| **INV-224** | Workspace and actor identity for an upload come from the authenticated context, never from multipart fields. | A body field is chosen by the client; reading identity from it is a complete tenancy bypass. | **ENFORCED** - the route never reads the body for identity, and `attachFieldsToBody` is false so there is no body to read |
| **INV-225** | Accepted artifacts are byte-identical to what was uploaded. No silent transformation, re-save or sanitization. | Rewriting bytes invalidates the digest, and therefore every signature derived from it. | **ENFORCED** - byte-exact round trip through real storage |
| **INV-226** | Artifact metadata is never committed before the accepted bytes exist, and an accepted upload always names its artifact. | A row claiming an artifact whose bytes do not exist is a document the product believes it has. | **ENFORCED** - ordering test plus a database CHECK constraint |
| **INV-227** | No PostgreSQL transaction spans file transfer, PDF inspection or malware scanning. | Those take seconds. A transaction held across them holds a connection and its locks for the length of an antivirus scan. | **ENFORCED** - one short transaction at acceptance; reviewed and documented |
| **INV-228** | Rejected or failed uploads create no accepted artifact and no signing evidence. | An upload rejection is not an event in a signing transaction. | **ENFORCED** - failure tests assert zero artifacts; no evidence write exists in this path |
| **INV-229** | Document bytes, malware payloads, storage credentials and scanner secrets are never logged, and never placed in queue payloads. | A log aggregator is a wider audience than the workspace that owns the document. | **PARTIALLY ENFORCED** - no logging exists in this path yet; classification documented, queue rule inherited from INV-198 |
| **INV-230** | Quarantine cleanup is driven by database records, is bounded by a horizon, and is idempotent. | A cleanup job that lists a bucket to decide what to delete is a job that can delete something real. | **ENFORCED** - row-driven; run twice in a test; in-flight rows excluded |

## Account identity and registration (BACKEND-19)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-231** | ONE canonical email normalization rule (trim + lowercase) is used by every auth flow, and no adapter normalizes independently. | Two flows disagreeing about identity produces either a duplicate account or an authentication bypass. | **ENFORCED** - single normalizer, repository-boundary assertion, probed by removing lowercasing |
| **INV-232** | Normalized email uniqueness is enforced by PostgreSQL. An application pre-check is behaviour, never the guarantee. | Two concurrent registrations both pass a pre-check; only the constraint decides. | **ENFORCED** - unique constraint; six simultaneous registrations produce one row |
| **INV-233** | Plaintext passwords are ephemeral, never persisted, never logged, and never altered by trimming or normalization. | A stored or logged password is a breach; an altered one means two inputs authenticate one account. | **ENFORCED** - marker tests across DB, logs and response; probed by trimming |
| **INV-234** | Password hashing runs after cheap validation and abuse controls, and outside any database transaction. | Argon2id is memory-hard by design; unlimited it is a DoS primitive, and inside a transaction it holds locks for its full duration. | **ENFORCED** - ordering tests; probed by hashing first |
| **INV-235** | Public registration never overwrites an existing account or replaces an existing password. | It would be an account takeover primitive: anyone could reset any account by re-registering it. | **ENFORCED** - the repository has no update path; verified against real PostgreSQL |
| **INV-236** | Account identity is GLOBAL, not workspace-scoped, and this is deliberate rather than a tenancy bypass. | A user exists before any workspace and may belong to several; requiring a tenant would make login impossible. | Documented + **ENFORCED** by schema - no workspace_id, no RLS, stated in the migration |
| **INV-237** | Email-verification credentials are cryptographically random, and only a DIGEST is stored. | The raw token is a bearer credential; storing it would let a database read verify every pending account. | **ENFORCED** - digest column with a shape CHECK; probed by storing the raw token |
| **INV-238** | Password hashes and verification digests never appear in a public projection or API response. | A field absent from the type cannot be serialized by accident. | **ENFORCED** - `UserRecord` has no hash field; closed response schema, probed |
| **INV-239** | Verification, session and CSRF tokens are domain-separated before digesting. | Otherwise one credential's digest could match another's stored value, and a token minted for one purpose would be accepted for the other. | **ENFORCED** - distinct prefixes; digest allowlist test |
| **INV-240** | The user row and its verification challenge commit atomically. | A user with no challenge can never verify; a challenge with no user references nothing. | **ENFORCED** - one transaction; rollback tested against real PostgreSQL |
| **INV-241** | A new account is unverified. Verification is never implied by successful registration. | Registration proves someone typed an address, not that they receive mail at it. | **ENFORCED** - repository always writes NULL; probed |
| **INV-242** | Password-hashing libraries are confined to the security adapter. | Everything above works with the PasswordHasher port, which is what makes the algorithm replaceable. | **ENFORCED** - one importing file; port used everywhere else |
| **INV-243** | Stored credentials must be Argon2id. | A bug that wrote a plaintext password or a weaker hash is refused by the database rather than discovered at login. | **ENFORCED** - database CHECK, tested by direct insert |
| **INV-244** | Verification URLs are built from configured application URLs, never an incoming Host header. | A Host-derived link sends the user's token to an attacker's domain. | **ENFORCED** - URL builder takes a configured base; tested |
| **INV-245** | Registration input may not set ids, roles, verification state, timestamps or account status. | Mass assignment. | **ENFORCED** - closed request schema plus explicit field mapping; probed |
| **INV-246** | Registration issues no session and no session-bound CSRF credential. | The frontend requires email verification before authenticated use; a session here would authenticate an unproven mailbox. | **ENFORCED** - no cookie set, asserted by test |
| **INV-247** | Registration never claims a verification email was sent unless a durable delivery path exists. | A false claim sends a user to wait for mail that will never arrive. | **ENFORCED** - the response carries `nextAction`, never a sent claim; asserted |

## Login and sessions (BACKEND-20)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-248** | Login resolves accounts through the SAME canonical email normalizer registration used. | A login that normalized differently would fail to find accounts registration created. | **ENFORCED** - one normalizer; every casing tested against a real registered account |
| **INV-249** | An unknown account and a wrong password produce identical public results - status, code, body and absence of metadata. | Any difference is an account-existence oracle. | **ENFORCED** - responses compared byte for byte; probed by splitting them |
| **INV-250** | An unknown account still runs a real Argon2id verification against a fixed dummy hash, unless rejected earlier by validation or rate limiting. | Returning early makes response time an existence oracle. | **ENFORCED** - probed by removing the dummy path |
| **INV-251** | Account eligibility is checked AFTER password verification, never before. | Checking first lets anyone learn verification state without knowing the password. | **ENFORCED** - probed by reordering |
| **INV-252** | Raw session and CSRF tokens exist only long enough to write cookies. They are never persisted, never logged, never serialized into JSON. | A credential in a log or a response body is a credential in someone else's hands. | **ENFORCED** - digest-only storage; marker tests over logs and bodies; closed response schema, probed |
| **INV-253** | Successful login always issues a FRESH session credential. An incoming cookie is never promoted to authenticated state. | Session fixation: an attacker plants a cookie, waits for the victim to log in, and inherits the session. | **ENFORCED** - planted-cookie test |
| **INV-254** | Login input may not set ids, roles, verification state or session identity. | Mass assignment. | **ENFORCED** - closed schema, probed |
| **INV-255** | Login-CSRF is defended by SameSite, exact-origin CORS and a server-side Origin check - not by declaring the route public. | A forged login authenticates a victim's browser as the ATTACKER, who then observes what they do. | **ENFORCED** - cross-site origin rejected before credential work; probed |
| **INV-256** | Browser authentication never returns a JWT, access token, refresh token or script-readable session credential. | A token a script can read is a token XSS can steal, and the session cookie already solves the problem. | **ENFORCED** - closed response schema; no such dependency exists |
| **INV-257** | Logout revokes server-side session state. Clearing a cookie alone is never sufficient, and a failed revocation is never reported as success. | A copied credential keeps authenticating until the server refuses it. | **ENFORCED** - revocation tested against real PostgreSQL; probed twice |
| **INV-258** | Logout is a POST and carries CSRF protection. | A GET logout can be fired by an image tag on any page on the internet. | **ENFORCED** - GET is not routed; CSRF is the standard authenticated-mutation hook |
| **INV-259** | Login is not wrapped in the generic idempotency replay framework. | Replaying a stored response would replay a session credential. | Documented - no idempotency wrapper on this route |
| **INV-260** | Session identity authenticates a user and implies no workspace authorization. | Permissions in a cookie cannot be revoked. | **ENFORCED** - the login response carries no workspace data |

## Email verification (BACKEND-21)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-261** | Verification credentials are opaque, cryptographically random, single-purpose and expiring, and their strength does not depend on rate limiting. | If entropy were weak, limits would only slow an attacker down. | **ENFORCED** - 60 bits, rejection-sampled; probed by shortening the code |
| **INV-262** | Raw verification codes and full verification URLs are never persisted or logged. | A stored code means a database read verifies every pending account. | **ENFORCED** - digest-only schema; marker tests over trace-level logs |
| **INV-263** | Challenge lookup uses a domain-separated digest, never a scan and never the raw value. | Domain separation stops a code minted for one purpose being accepted for another. | **ENFORCED** - unique indexed digest; probed by removing the prefix |
| **INV-264** | Successful verification transactionally consumes the challenge and sets the verification timestamp, and never rewrites an existing one. | The first verification time is account-security history. | **ENFORCED** - conditional updates, tested directly at the repository; probed twice |
| **INV-265** | A consumed, expired or superseded credential can never perform first-time verification. | Otherwise single-use and rotation mean nothing. | **ENFORCED** - probed for each state independently |
| **INV-266** | Resend rotates and supersedes prior active credentials rather than accumulating valid ones, and at most one challenge per user is active. | An unbounded set of live codes multiplies the chance one leaks. | **ENFORCED** - partial unique index; concurrent-resend test; probed |
| **INV-267** | A resend that cannot durably schedule delivery may not invalidate the currently usable credential. | Otherwise a queue outage strands an account with no way in. | **ENFORCED** - scheduling inside the rotation transaction; failure test |
| **INV-268** | Public resend never reveals whether an account exists, is verified, or is unverified. | Unlike registration the caller has asserted nothing about the address. | **ENFORCED** - one response shape; the route discards the reason; probed |
| **INV-269** | Provider delivery never verifies an email. Only redeeming a credential sets the verification timestamp. | A delivery receipt proves a message reached a server, not that the owner read it. | Documented - nothing in this path writes verification state from delivery |
| **INV-270** | A verification credential authorizes only email verification - never a session, a password reset, or workspace access. | Credential-purpose confusion is how one flow becomes an entry to another. | **ENFORCED** - verification issues no session, tested; digest domain separation |
| **INV-271** | Verification links are built from configured application URLs, never an incoming Host header. | A Host-derived link sends the code to an attacker's domain. | **ENFORCED** - builder takes a configured base; tested |
| **INV-272** | Verification state is server-authoritative and is never an immutable client-side claim. | A cached claim goes stale exactly when it matters. | **ENFORCED** - read from the account on every login |
| **INV-273** | Link-preview and security scanners cannot consume a verification credential: consumption is a POST, and the product collects a typed code. | Scanners fetch every link in a message before a human sees it. | **ENFORCED** - GET is not routed; tested |

## Password recovery (BACKEND-22)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-274** | Password-recovery requests use the same canonical email normalization as registration and login. | An address that resolves to one account at login and another at recovery is a way to request a reset for someone else. | **ENFORCED** — one normalizer, tested with mixed case and whitespace |
| **INV-275** | Public password-recovery requests never reveal whether an account exists. | Unlike registration, the caller has asserted nothing — and this oracle also emails the account it confirms. | **ENFORCED** — identical status, body and header set; the route discards the reason; probed |
| **INV-276** | Password-reset credentials are distinct from verification, session, CSRF, signing, OTP and API-key credentials. | A credential valid for two purposes is a way to convert proof of one thing into authority to do another. | **ENFORCED** — separate table, separate brand, separate digest domain; probed |
| **INV-277** | Reset credentials are opaque, cryptographically random, expiring and single-use, and their strength does not depend on rate limiting. | If entropy were weak, limits would only slow an attacker down. | **ENFORCED** — 256 bits; probed by shortening to 4 bytes |
| **INV-278** | Reset challenge lookup uses a domain-separated digest, never a scan and never the raw value. | Without the prefix, a verification code and a reset token that were the same string would digest identically. | **ENFORCED** — unique indexed digest; probed, and the test was rewritten after the probe caught nothing |
| **INV-279** | Raw reset credentials and full reset URLs are never persisted or logged. | A stored reset token means a database read is an account-takeover primitive for every pending reset. | **ENFORCED** — digest-only schema; the row is scanned for the raw value in a test |
| **INV-280** | At most one reset credential is active per account. | Every additional live link is another chance for the most dangerous credential in the system to be read by the wrong person. | **ENFORCED** — partial unique index; five concurrent requests leave one |
| **INV-281** | A replacement challenge and its durable delivery intent commit atomically, so a failed resend never invalidates the prior usable challenge. | Otherwise a queue outage leaves the old link dead and the new one unsent — an unrecoverable account. | **ENFORCED** — scheduling inside the transaction; failure test; probed |
| **INV-282** | Reset submission is rate-limited and cheaply validated before Argon2. | Argon2 is ~50ms of dedicated CPU by design; hashing before checking is a one-request-per-core denial of service. | **PARTIALLY ENFORCED** — ordering enforced and probed; the limiter is defined but not bound |
| **INV-283** | New reset passwords use the same server-side policy and Argon2id configuration as registration. | A reset path with its own rule is how an account ends up with a password its own signup form would have refused. | **ENFORCED** — the policy is imported, not restated; probed |
| **INV-284** | Reset links are built from configured application URLs, never an incoming Host header. | A Host-derived link makes LAGDA itself email a victim a link pointing at the attacker's domain. | **ENFORCED** — the builder takes a configured base; probed |
| **INV-285** | Successful reset transactionally replaces the password hash, consumes the challenge and revokes the required sessions. | A password change that leaves a stolen session alive achieves nothing — and the stolen session is usually why the user is resetting. | **ENFORCED** — one transaction; probed for each part |
| **INV-286** | A consumed, superseded or expired reset credential can never change a password, and two concurrent uses of one token produce exactly one change. | Otherwise single-use means nothing and the surviving password may be one the user never chose. | **ENFORCED** — conditional UPDATE; 4 concurrent resets verify exactly one candidate; probed |
| **INV-287** | Password reset never verifies an account email. | Possession of a reset link proves mailbox access, but verification is a separate account fact with separate consequences. | **ENFORCED** — `replacePasswordHash` sets one column; probed by making it set both |
| **INV-288** | Browser GET navigation to a reset page never mutates the password or consumes the credential. | Mail security scanners fetch every link in a message before a human sees it. | **ENFORCED** — GET is not routed on either endpoint; tested |

## Multi-factor authentication (BACKEND-23)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-289** | MFA credentials form a security domain separate from verification, reset, session, CSRF, signing and API credentials. | A credential valid for two purposes converts proof of one thing into authority to do another. | **ENFORCED** — own tables, own brands, own digest domains; a reset token presented as a code is refused |
| **INV-290** | The implemented factor is what the product actually has, and unsupported factors are neither implemented nor documented as available. | Claiming SMS or email MFA that does not exist misdescribes the product's security posture to whoever reads the docs. | **ENFORCED** — closed factor type with a database CHECK; inventory documents each classification |
| **INV-291** | TOTP secrets are stored encrypted, never in plaintext, and the key never enters the database. | This is the one credential the server must recover rather than compare; a dump of plaintext secrets is every user's second factor. | **ENFORCED** — AES-256-GCM; the row is scanned for plaintext in a test |
| **INV-292** | Every credential digest is domain-separated. | Seven credential types now digest to 64 hex characters; without prefixes any could resolve another's row. | **ENFORCED** — distinct prefixes and distinct brands |
| **INV-293** | OTP verification attempts are counted durably and atomically, per ceremony. | A 6-digit code is 10^6 possibilities; a read-modify-write counter gives an attacker many guesses for the price of one. | **ENFORCED** — increment computed by PostgreSQL; 8 concurrent attempts cost exactly 5; probed |
| **INV-294** | An exhausted ceremony can never authenticate, even with a correct code. | Otherwise the limit is a pause rather than a bound. | **ENFORCED** — ceiling in the UPDATE's WHERE clause; probed |
| **INV-295** | A code valid for its time window may be used once. | A code stays valid ~90 seconds; without a watermark one observed over a shoulder works again. | **ENFORCED** — conditional watermark advance; probed |
| **INV-296** | A malformed second-factor submission still costs an attempt. | Otherwise unlimited guesses are available by varying the length. | **ENFORCED** — tested |
| **INV-297** | Login MFA issues no code and sends nothing before the first factor succeeds. | Trivially satisfied by TOTP, which issues nothing at all — and the requirement stands for any future delivered factor. | **ENFORCED** — no ceremony is created for a failed password; tested |
| **INV-298** | An account requiring MFA receives no normal session until every factor succeeds. | A password alone must not produce access when the account has said it should not. | **ENFORCED** — the `mfa-required` result type has nowhere to put credentials; probed |
| **INV-299** | Pending MFA authentication uses a distinct, short-lived, restricted credential and is never a normal session. | Overloading one cookie makes "is this browser authenticated?" a question every middleware must answer correctly forever. | **ENFORCED** — separate cookie name, `Path=/auth`, 10-minute absolute life |
| **INV-300** | The final session credential is freshly generated after MFA and never reuses the pre-auth credential. | Promoting it is session fixation with extra steps. | **ENFORCED** — probed |
| **INV-301** | A pre-authentication credential cannot access normal application routes. | A half-finished ceremony is not authorization. | **PARTIALLY ENFORCED** — the cookie's `Path=/auth` means browsers do not transmit it to application routes, and the settings routes refuse it; but no auth route is composed into `createApp`, so this is not demonstrated in a running application (OD-069) |
| **INV-302** | MFA requirement and factor state are server-authoritative and cannot be set through any client-supplied field. | A client that could assert `mfaRequired: false` would have no second factor. | **ENFORCED** — read from the factor table; closed request schemas |
| **INV-303** | Password reset invalidates pending authentications based on the prior password proof, and does not disable MFA. | A ceremony started with the old password must not complete after a reset; and a weaker credential must not strip a stronger one. | **ENFORCED** — both probed |
| **INV-304** | Removing a second factor requires re-proving the password, not merely a session. | Disabling MFA is the first thing an attacker with a stolen session tries. | **ENFORCED** — probed |
| **INV-305** | Codes, secrets, provisioning URIs, recovery codes, pre-auth credentials, encryption keys and attempt counts never appear in logs, metrics or response bodies. | Any one of them in a log is the factor itself. | **ENFORCED** — response-body tests; no log statement on these paths |

## Account and profile (BACKEND-24)

| ID | Invariant | Why | Enforcement |
|---|---|---|---|
| **INV-306** | Generic account mutations may alter only explicitly approved profile and preference fields. | `users` holds the display name and the password hash in one table; only the set of columns a repository names keeps them apart. | **ENFORCED** — three repository methods, explicit column lists, no generic patch; probed |
| **INV-307** | Authentication-security state can never be mass-assigned through account APIs. | A profile form that could set `emailVerified` or `role` is a privilege-escalation primitive. | **ENFORCED** — closed request schemas; fourteen distinct payloads each rejected; probed |
| **INV-308** | The current user for `/me` and every account mutation comes exclusively from the validated session. | An id taken from a request body is an authorization check waiting to be written wrongly. | **ENFORCED** — no `:userId` path segment and no `userId` field exists to supply one |
| **INV-309** | Pre-authentication credentials cannot reach account APIs. | A half-finished MFA ceremony has proved a password and nothing more. | **PARTIALLY ENFORCED** — the route refuses it and the pre-auth cookie is scoped to `/auth`, but no route is composed, so this runs through a test double (OD-069) |
| **INV-310** | Account identity is global and separate from workspace membership and authorization. | Collapsing user and tenant is the mistake that makes multi-workspace impossible later. | **ENFORCED** — no workspace field on any account type or schema |
| **INV-311** | Account projections never expose password hashes, normalized identity, token digests, session or CSRF credentials, OTP verifiers, or MFA secrets. | One careless field is a credential in a response body. | **ENFORCED** — `CurrentUser` has no such fields; an attempt to add one does not compile |
| **INV-312** | Email identity changes use a dedicated security workflow and are never a generic profile edit. | Replacing a login identifier on a `PATCH` is account takeover by typo. | **ENFORCED by absence** — email is not on any mutation schema, and the product has no self-service change (documented for the future) |
| **INV-313** | Password change requires re-proving the current credential, not merely a session. | Changing a password from a stolen session locks the owner out using nothing but the theft. | **ENFORCED** — probed |
| **INV-314** | Password change uses the same policy and Argon2id configuration as registration and reset. | A settings path with its own rule is how an account gets a password its own signup form would refuse. | **ENFORCED** — the policy is imported; probed |
| **INV-315** | Sensitive account changes revoke the sessions the change is meant to protect against. | A password change that leaves other sessions alive achieves nothing when the reason is a suspected intruder. | **ENFORCED** — every other session revoked, current preserved; probed both ways |
| **INV-316** | Session management exposes safe projections only and scopes every operation to the authenticated user. | A revoke keyed on a session id alone lets anyone who learns one sign another account out. | **ENFORCED** — user-scoped WHERE clauses; probed |
| **INV-317** | Personal preferences use closed vocabularies and IANA time zones, never raw offsets. | An offset is wrong twice a year and cannot be corrected without knowing its origin zone — and `Intl` accepts one. | **ENFORCED** — shape check plus runtime check plus database CHECKs; probed |
| **INV-318** | Profile text accepts Unicode names and rejects only control characters. | An ASCII allowlist rejects a large share of this product's own users, and punctuation rejection is not an XSS defence. | **ENFORCED** — tested against real names; probed |
| **INV-319** | Mutable account and profile state can never rewrite historical signing evidence. | Someone renaming themselves must not change who signed a document last year. | **ENFORCED ARCHITECTURALLY** — no account code imports or reaches evidence tables |
