
  # Clickable eSignature Prototype

  This is a code bundle for Clickable eSignature Prototype. The original project is available at https://www.figma.com/design/RalHau8Pexocds0lQBZFOZ/Clickable-eSignature-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  